---
/**
 * /en/ — marketing landing, English. Same composition as the zh page
 * (LandingPage), reading the en landing YAML.
 */
import LandingPage from '../../components/landing/LandingPage.astro'
---

<LandingPage locale="en" />
