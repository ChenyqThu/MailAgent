# Virtual Environment
venv/
env/
ENV/

# Local UI reference (xulux base demo copied out of ~/Downloads for the chat redesign; never tracked)
.xulux-ref/

# Database and runtime data
data/
# M1 mem0 记忆 store（faiss / history.db / fastembed_cache，feature-owned = DATA_ROOT/mem0）
mem0/
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Environment variables
.env
.env.local

# Logs
logs/
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Temporary files
/tmp/
*.tmp
*.bak

# Claude Code
.omc/
.claude/worktrees/
.claude/scheduled_tasks.lock
.agents/
.codex/
AGENTS.md

# Env backups (含 secrets)
.env.bak.*

# 证书 / 私钥（绝不入库 —— PKCS#12 / PEM / 私钥；如 Developer ID Application 签名证书 cert.p12、CSC_LINK 来源）
*.p12
*.pem
*.key
*.cer
*.mobileprovision

# DavMail PoC 工作目录 (含 client_id 伪装 config 和 OAuth token, 整体不进 git history)
davmail-poc/

# Python build artifacts
*.egg-info/
/build/          # setuptools bdist 产物 (build-python-venv.sh 跑 pip install . 时生成于仓库根; 不影响 frontend/build/)

# Node / 前端构建产物
node_modules/
*.tsbuildinfo

# Coverage / profiling 产物
*.profraw

# Project specific
email-notion-sync/

# 项目周报同步临时产物（build*.py 生成 fixture, 含真实业务数据）
data/_probe_tmp/
tests/project_progress/fixtures/sample_*.xlsx
data/notion_ds_cache.json

# 工具产物（OMC 状态 / Playwright MCP 日志）— 不入库
.omx/
.playwright-mcp/

# --- v0.3.0 后清理（2026-06-04）: 本地运行时 / 产物（文件保留, git 不再 track）---
# AI chat 运行时库（dogfood 对话数据）+ 迁移备份（test 跑 schema migration 时落的
# .pre-vN.bak / -shm / -wal）。通配覆盖所有变体，防 `git add frontend/` 误纳入。
frontend/ai_chat.db*
# electron-vite/rollup build 误落根目录的 bundle 产物（正常应在 out/main|out/renderer/）
frontend/index.js
frontend/index-*.js
# 编辑器配置
.cursor/
# 验证截图 + 误创建的 .app 残骸目录
/live-*.png
/v2-app-*.png
/onboarding）/

# --- 第三方 skill 安装产物（transitions-dev 等，留本地不进 git）---
/skills-lock.json
.claude/skills/transitions-dev

# --- Trellis 工具（用户裁定: 留本地工具, 不进 git）---
.trellis/
.claude/agents/trellis-*.md
.claude/commands/trellis/
.claude/skills/trellis-*/
.claude/hooks/inject-subagent-context.py
.claude/hooks/inject-workflow-state.py
.claude/hooks/session-start.py

# Phase 1 skill pack (generated by scripts/export_skill_pack.py)
/dist/mailagent-skill-pack/

# chat-panel P4: transient agent_eval run-output reports (run_baseline --compare/score)
tests/agent_eval/baselines/*.report.*

# design-sync (claude.ai/design) — only machine-local build artifacts are ignored;
# durable config + authored previews (.design-sync/{config.json,NOTES.md,conventions.md,
# overrides,stubs,scripts,preview-support,previews}) ARE committed.
/.ds-sync/
/ds-bundle/
/.design-sync/.cache/
/.design-sync/node_modules
/.design-sync/learnings/
scripts/backfill_thread_id_rfc2047.journal.json
scripts/backfill_message_id_normalize.journal.json

# impeccable 插件：项目级设置（live/config.json）入库，本机态与缓存不入库
# —— hook.cache.json 含 session UUID 与绝对路径，config.local.json 是本机同意状态。
.impeccable/hook.cache.json
.impeccable/config.local.json
