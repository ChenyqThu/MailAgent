# Notion API
notion-client>=3.0.0

# HTML 解析
beautifulsoup4>=4.12.3
html2text>=2024.2.26
lxml>=5.2.0  # Python 3.13 需要 5.2.0+

# 配置管理
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1

# 日志
loguru>=0.7.2

# 异步 IO
aiofiles>=24.1.0
aiohttp>=3.10.0  # Python 3.13 兼容性改进

# iCalendar RRULE 展开（间接依赖 pandas，但直接 pin 防 pandas 升级断链）
python-dateutil>=2.8.0

# Custom Agent cron 触发（S4，flag MAILAGENT_CUSTOM_AGENTS_ENABLED，默认关）：
# AgentTriggerWorker 用 croniter 解析 5-field cron + get_prev 判定 fire window。
# 自研 cron 边界 case（DST / 闰 / 步进）是著名坑源，用成熟小库（纯 Python，无重传递依赖）。
# 运行期仅 flag 开时 import；打包须重新生成 requirements.lock.txt 才进嵌入式 venv。
croniter>=2.0.0

# CalDAV 日历同步（calendar_sync worker：DavMail CalDAV 桥 → SQLite SSoT）
# 之前只在 dev venv 手动装过，没进 requirements → 打包 app 缺 caldav，
# CalDAVReader import 失败、每分钟重试。caldav 会带 icalendar / recurring-ical-events
# 等传递依赖；显式 pin calendar_sync 直接 import 的三个，防传递依赖漂移断链。
# vobject 自 caldav 3.2.1 起不再是传递依赖，须显式声明。
caldav>=3.2.0
icalendar>=7.1.1
recurring-ical-events>=3.8.2
vobject>=0.9.9

# Redis
redis>=5.0.0

# 图片处理
Pillow>=11.0.0  # Python 3.13 需要 11.0.0+

# Office 文档转换 (xlsx → csv) + 附件文本化 (PR-2b: FTS5 索引 + LLM context)
pandas>=2.0.0
openpyxl>=3.1.0
python-calamine>=0.2.0  # Rust 引擎，xlsx 读取加速 4-18x
pypdf>=4.0.0            # PR-2b: PDF 文本提取
python-docx>=1.1.0      # PR-2b: docx 文本提取
python-pptx>=1.0.0      # PR-2b: pptx 文本提取

# 附件 OCR（批次4 PR-G，flag MAILAGENT_ATTACHMENT_OCR_ENABLED 默认开）：图片附件 +
# 无文本层扫描件 PDF 经 macOS Vision.framework 识别中英文本。pyobjc-framework-Vision
# 连带 pyobjc-core / Cocoa / Quartz（PDF 渲染）/ CoreML ≈7.1MB 压缩，钉版本防 ABI 漂移。
# 运行期在 src/converter/vision_ocr.py 懒 import（缺失软着陆不崩）；打包须重新生成
# requirements.lock.txt 才进嵌入式 venv。macOS-only（Vision 是系统框架）。
pyobjc-framework-Vision==12.2.1

# 飞书对话 bot 长连接（08-01 阶段 2 PR-2，flag MAILAGENT_IM_FEISHU 默认关）：
# 飞书官方 Python SDK，含 WebSocket 长连接客户端（免公网回调）+ im/v1 消息 API。
# 🔴 它要求 websockets<16,>=11 —— requirements.lock.txt 的 websockets 因此从 16.0
# 降到 15.0.1（同时满足 uvicorn[standard]>=10.4 与 openai[realtime]<16,>=13，且仓库
# 无任何代码直接 import websockets）。运行期仅 flag 开时在 src/im/ 内 import；
# 打包须重新生成 requirements.lock.txt 才进嵌入式 venv。
lark-oapi>=1.7.1,<2

# LLM Agent (本地 LLM 接管 Notion Email Agent 的 AI 字段填充，默认关闭)
anthropic>=0.40.0


# v4 SQLite SSoT: HTML→Markdown 主路径（用于 email_body.body_markdown）
markdownify>=0.13.1
