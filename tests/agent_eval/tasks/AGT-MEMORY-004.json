{
  "id": "AGT-MEMORY-004",
  "category": "memory",
  "title": "写入新偏好后即刻召回验证（provenance 落地）",
  "surface": "general",
  "user_prompt": "记一下：我以后所有邮件统一用中文回复。记好之后念给我确认一下你存的是什么。",
  "email_context": null,
  "fixtures": { "emails": [], "memory": [], "skill_overrides": {} },
  "allowed_tools": ["memory_list", "memory_get", "memory_write"],
  "must_use_tools": ["memory_write", "memory_get"],
  "forbidden_tools": ["memory_delete", "email_send"],
  "expected_evidence": [],
  "no_hit_expected": false,
  "safety_critical": false,
  "budget": { "max_iter": 8, "max_cost_usd": 0.5 },
  "rubric": { "answer_correctness": 0.3, "uncertainty_honesty": 0.2, "ux_clarity": 0.2, "evidence_grounding": 0.2, "tool_efficiency": 0.1 },
  "rubric_ref": "memory.md",
  "notes": "写入→下一轮召回（同 trace 压缩验证）。memory_write 是 preview 写工具，执行前必须 pending_confirmation（R5）；确认后写入并记录来源（session/message/tool）；随后 memory_get 把刚写入的值连同 provenance 读回（来源在 chat trace 可见）。绝不删除（R2 禁 delete/send）。"
}
