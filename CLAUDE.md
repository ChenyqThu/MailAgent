# CLAUDE.md

为 Claude Code 提供的项目指南。

> **本文件是精简索引** —— 只保留每个 session 必须在场的核心约束、导航、速查。
> 深度内容（架构内核、各子系统运维、CLI 全表）按需下沉到 `docs/reference/<子系统>/`，
> 需要时用「文档地图」里的指针去 `Read`，不要全量塞进 context。
> 改完某子系统的运行语义后，同步更新它在 `docs/reference/` 的常青文档，别把流水账堆回这里。
>
> **文档规范** → [`docs/DOC-GUIDE.md`](./docs/DOC-GUIDE.md)：常青参考进 `docs/reference/`，过程产物（handoff/complete/phaseN/prN）进 `docs/archive/{年-月}/`；**新增常青文档必须在下面「文档地图」加一行**，否则无人发现。

## 通用指南

- 被要求做具体修改时，直接动手。不要花大量时间读文件或反复确认简单任务，偏向行动。
- macOS 环境下 **没有 sudo**，不要尝试 sudo 命令。
- 不要在嵌套 session 中做 CLI 更新或全局变更。
- 遇到环境问题时，优先检查已知的 macOS 限制（FDA 权限、symlink、沙盒）再尝试修复。

## 文档地图（渐进式加载索引）

| 主题 | 何时读 | 路径 |
|---|---|---|
| 架构内核（v3 流程 / 重试 / Processing Status / webhook / 线程 / Sprint15 outbox / Sprint16 dual-backend / **跨语言手抄常量的一致性闸**） | 改正/反向 sync、webhook、状态机前；**在第二处手抄某个常量·枚举·派生表前** | [`architecture/architecture-internals.md`](./docs/reference/architecture/architecture-internals.md) |
| DavMail 写路径 trace + Notion 反向链路(B1) 退役决策依据（写op×路径 / B1 现状 / outbox 灰度死分支 / AppleScript fallback 链） | 退役 Notion 反向链路 / 清 outbox 死分支 / 动 reverse_sync·handlers 前 | [`architecture/davmail-write-path-trace.md`](./docs/reference/architecture/davmail-write-path-trace.md) |
| **排程规则跨端契约**（`kind:'schedule'` 结构化排程：RRULE 语义[🔴 WKST=SU / clamp 非 RRULE 分支 / DST 两种落点 / anchor 相位] + 星期编号双口径[契约 0=周日 vs Python 0=周一] + 老形状惰性映射与空时区写实 + 两个 worker 共用求值器 + 黄金 fixture 跨语言闸） | 动 custom agent 定时触发 / 报告 Agent 排程 / schedule-builder 预览 / `src/agents/schedule_rule.py` 前 | [`architecture/schedule-rule-contract.md`](./docs/reference/architecture/schedule-rule-contract.md) |
| **架构整体 Review 2026-07 + E0-E4 收敛路线**（P0=CI 测试闸/数据安全网；P1=半程迁移收口：Backend 契约/减法 Sprint/配置治理/可靠性；EWS=跟随 davmail 上游零工程，仅留 watch 项） | 启动 E0-E4 任一收敛 epic / 排架构债优先级前 | [`architecture-review-2026-07/README.md`](./docs/plans/architecture-review-2026-07/README.md)（docs/plans，含 e0-e4 五份实施方案） |
| LLM Agent（本地 LLM 分类，fallback / cache / 监控 / payload） | 改邮件分类、prompt、cache 前 | [`llm-agent/llm-agent.md`](./docs/reference/llm-agent/llm-agent.md) + [`llm-agent/LLM_AGENT_SETUP.md`](./docs/reference/llm-agent/LLM_AGENT_SETUP.md) |
| LLM Provider Registry（上游多 provider：`agent_config.db` 双表 + snapshot 契约 + TS AI SDK registry + Python protocol 路由 + seed/回退/凭证面） | 动 provider 配置 / gateway 模型层 / 协议路由 / 模型选择链 / `MAILAGENT_LLM_PROVIDER_REGISTRY` 前 | [`llm-agent/llm-provider-registry.md`](./docs/reference/llm-agent/llm-provider-registry.md) |
| 项目周报同步（外挂模块，xlsx → Notion） | 动 `src/project_progress/` 前 | [`project-progress/project-progress-sync.md`](./docs/reference/project-progress/project-progress-sync.md) |
| 报告 Agent 系统（日/周/月 + custom artifact，18 种 ReportDoc 块 + 定时生成 + 前端渲染 + KOS 工具桥） | 动 `src/reports/` / 报告 / Custom AI Agents 区前 | [`remote-chat-report/report-agent-prd.md`](./docs/reference/remote-chat-report/report-agent-prd.md) |
| Calendar Module（CalDAV → SQLite SSoT） | 动日历同步 / `calendar_event` 表前 | [`calendar/calendar-ops.md`](./docs/reference/calendar/calendar-ops.md) + [`calendar/calendar-module-prd.md`](./docs/reference/calendar/calendar-module-prd.md) |
| v4 SQLite-SSoT（body/附件 SSoT + FTS5 全文搜索） | 动 `EmailRepository` / 双写 / 搜索前 | [`architecture/v4-ssot-ops.md`](./docs/reference/architecture/v4-ssot-ops.md) + [`architecture/architecture_v4_sqlite_ssot.md`](./docs/reference/architecture/architecture_v4_sqlite_ssot.md) |
| 后端服务层（统一写面：`src/services/` 应用服务 + CLI/serve-api 薄适配器 in-process + async-jobs + 双层鉴权 + 前端 daemon 转发） | 改写操作（flag/resync/archive/pin/llm/compose/send）/ 加传输端 / 动 `src/services/` 前 | [`architecture/service-layer-architecture.md`](./docs/reference/architecture/service-layer-architecture.md) + `~/.claude/plans/cli-streamed-brook.md` |
| AI Agent Harness + KOS（前端 chat 多轮 agent + 跨域知识图；**S3 起 AI SDK Gateway 是唯一引擎，旧自研 harness 已删除**） | 动前端 chat / gateway / KOS 集成前 | [`llm-agent/ai-sdk-gateway-architecture.md`](./docs/reference/llm-agent/ai-sdk-gateway-architecture.md)（**唯一引擎权威**：embedded gateway / 工具 / HITL 审批 / A2UI / memory / standing-context / serve-api 代理，§13）+ [`llm-agent/kos-integration-design.md`](./docs/reference/llm-agent/kos-integration-design.md) |
| Skill Delivery API（对外 agent 交付面：scoped Bearer key 第四腿 + Python Skill manifest + `/api/skills(+invoke)` + MCP stdio + skill pack） | 动 scoped key / `src/skills` / `/api/skills` / MCP / pack 前 | [`llm-agent/skill-delivery-api.md`](./docs/reference/llm-agent/skill-delivery-api.md) |
| Capability & Context Foundation（Phase -1/0A：backend `agent_config.db` + Standing Context 文档体系 + installed skill registry + @mention scope 激活 + 配置快照 hash） | 动 `src/agent_config` / `/api/agent/*` / standing-context prompt / installed skill / @mention 前 | [`llm-agent/capability-context-foundation.md`](./docs/reference/llm-agent/capability-context-foundation.md) |
| **MCP Connectors**（外部服务工具面 · **双轨**：Notion/Atlassian 自建 MCP 直连 + 其余 14 家 Composio 托管；`agent_config.db` 双表 + OAuth 2.1/PKCE/DCR + needs_reauth/orphan 状态机 + **独立配置台 `/connectors`** + **per-tool 三档 `auto|ask|off`**[08-05 拆「写恒 HITL」地板：NULL=默认 auto 写可免卡 / owner-present ask 恒卡 / off 不注册；预处理 read 硬天花板已拆、该场地 ask≙不注册；im_chat 写类跟随] + headless `grant_connectors` + destructive 红警告[delete 分类 08-03 已退役] + 灰度 flag） | 动 `src/connectors/` / connector 端点 / gateway connector 工具注入 / 第七能力卡 / **`/connectors` 配置台或内置工具审批档 UI** / `MAILAGENT_MCP_CONNECTORS` 前 | [`llm-agent/mcp-connectors.md`](./docs/reference/llm-agent/mcp-connectors.md) |
| **飞书对话**（IM agent 场地：lark-oapi 长连接挂 serve + `im_chat` 第四态工具矩阵 + 飞书内按钮审批闭环 + 会话映射与桌面可见 + 独立凭证/绑定码 + 与通知 bot 的隔离边界） | 动 `src/im/` / `/api/im/*` / gateway `im-chat` 入口 / 设置-AI「飞书对话」区 / `MAILAGENT_IM_FEISHU` 前 | [`llm-agent/im-feishu-chat.md`](./docs/reference/llm-agent/im-feishu-chat.md) |
| V2.1 远程 chat + report/agent（B-pure-unified 传输层；一份 serve-api；本地 token webRequest / 远程 CF cookie；chat 引擎 = embedded AI SDK Gateway（Electron main 进程内，S3 起唯一引擎，旧 `shared/chat` 已删）；远程 web 经 serve-api `ai_gateway_proxy`(httpx stream 反代) → loopback gateway:8300，恒 ai-sdk、无 flag） | 动远程 web chat / serve-api chat 端点 / chat 引擎语义前 | [`remote-chat-report/remote-chat-report-architecture.md`](./docs/reference/remote-chat-report/remote-chat-report-architecture.md) + [设计](./docs/reference/remote-chat-report/v2.1-stage3-chat-platform-design.md) + [看板](./docs/reference/remote-chat-report/v2.1-remote-chat-report-matrix.md) |
| **Agent 体验大版本专项**（P0-P4 换引擎 **✅ cutover v0.20.0**；mem0 记忆核心重构 **M1-M5 ✅ + web→ai-sdk(任务A) ✅ + Settings 身份文档编辑器 ✅**（M1-M4 随 **v1.0.1**；M5b[agent_memory_kv 退役] + 4 flag cutover 默认 ON 随 **v1.1.0**）；**任务B[删 legacy harness] ✅**（S3，见 `07-02-s3-remove-legacy-harness` task）；P0-P4 过程档已归 `docs/archive/2026-06/`，M1-M5 计划档已归 `docs/archive/2026-07/agent-experience-epic/`） | 理解 mem0 记忆演进史 / 跑 agent 回归网前 | 引擎架构 [`llm-agent/ai-sdk-gateway-architecture.md`](./docs/reference/llm-agent/ai-sdk-gateway-architecture.md) §13.18 + M1-M5 计划（已归档）[`memory-skill-core-refactor.md`](./docs/archive/2026-07/agent-experience-epic/memory-skill-core-refactor.md) + 回归网 `tests/agent_eval/` |
| CLI 完整命令表 + 退出码 + schema 契约 | 查命令明细 / 加 CLI 命令前 | [`cli/cli-reference.md`](./docs/reference/cli/cli-reference.md) + [`cli/agent-cli-rfc.md`](./docs/reference/cli/agent-cli-rfc.md) |
| 存档/草稿箱 + 多文件夹同步（folder_sync） | 动 folder 同步前 | [`folder-sync/multi-folder-sync-design.md`](./docs/reference/folder-sync/multi-folder-sync-design.md) + [`folder-sync/folder-ui-prd.md`](./docs/reference/folder-sync/folder-ui-prd.md) |
| Webhook / SSE / Openclaw / Notion API（集成面） | 动 webhook-server / 事件流 / 飞书回调前 | [`integrations/`](./docs/reference/integrations/) |
| 邮件搜索（**单核 CORE#1** + Query DSL + 多模式 FTS5 + agentic AI 搜索） | 改搜索语法 / 检索引擎 / agentic 前 | [`search/search-query-syntax.md`](./docs/reference/search/search-query-syntax.md) |
| 灵动岛 Ping Island 集成 | 动通知/ack 中心前 | `~/.claude/plans/ultrathink-session-curious-cloud.md` |
| 前端动效 + 列表性能铁律（Electron renderer：GSAP §8 动效 / snippet 懒取 / 线程批量 / 查询缓存 / 正文 iframe 链接） | 动前端列表/正文/动效前 | [`frontend/ARCHITECTURE.md`](./frontend/ARCHITECTURE.md) §7.1-7.2 + [`frontend/docs/motion-gsap.md`](./frontend/docs/motion-gsap.md) |
| **前端设计体系 v3「原生材质」**（token SSoT=index.css：圆角四档 --r-ctl/8·--r-row/9·--r-card/12·--r-pop/14 + 选中签名 --sel-wash 药丸（左条=导航面专属 sidebar/rail/会话行，邮件行无条且线程单行选中）+ 未读前景色 650 + grain/specular/静态辉光/列表悬浮删除钮已退役 + 列表 scrollbar-none + 动效红线清单；EmailRow 药丸=透明 border+clip+补偿圆角，见 ARCHITECTURE §7.3） | 改主题/token/选中态/圆角/装饰层前 | [`frontend/DESIGN.md`](./frontend/DESIGN.md)（v3 节）+ 落地台账 [`theme-v3-native-material/README.md`](./docs/plans/theme-v3-native-material/README.md) |
| 桌面 App 打包 / 发布（一体化 .app + 版本号机制 + 签名闸 + 故障排查） | 出新版 / 发布 App 前 | [`packaging/packaging-release.md`](./docs/reference/packaging/packaging-release.md) |

技能（按需触发，正文不常驻）：`/deploy`（部署验证）、`/debug`（系统化排查）、`/health`（健康巡检）、`/db-migration`（schema 升级）、`/sprint-handoff`（交接文档）。

## 项目概述

**MailAgent** 是一个 macOS 邮件实时同步系统，将 Mail.app / Outlook 邮件同步到 Notion，支持：
- 邮件内容、附件、线程关系同步 + 会议邀请（iCalendar）→ 日程
- AI 分类与处理（本地 LLM / Notion Custom Agent）
- 双向 Flag 同步（Mail.app ↔ Notion，Sprint 15 起统一走 outbox + FanoutWorker 异步派发）
- 飞书应用机器人通知（重要邮件推送 + 交互式回复按钮 → Openclaw）
- Notion Webhook → Redis → Mail.app 实时事件驱动
- Office 附件自动转换（docx/pptx→PDF, xlsx→CSV）

**当前架构状态**（演进叠加，详见 [`docs/reference/architecture/architecture-internals.md`](./docs/reference/architecture/architecture-internals.md)）：
- **v3 SQLite-First**（2026-01）：`internal_id`（ROWID = AppleScript id）主键，`whose id is <int>` 查询比旧方式快 127 倍，支持 6-7 万封大邮箱。
- **Sprint 15 SQLite SSoT inversion**（2026-05）：所有 mutating 操作反转方向，SQLite 是写入 intent 聚合点，FanoutWorker 异步派发到 Mail.app + Notion，统一走 `email_outbox`（E2 2026-07 起 outbox **恒启用**，灰度开关 `MAILAGENT_OUTBOX_ENABLED` 与老直调分支已退役；三处 flag→outbox 入队归一 `src/sync/outbox_intents.py`）。
- **Sprint 16 Dual-Backend**（2026-05-22 cutover）：抽象 `IMailBackend` Protocol，**davmail 模式为当前主路径**（IMAP/SMTP/CalDAV 桥 EWS），AppleScript 保留作 emergency fallback。
- **v4 SQLite-SSoT**（2026-05，Phase 1-4 已上线/灰度）：SQLite 是邮件正文 + 附件的 SSoT，Notion 退化为镜像，FTS5 全文搜索就位。

**死硬约束**：
- DavMail 当前用 Outlook for Windows well-known client_id 伪装（PoC），**不可上生产** —— 需走公司 IT 审批（推荐直接申请 Graph API）。
- EWS 2026-10-01 关停，DavMail 6.7 仍走 EWS，Graph 路线图（Issue #404）未 merge —— 见 [`docs/reference/architecture/roadmap-post-cutover.md`](./docs/reference/architecture/roadmap-post-cutover.md) §5.1。
- AppleScript fallback 路径**始终可用** —— 任何重构都必须保证 emergency 回切不丢数据（回切步骤见 architecture-internals.md）。

**技术栈**：Python ≥3.9（本地 3.11+，远程 webhook-server 3.9+）· AppleScript（fallback）· DavMail 6.7 JVM（主路径）· SQLite（状态 + v4 SSoT + FTS5）· Notion API · BeautifulSoup/lxml · Pydantic · Redis · FastAPI · LibreOffice headless · pandas + python-calamine。

## 关键开关现状（代码默认值；★ = 生产已偏离默认）

| 开关 | 代码默认 | 说明 |
|---|---|---|
| `MAILAGENT_BACKEND` | `applescript` | ★ 生产 = `davmail`（Sprint 16 cutover 后） |
| `DRAFTS_SYNC_ENABLED` | `true` | 草稿箱同步（davmail-only）：Exchange Drafts 全量 UID 对账进 `email_metadata`（mailbox='草稿箱'，编辑/发送/删除会同步删本地行），仅本地（列表/数量/正文/FTS），不进 Notion / LLM / 飞书 / KOS / 报告 |
| `MAILAGENT_INBOUND_READ_RECONCILE_ENABLED` | `false` | 入向「未读→已读」单向回收（davmail-only，issue #58）：外部客户端（Outlook/OWA）标已读后把本地未读收敛为已读。**只做未读→已读单向**（已读→未读 / flagged 不碰）、**只收件箱**、**恒走 outbox→notion**（绝不直调 Notion——Sprint15 死循环根因）。🔴 **判定分两步：`UID SEARCH UNSEEN` 只出候选，提交前对候选 uid 定向 `UID FETCH (UID FLAGS)` 复核才是判据**——「不在 UNSEEN 集」有三种成因且在 SEARCH 结果里长得一样：被外部标已读 / 已归档·删除·被规则搬出 INBOX（**仍未读**）/ 窗口内 UID 空洞；只凭缺席就写库 = 把归档掉的未读邮件永久误标已读。复核语义：uid **未返回**→已移出 INBOX→跳过；返回但无 `\Seen`→仍未读（SEARCH 快照可能耗时数分钟、期间又被标未读）→跳过；返回且含 `\Seen`→才收敛。两次会话之间 UIDVALIDITY 变了整轮作废。五道安全闸：(a) 该邮件 outbox 有**未终态**（pending/processing/failed）flag_sync intent → 本轮跳过（避 fanout 窗口误判；只认 pending 会漏掉正在派发/退避重试的在途 intent）；(b) `mailapp_payload=None`（服务器是已读真源，不回写 Mail.app）；(c) 本地 imap_uidvalidity ≠ 服务器 UIDVALIDITY 的行整批跳过（uid 全失效防误判）；(c2) 🔴 **截断窗口下界闸**——`davmail.folderSizeLimit`（>10k 邮箱必配）让 IMAP 视图只剩最近 N 封，窗口外老邮件在 UNSEEN 里必然缺席，**缺席 ≠ 已读**；故 SELECT 后按序号 FETCH 视图最老 UID 当下界，只判 `imap_uid >= 下界` 的行（少了它会把真未读老邮件批量误标已读）；(d) 🔴 **提交原子性**——「(a) 在途 intent 复核 + `WHERE is_read=0` 的 CAS 置已读 + notion 入队」在**同一个 `BEGIN IMMEDIATE` 事务**内（`src/sync/outbox_intents.py::converge_local_read_atomic`，两表同库故一个连接盖得住）：分开提交时 ① 入队失败会留下「本地已读但 Notion intent 永久缺失」（下轮只查未读行，该封再不进候选）② 闸与写之间的 TOCTOU 会把用户刚点的「标为未读」吞掉（json_patch 合并覆盖）。🔴 **收敛必须在工作线程**——`converge_local_read_atomic` 是同步 SQLite 事务，锁**等待**若发生在 event loop 线程上，别的进程持写锁时每封候选都能把整个 watcher（fanout / reverse / island 全部 worker 的 tick）冻住；故整块 `asyncio.to_thread` 出去（本地未读集查询同理），且传**短** busy timeout（`READ_RECONCILE_LOCK_TIMEOUT_SEC=2s`，远低于 outbox 默认 30s——收敛是可跳过的便利型写，下轮必重判）；超时抛 `ConvergeLockBusy` 保守让路，且**第一次锁超时就终止本轮**（不是"连续 N 次"——锁超时与成功交替出现时"连续"计数永远清零，chunk 里靠后的封会在前面几十次 × 2s 的锁等待之后才提交，把下面那个残留竞态窗口撑到近百秒）；已提交的前序封不回滚，被跳过的封下个周期重新 FETCH 复核重判（收敛幂等，不丢状态）。另有**累计耗时预算**（`READ_RECONCILE_CHUNK_BUDGET_SEC=3s`）兜住另一形态：别的 writer 在每封之间反复抢放锁时，每封都在 timeout **之前**拿到锁 → 每封都成功、`ConvergeLockBusy` 一次都不抛，但 100 封 chunk 能累计到分钟级、seen_map 一样在变旧；故单 chunk 收敛累计超预算即同样终止本轮（`abort_reason` 分 `lock_timeout`/`time_budget`，warning 文案可辨）。取 3s 的依据=无竞争时整个 chunk 全是本地语句（100 封 × 个位数毫秒 ≈ 0.2-0.5s），留一个数量级余量不误伤正常路径。⚠️ **已知残留竞态（有意不消除，有测试固化）**：定向 FETCH 返回 `\Seen` 之后、本轮事务提交之前，用户在 Outlook 标回未读——此刻**尚无**任何本地写或 outbox intent，CAS 与在途闸都看不见 → 该封仍被收敛，且本功能单向、不自愈。处置=复核与提交按 chunk 紧邻（`READ_RECONCILE_CONVERGE_CHUNK=100`，把窗口从「整批候选处理时长」收窄到「单 chunk 内、该封之前那些收敛事务的累计时长」——无锁竞争时全是本地语句、毫秒级；一旦有一封等锁超时、或整个 chunk 的收敛累计超过 `READ_RECONCILE_CHUNK_BUDGET_SEC` 即终止本轮，既不让锁等待逐封累加撑大窗口，也不再消费已经变旧的 FETCH 快照；两道闸合起来把窗口钉在常数量级「预算 + 一次锁等待」≈5s，与候选量、chunk 长度都解耦），再窄一档要 CONDSTORE/MODSEQ（DavMail 经 EWS 桥支持存疑，且**写在本地**、TOCTOU 依旧只是窗口更小，并非闭合），真正闭合只有提交后补偿事务（撤销本地已读 + 覆盖可能已派发的 Notion intent，而「覆盖」正是 Sprint15 死循环那条出向路径，补偿本身又有自身竞态、代码量超过功能本体），对默认关的便利功能不成比例；用户侧兜底=再标一次未读。🔴 **绝不挂 5s radar poll**——独立低频周期（`MAILAGENT_INBOUND_READ_RECONCILE_INTERVAL_SEC` 默认 300s），radar 每轮只用轻量 STATUS，把 UID SEARCH UNSEEN 挂每轮会重现 EWS 全量枚举限流（issue #46）。收敛后整轮只发一条 `email.flag_changed`（`internal_id=None`，不按封刷屏），data 携带**有界** `internal_ids`（上限 `READ_RECONCILE_EVENT_ID_CAP=200`，超限 `ids_truncated=true` 让前端退化成失效所有活跃 detail）——不带 id 时只失效 main-list + `mailboxes`，打开中的邮件详情 toolbar 会一直显示未读。默认关（灰度）；false = 字节级 inert。AppleScript 模式不激活（缺 `fetch_inbox_seen_flags` 即整段不激活——没判据就不许猜）。详见 new_watcher `_reconcile_inbound_read` |
| `MAILAGENT_INBOUND_READ_RECONCILE_INTERVAL_SEC` | `300` | 入向已读回收的独立低频节拍（秒），与 5s radar poll 解耦。仅 `MAILAGENT_INBOUND_READ_RECONCILE_ENABLED` 开时生效 |
| `DAVMAIL_FOLDER_SIZE_LIMIT` | `500` | IMAP 文件夹视图只保留最近 N 封（davmail-only）。**2026-07-24 停摆事故的可配置化**：10617 封收件箱 + 该项从未配置 → 每次 `SELECT`/`STATUS` 都让 DavMail 经 EWS 全量枚举，连雷达的 `STATUS (UIDNEXT)` 都撞 30s 超时、整条同步链停摆（实测裸 IMAP greeting 16.7s）。🔴 **它是 DavMail 自己的参数（`davmail.folderSizeLimit`），App 只是代写**：mail-sync 启动时（davmail 模式）由 `src/mail/davmail_properties.py` 写进 `<DAVMAIL_ROOT>/config/davmail.properties`，**写文件 ≠ 生效 —— DavMail 只在自己启动时读配置，须 `pm2 restart davmail-poc`**；`DAVMAIL_ROOT` 没配（打包 .app 的默认解析落进 site-packages）→ 文件不存在 → 该设置**根本不生效**。故同步结果落 sync_state `davmail.folder_size_limit.*` → `admin.davmailHealth` → 设置-账户-DavMail 面的状态行**如实**告知（file_missing 明说"当前不生效"）。0 = App 不管这个键。代价：窗口外老邮件在 IMAP 层不可见（本地已同步的历史不受影响，只影响增量同步与写操作可触达范围）；这也是入向已读回收 (c2) 下界闸存在的原因 |
| `BODY_DUAL_WRITE_ENABLED` | `true` | v4 双写总开关；失败仅 warning 不阻断 |
| `NOTION_READ_FROM_SQLITE` | `true` | v4 Phase 4；2026-07-11 生产观察窗口拍板（07-05~07-11 263 hit / 0 fallback_miss / 0 fallback_error）默认翻 true，sync/resync 走 SQLite SSoT，miss fallback 老路径；`.env` 显式 false 应急回退 |
| `SEARCH_TRIGRAM_ENABLED` | `true` | 中文子串全文检索（并行 trigram FTS5 表路由）；Phase A 起默认开，设 false 则 CJK 搜索退回 unicode61 + smart 字符级 fallback。批次1（2026-07-22）起 trigram 路由泛化：拉丁≥3 双 lane（配套子开关 `SEARCH_LATIN_TRIGRAM_ENABLED` 默认 true，显式 false 单撤英文子串 lane）+ 附件/文件名融合（`email_attachment_fts_trigram`，DB v39）+ OR 组/列级 CJK；master=false 仍整体回 unicode61 老路径。详见 search-query-syntax.md §9 |
| `MAILAGENT_ATTACHMENT_OCR_ENABLED` | `true` | 附件 OCR（批次4 PR-G）：图片附件（png/jpg/…）+ 无文本层扫描件 PDF 经 macOS Vision（`pyobjc-framework-Vision`，本地识别、无网络出口）识别中英文本 → 双 FTS 表可搜（extractor `vision_ocr` / `pdf_ocr`）。默认开；显式 false = 图片/扫描件行为与现状逐字节一致（维持 unsupported/failed 老文案）。懒 import 缺 pyobjc 软着陆不崩；护栏 20 页/15MB/4096px。详见 v4-ssot-ops.md「附件 FTS5 全文搜索」 |
| `LLM_AGENT_ENABLED` | `false` | 本地 LLM 分类总开关（启用前必看 llm-agent.md 防双跑）；模型/fallback/参考上下文源支持预处理 Agent 行级覆写（`report_agent.model` / `fallback_models_json` / `context_source`（v38），空/NULL=跟随全局 `LLM_MODEL` / `LLM_FALLBACK_MODELS` / 继承派生，**保存即生效不重启**）。`context_source`（task 07-22）= 分类 system prompt 参考背景二选一 `standing_docs`\|`notion_context`（互斥不叠加）；运行时**行权威**（processor 每封热读 preprocess 行），env `LLM_PREPROCESS_CONTEXT_SOURCE` 仅作 v38 migration 首次 seed 默认（显式合法值→写行，否则按 `LLM_CONTEXT_PAGE_ID` 有无派生），行落地后改 env 不再影响运行时；`LLM_CONTEXT_PAGE_ID` 仍 env+重启（低频） |
| `LLM_PREPROCESS_CONTEXT_SOURCE` | `""`（仅 seed） | 预处理分类 system prompt 的「参考上下文源」二选一（task 07-21 引入、07-22 迁行；`_resolve_context_source` 读行）：`standing_docs`=注入身份文档（沿用预处理行 `context_docs` 勾选）、不注入 notion；`notion_context`=注入 `LLM_CONTEXT_PAGE_ID` 页面、不注入身份文档；**二者互斥不叠加**（旧版是双注入，owner 反馈冗余）。空/非法=升级继承：配了 `LLM_CONTEXT_PAGE_ID`→`notion_context`，否则 `standing_docs`。**🔴 task 07-22 起运行时权威已迁 `report_agent.context_source` 行（v38，processor 每封热读，保存即生效），本 env 键仅作 v38 migration 首次 seed 默认——行落地后改此 env 不再影响运行时**（详见上方 `LLM_AGENT_ENABLED` 行）。编辑入口=前端「Custom AI → AI 邮件预处理」抽屉（设置-AI 的 Prompt/context page 区已移除）。配套：`LLM_CONTEXT_PAGE_ID` 语义收窄为「仅 `notion_context` 时生效，**不再注入 chat**」（chat 由 Standing Context 单源），仍 env+重启。 |
| `MAILAGENT_LLM_PROVIDER_REGISTRY` | `true` | LLM 上游多 Provider 体系（task 07-12；**默认开，cutover 2026-07-13**；env 显式 false 应急回退，删键 = on）：on = provider/model 配置权威切到 `agent_config.db` 双表（`llm_provider`/`llm_model`，key Fernet 加密），gateway 走 AI SDK `createProviderRegistry`、Python 分类/报告/mem0 走 protocol 路由、translate/⌘K NL 搜索同源、Settings 显示「模型服务」区；模型引用统一 `providerId:modelId`（无冒号 legacy id → `default`）。首次启动表空时 `LLM_API_BASE/KEY/MODEL/ENABLED_MODELS` seed 成 default provider 行，行落地后**行权威**（env 键降级为首次 seed 默认）。off（显式 false）= 老 `createAnthropic` 单例 + 前缀路由字节级现状。双端读：Python pydantic（重启生效）+ Node main env（重启 app）。详见 llm-provider-registry.md |
| `CALENDAR_CALDAV_SYNC_ENABLED` | `false` | ★ 生产 = `true`（日历 CalDAV → SQLite SSoT 同步已上线）；CalendarSyncWorker 总开关 |
| `SYNC_FOLDERS` | `[]`（空） | 多文件夹同步白名单（JSON 数组的 imap_name，davmail-only）；空=零激活=逐字节同现状；勾选的自定义 Exchange 文件夹走 `email_metadata` 主链路（AI/Notion/FTS/线程/写操作全等同收件箱）。配套 `FOLDER_NOTIFY_ENABLED`（自定义文件夹默认不推飞书，JSON 白名单 opt-in）/ `FOLDER_LLM_DISABLED`（默认全跑 LLM，JSON 黑名单可关）/ `FOLDER_SYNC_PAST_DAYS`(90) / `FOLDER_SYNC_MAX_MESSAGES`(2000)。详见 architecture-internals.md「多文件夹同步」 |
| `PROJECT_PROGRESS_SYNC_ENABLED` | `false` | 项目周报 CLI + 钩子**总闸**（env 权威，镜像 `LLM_AGENT_ENABLED`）。S5 W5a 起项目周报迁进 custom agent 框架的**专型行**（`type='project_progress'` 单例，**DB v31** seed）：trigger 配置（sender/subject）从 env 搬进**行内热读**（new_watcher hook 1 每封裸 sqlite3 读行→重建 `ProjectProgressDetector`，Settings 可编辑），env 值仅作首次 seed 默认、行落地后**行权威**；`_DATABASE_ID`/`_FILTER_BU` 仍 env 权威（v1.3.x dogfood 批起在 agent 抽屉可编辑，env PATCH+重启生效；Settings→集成 旧配置区已移除）。执行历史：`GET /api/project-progress/runs` + 抽屉「执行历史」section。runner/xlsx/detector 逐字不变、执行仍 Python 直调不进 gateway（确定性 ETL 不塞 LLM loop）。详见 project-progress-sync.md |
| `MAILAGENT_STANDING_CONTEXT_ENABLED` | `true` | Standing Context 分层 prompt（不可弱化 `PRODUCT_SAFETY_FLOOR` + `SOUL/AGENT/RULES/USER` 由 backend `agent_config.db` 组装）；**默认 ON**，OFF 回退旧 `SOUL_MARKDOWN`（字节一致，应急回切）。`standingContextActive` 在 /chat/config 可观测。详见 capability-context-foundation.md |
| `MAILAGENT_AGENT_CONFIG_DB_PATH` | —（空） | backend-owned `agent_config.db` 路径覆盖（默认 sync_store 同目录）；不进 `DB_VERSION`，镜像 api_keys 纪律 |
| `MAILAGENT_MEM0_CAPTURE` / `_RETRIEVAL` / `MAILAGENT_USER_MD_COMPILE` / `MEMORY_MD_BUDGET_CHARS`(5000) / `MEMORY_CAPTURE_MODEL`(haiku) | `true`（前 3，2026-07-02 cutover） | **memory.md 有界记忆**：CAPTURE=chat 完成后后台把持久偏好/事实合并进有界 memory.md（去重+超预算同模淘汰）；RETRIEVAL=恒注入 memory.md（MEMORY fence untrusted 背景）；USER_MD_COMPILE=Settings 手动编译偏好进 `user.md`（diff+rollback）。env 显式 false 应急回退；CAPTURE 走 Node gateway env、RETRIEVAL 热读 .env、其余 pydantic 需重启 serve-api。详见 [`memory-skill-core-refactor.md`](./docs/archive/2026-07/agent-experience-epic/memory-skill-core-refactor.md) |
| `MAILAGENT_SKILL_SELF_MOUNT` | `true` | mem0/skill epic **M4a**（gateway skill→tool 门控，纯后端 main env，**不加 vite define**；默认开 —— 2026-07-02 cutover，env 显式 false 应急回退）：on 时 embedded AI SDK Gateway 工具注册受 skill 启用态驱动——关掉某 skill 其读工具不再注册给模型（收 cutover 后「关了 skill 工具仍可调」真 bug）。Python `/chat/config.advertisedSkills`（业务态）+ Node `applySkillGating`（工具集结构）。范围 = email/search/report 读工具（含 `email_list_filter` —— 批次2 PR-D 起归 email skill 家族正常门控；其前身 `email_search` 改名后 collision-exempt 特例退役、集合置空但机制保留）；kos/memory/write/send(core) 永不门控。off → gateway ToolSet 字节级同 cutover（/chat/config 字段恒发、值可 null）。M4b(update_system_md 全文人审 edit-tier)/M4c(discover_skills+set_skill_enabled) 已落地，且 flag 还门控这 3 个自挂载工具（off 不注册）；含 codex HIGH-1 审批 guard 收紧（approval.verify 对所有 tier 拒 raw-changed input，identity 不可 retarget）。详见同 epic 计划档 |
| `MAILAGENT_STANDING_DOCS_EDITOR` | `true` | mem0/skill epic 收尾（2026-06-30，随 v1.0.1）：Settings「身份文档」编辑器（AI tab → Custom AI 区，`StandingDocsSection`）显隐开关。on（默认）→ `/chat/config.standingDocsEditorEnabled=true` → owner 可在设置里查看/编辑 **SOUL/AGENT/RULES/USER** 全文 + 保存（`setProfileDoc` updatedBy='user'）+ 每文档 history/rollback；RULES 经 `validate_rules_content` 拒越权、空内容后端拒（防搞坏恒注入身份文档）、SOUL/AGENT/RULES 高危红样式。off → section 字节级不渲染（编辑仍可经 agent `update_system_md` 工具 / `/api/agent/profile/docs` API）。Python pydantic（翻需重启 serve-api）。 |
| `MAILAGENT_KOS_INGEST_ENABLED` / `_CONSUMER_ENABLED` / `_L1_HOT_BLOCK_ENABLED` | `false` | KOS 集成三层，全默认 OFF |
| `KOS_REQUIRE_LABELED` | `false` | issue #49：priority 的「标注状态」是三态（`missing` 从未标注 / `unknown` 野值 / `known` 已知枚举），判定单源 `src/kos/producer.py` 的 `priority_label_state()`。默认 false = `_normalize_priority` 把 `missing` 和 `unknown` **都**降成 `normal`，于是 `KOS_INGEST_PRIORITY_FLOOR=normal` 会把从未跑过 LLM 的历史邮件**全部放行**（实测占候选量 ~89%）。true = 只放行 `known`；增量 producer（`push_email_to_kos`）与 `bulk_ingest` 两处过滤点共用 `passes_priority_gate()` 单源，且与 floor **独立**（`floor='low'` 也照挡）。bulk 侧 `--require-labeled` / `--no-require-labeled` 双向覆盖 env，三个跳过原因分开计数（`skipped_unlabeled` / `skipped_invalid_priority` / `skipped_low_priority` —— 合成一个数字正是本 issue 的病根）。默认 off 时**过滤语义**等价于改动前（不是字节等价：stats 多了两个计数键、日志多了 `require_labeled` 字段）。`priority_at_or_above` 的 None→normal 老语义**有意不动**（改返回值波及所有调用点且不向后兼容） |
| `MAILAGENT_KOS_RETRY_ENABLED` | `true` | KOS 推送失败重试（issue #59）：producer 失败即丢（2026-07-24 KOS 卡死 35min → 101 封页+69 条边永久空洞，且实测 producer 连成功都不记账）→ 修为成功/跳过/失败全落台账 `kos_ingest_log`（**DB v41** 升格正式表镜像 v37 llm_processing 三件套，+`retry_count`/`next_retry_at`/`error_code`/`source('producer'\|'bulk')`，status 值域 `pushed/failed/dead/skipped`，存量行回填 source='bulk'），瞬时错误码（网络/限流/5xx）指数退避 1min/5min/15min/1h/6h 重试至 `MAILAGENT_KOS_RETRY_MAX_ATTEMPTS`(5) 转 `dead`，永久错误码（鉴权/4xx/协议）直接 `dead` 不烧预算；`E_KOS_HTTP` 按 status 二分、拿不到 status 保守按永久。🔴 `dead` 是台账**自己的**终态（镜像 llm `gave_up` 纪律），与 `email_outbox` 死信告警体系完全解耦。重试挂 new_watcher **主 tick 第 6c 步**（镜像 6b LLM 重试队列：3 封/tick、`next_retry_at` 排程、不新起 worker 不进 supervise），处理前先 `KOSClient.health()` 探活——不可用**整段跳过不逐封试**（retry_count 不增长，防故障期把积压全烧成 dead）并冷却 `MAILAGENT_KOS_RETRY_INTERVAL_SEC`(300s)。探活/最近成功落 `sync_state` `kos.*` 键（**canonical=`src/kos/ingest_log.py` STATE_\* 常量，读侧必须 import 勿手抄**；时间戳 ISO 镜像 `davmail.*` 先例，不 bump DB_VERSION）。`push_email_to_kos` 返回值三态化（`KosPushOutcome`：pushed/skipped/failed，调用侧失败记 warning 不再伪装成 skipped debug）；bulk 与增量共用台账——bulk 候选判据收窄为「done=仅 pushed」（不重推增量已成功的；增量记的 failed/skipped 不挡 bulk 补漏/低 floor 回填）。默认**开**（D1 有意偏离默认关惯例：纯补偿+幂等，`MAILAGENT_KOS_INGEST_ENABLED=false` 时整段不激活）；显式 false 应急回退=只推不补 |
| `MAILAGENT_REPORT_AGENT_ENABLED` | `false` | 报告 Agent worker（日/周/月报，`src/reports`）；per-agent 还需 `report_agent.enabled`（种子 daily 默认关） |
| `MAILAGENT_ISLAND_AGENT_ENABLED` | `true` | harness 审批上灵动岛（**默认开，E3 cutover 2026-07-06**；无岛=announce 静默 fail-open 有单测；env 显式 false 应急回退=字节级 inert）：gateway 审批暂停 stash+岛卡 → 岛点批准 → serve-api `POST /api/island/ack` → gateway 服务端 resume（chat 面板关着也能批+真执行）。main-env-only 无 vite define。🔴 **2026-07-15 owner 拍板：审批以无岛方案优先**——stash/pending/decide/server-resume 链自 harness-chat 批起**恒接线**（`serverResumeEnabled` 恒真），本 flag 只管 announce 上岛面；chat 内 `PendingApprovalPanel` 可批卡是主路径，岛未来可能移除。详见 [决策表](docs/plans/architecture-review-2026-07/e3-defaults-decision-table.md) + `docs/plans/agent-experience-epic/harness-island-integration-design.md` |
| `MAILAGENT_CHAT_DETACHED_RUNS` | `true` | harness-chat 批（2026-07-15）：chat run 与客户端连接解耦（**默认开**；env 显式 false 应急回退=**回旧 close→abort drain 行为 + 保留审批互斥安全性**——codex r2 [A] 起 ActiveRunRegistry 恒建、与本 flag 解耦：off 时 chat 仍占 per-session 槽（响应 close/abort 即释放），/decide resume 与新 turn 的 409 互斥、`/run/active` 真值在回退位照常成立；flag 只管 drain 形态，不再是字节级回退）。on = 切会话/关面板不中断流，gateway 自持 drain 到 onFinish 正常落库（ai@7 pipe 在 res 销毁后停止消费，故手工 drain、wire 字节级 parity 有测试 pin）；配 ActiveRunRegistry：`GET /api/ai/run/active`（「AI 仍在后台输出」真值）+ `POST /api/ai/run/stop`（composer 停止钮显式停止）+ 同 session 并发第二 POST 409（审批 renderer-resume 二次 POST 不受影响，暂停即释放 registry，有 pin 测试）。落库后广播 `chat:turn-persisted`（'finished'\|'paused'，codex r2 [C] 起携带 runId 供 per-run settle 去重）驱动切回自动刷新与未读标记（CHAT_DB v20 `last_read_at`）。main-env-only 无 vite define。 |
| `MAILAGENT_OPENNESS_SESSION_TOOLS` / `_CONFIG_TOOLS` / `_WEB_TOOLS` | `true` | Agent 开放性 S1（**默认开，E3 cutover 2026-07-06**；env 显式 false 应急回退 → `buildGatewayTools` 字节级回退，有测试断言）：SESSION=会话检索 3 读工具（CHAT_DB 16→17，返回恒 `UNTRUSTED_CHAT_HISTORY` 围栏）· CONFIG=配置读补全 4 工具（restore/memory_update 默认弹卡，08-05 WP-11 起 per-tool 档可配 auto）· WEB=`web_fetch`/`web_search`（SSRF 防护；08-05 WP-11 起 manual 出厂档 = **auto 免卡**（活库两月 0 拒绝拍板），设置里可 per-tool 调回 ask；S6 起 custom agent 经 `grant_web` 三档赋权）。9 工具全 `CORE_UNGATED` + 入 `tool_catalog.json`。详见 `.trellis/tasks/07-02-s1-openness-wave1/` + [决策表](docs/plans/architecture-review-2026-07/e3-defaults-decision-table.md) |
| `MAILAGENT_OPENNESS_EXEC_TOOLS` / `_SKILL_INSTALL` | `true` | Agent 开放性 S2（**默认开，E3 cutover 2026-07-06**；env 显式 false 应急回退）。**安全地板不变：`run_command` 无白名单命中恒 HITL、skill 安装 capability_change 恒 HITL**。EXEC=`run_command`/`file_read`/`file_write`（manual_chat 专属；执行权威 Python `/api/exec/*`；`run_command` 免卡仅经 owner 结构化白名单 `policy_rules`（per-tool 档对它 configurable=false），file 两写 08-05 WP-11 起默认弹卡、owner 可 per-tool 设 auto，CHAT_DB 17→18）· SKILL_INSTALL=两段两卡安装 + 卸载/读（供应链 quarantine→逐文件 sha256→TOCTOU re-hash→atomic promote；per-skill secret Fernet+Keychain；首跑闸）。详见 `.trellis/tasks/07-02-s2-exec-skill-install/` + architecture §13.17 |
| `MAILAGENT_CUSTOM_AGENTS_ENABLED` | `true` | Custom AI Agents（**默认开，E3 cutover 2026-07-06**；env 显式 false 应急回退=hook/worker/端点/CRUD 工具全灭字节级回 S4 前；**on 不配 grant/规则=恒 HITL**，per-agent 仍需 `report_agent.enabled` + `type='custom'`）。S4-S6 基线=trigger→async_jobs→AgentRunWorker→gateway headless 路径 C + Settings/对话式 CRUD 六工具 + per-agent grant/web/skill 与审批反查。**07-31 W1-W6 当前真相**：用户预算只剩 runs/day + runtime（默认/上限 1800s，存量 `max_steps` 忽略；内部 `stepCountIs(10000)` 仅防无终止 API）；run 读态 9 值含 `skipped` 并投影 steps/tokens/duration；六张能力卡投影既有工具/grant；`report_write` 以 artifact class 写 custom 报告；CHAT_DB v21 分离 interactive/agent 会话并支持置顶/星标；SyncStore v42 `avatar_json`；code-owned `custom_agent` builtin skill 复用六 CRUD 工具。exec 免卡仍叠加依赖 `MAILAGENT_OPENNESS_EXEC_TOOLS`，安全地板不变。详见 ai-sdk-gateway-architecture.md §13.19-13.21 + [决策表](docs/plans/architecture-review-2026-07/e3-defaults-decision-table.md) |
| `MAILAGENT_ALERT_EPISODE` | `true` | 状态型告警（判据成立后不会自行消失：死信/不健康/雷达/outbox 积压/davmail token age）的 **episode 语义**（task 07-14；env 显式 false 应急回退）：on = 进入异常态告一次 → 中间静默 → **值翻倍**才再告 → 恢复时发 `alert_recovery` 并复位；状态落 `sync_state['alert.<key>.*']`（镜像 `davmail.*` 键先例，**不 bump `DB_VERSION`**）→ 跨进程 + 跨重启存活。🔴 **两阶段提交**：`evaluate` 只判定，**告警投递成功**（`alert_*` 返回 True）才 `commit` —— `send_alert` 会因 level 门/cooldown 门/网络失败静默返回 False，判定时就落盘 = 永久标「已告警」却从未送达 = **永久漏告警**。灵动岛 `dead_letter_accum` 卡与告警同源同生命周期，不独立刷屏。off = 判据成立就告，仅剩 `ALERT_COOLDOWN`(300s) **内存**冷却兜底（= 死信恒真判据每 5min 刷一条到人工清理，且重启即复发 —— 即修复前的病根行为）。判定单源 `src/notify/episode.py`；非状态型告警（`sync_error:{id}` / `notion_error:{op}` / `worker_crashed:{name}` / `consecutive_errors`）+ `restart_frequency`（已自带 24h 持久冷却 `f4612a47`，有意不接）行为字节级不变。🔴 配套改动：恢复通知（`alert_recovery` + davmail 两条 `*_recovered`）level 从 info 提到 **warning** —— `ALERT_LEVELS` 默认不含 info，info 级恢复通知会被 `send_alert` 的 level 门挡掉、一条都发不出（预存 bug，实测 davmail 恢复两次零通知）；新增恢复类通知一律 ≥ warning。详见 architecture-internals.md「状态型告警的 episode 语义」 |
| `MAILAGENT_CALENDAR_AGENT_TOOLS` | `true` | 日历专项 epic 4.1/4.2；默认开，显式 false 应急回退 → gateway 工具集字节级回 epic 前。开时 gateway 多 5 个日历工具：`calendar_events_list`/`calendar_event_get`（silent 读，日期参数按 IANA timezone 墙钟解释、默认本机时区非 UTC——P2-4「今天」时区化；事件正文恒套 `UNTRUSTED_CALENDAR_EVENT` 围栏）+ `calendar_event_reschedule`/`calendar_event_rsvp`(iTIP REPLY 不可撤回)/`calendar_event_delete`(CalDAV 硬删)。🔴 三个写工具全 edit-tier，**出厂默认弹卡**（08-05 WP-11 注记：D4「恒 HITL」降级为 per-tool 审批档的出厂默认 ask——owner 可在 设置→AI→工具审批档 per-tool 设 auto，其中 delete 设 auto 需红警告一次性确认；headless 侧仍无任何白名单通道不变）+ 各自富审批卡 `CalendarApprovalCard`（改期 before/after 时间对比、RSVP 不可撤回警告、删除不可恢复警告）。执行权威 Python serve-api `/api/calendar/*`；由 Electron main 进程 gateway 读（main-env-only，非 config.py pydantic，不加 vite define）。 |
| `MAILAGENT_NOTION_AGENT_TOOL` | `true` | task 07-21；默认开，显式 false 应急回退 → gateway 不注册 `notion_agent_chat`，字节级回退。开时通用 agent 多一个工具：把 Notion 工作区请求（问答 + 执行任务，如改日程/编辑 context page）委派给 notion-agent CLI，经 serve-api `/api/skills/invoke`（Python builtin `notion_agent` skill handler 持 subprocess 桥，serial gate + idle 看门狗）。🔴 edit-tier **出厂默认弹卡**（外呼 + 副作用落 Notion 侧、批准后不可撤销；identity-only `SimpleApprovalCard` 预览将发送的 prompt + 续轮 `thread_id`。08-05 WP-11 注记（D1=a）：原「连 bypass 也不免卡」的 `BYPASS_STILL_ASK` carve-out **已退役清空**——bypass 恢复字面「无例外」压过一切；本工具的保护降级为 per-tool 审批档的出厂默认 ask + owner 设 auto 时红警告一次性确认（dangerAuto），外呼不可撤回的理由不变、从代码地板变成默认值）+ tool_class=`outbound`（数据出机 + 无 grant key → headless custom-agent run 结构性拿不到）+ 返回文本套 `UNTRUSTED_NOTION_AGENT` 围栏。直调面（`/api/skills/invoke`）另有三道闸：本 flag kill-switch（显式 false → 409）+ ToolDef `confirmation_tier=edit`（无 `confirm:true` → 403）+ skill 未启用 → 409（gateway 审批通过后显式传 `confirm:true`）。🔴 **Python 直调链同样受此 flag 约束**（codex R2）——`src/skills/invoke.py` 的 notion_agent 门热读 .env（`get_env_file_path` + `dotenv_values`，抄 `_hot_bool`/Tavily 先例，**不**直读 os.environ），否则 flag=false 时持 `notion_agent:invoke` scope 的外部 key 仍能绕过 gateway 直调这个外呼工具。⚠️ 与 web/calendar 不同：本工具 **skill-gated**——真开关是 设置→Custom AI→Skills 的 notion_agent 条目（skill default off，advertisedSkills 链路），本 flag 只是应急 kill-switch。gateway 侧由 Electron main 进程读（`envBool`，main-env-only，非 config.py pydantic，不加 vite define），Python 直调侧热读 .env（同 flag 同默认，跨两端一致）。 |
| `MAILAGENT_MCP_CONNECTORS` | `false` | MCP connector 总闸（epic 08-01 阶段 1 PR1-PR5，**灰度中，dogfood 后另拍 cutover**；沿用 island 的 ship-off→dogfood→cutover）。on = `/api/connector/*` 端点激活 + **配置台 `/connectors` 的「外部连接」段渲染**（08-06 起，见下）+ gateway 动态注入 `mcp__<connector>__<slug>` 工具 + Custom Agent 第七「外部服务」能力卡可改档。**双载体**（镜像 island）：pydantic `mcp_connectors_enabled`（serve-api，翻需重启 serve-api）+ Node `envBool`（`ai_gateway_lifecycle.ts`，main-env-only **不加 vite define**，翻需重启 app）—— 🔴 两侧默认必须同为 false，cutover 时一起翻，否则会出现「gateway 注册了工具但每次调用都 409」或反过来（闸 = `tests/config/test_flag_cross_language.py` 已登记）。刻意用 pydantic 而非 .env 热读：热读会让 UI 与端点在同一次会话里劈叉，且 env-only 直读另有 ratchet 闸禁新增。🔴 **08-05 owner 拍板 per-tool 三档**（master-plan WP-10；`connector_tool.mode ∈ auto|ask|off`，NULL=跟随默认，**默认折算 auto——含 write/update/destructive**；旧 `enabled` 列开库迁移一次性折入 0→off/1→auto/NULL 保持后退役为死列；值域跨语言闸并入 `test_connector_contract_parity.py`）：owner-present 场地（manual + `im_chat`）写类审批**按档**——`ask` 弹卡（manual 桌面卡 / im 飞书按钮卡）、`auto` 走 policyEvaluate 缝免卡执行（审计 `auto_tool_mode`，needsApproval/guard 内核未动）、`off` 任何场地不注册；read 的 auto=旧 silent，read 设 ask 同样弹卡。授权四调用方：manual chat 无天花板 · headless custom agent 走 `tool_policy_json.grant_connectors`（值域 read＜write＜update，**值域外含遗留 `delete` 字面量一律入库即拒**；grant 内免卡执行、grant 外根本不注册；per-tool ask/auto 对 headless 无意义、off 仍全局生效）· 报告 Agent 同 grant · 邮件预处理分类走**独立** `connector.preprocess_enabled` 列（默认关不变）+ **08-05 场地放开：read 硬天花板拆除**——工具面 = per-tool `auto` 档（含写类），`ask` 在该无人值守场地 ≙ 不注册（工厂 `only_auto_tools` + service `deny_ask_mode` 双道，lethal trifecta 攻击面 owner 知情拍板、留痕 master-plan §5 风险 4）· 未来新场地恒拒。设置面 = 按 crud 分组计数 + 组级批量下拉（`bulk_mode` 端点）+ Reset permissions + destructive→auto 一次性红色确认 + 升级/首连「工具面变宽」一次性概览；per-tool 端点 `…/tools/{tool}/mode`（旧 `…/enabled` 已删）。🔴 **delete 作为 crud 分类已整体退役**（08-03 owner dogfood 改判「连接工具应该都全部可配置」+ 不可达性实证：MCP annotations 无 delete 语义位 ⇒ `derive_crud_type` 结构上不产出它；原七道保证与 `E_CONNECTOR_TOOL_FORBIDDEN` 一并退役）——**在册工具一律可配置**，危险性由 `connector_tool.destructive` 独立列承担（审批卡红警告 + 设 auto 前一次性确认）；存量 `crud_type='delete'` 行由 `agent_config` 开库迁移幂等重推导成 `write`+`destructive=1`。🔴 **08-06 双轨**（owner dogfood 拍板，task `08-05-08-06-connector-dual-track-and-console`）：Composio 的 OAuth app 落到公司租户要 **IT 管理员同意**（owner 拿不到）⇒ Notion/Atlassian 走**自建直连**（OAuth2.1+PKCE+**DCR** 打官方端点，结构上不需要任何第三方审批），其余 14 家留 Composio 托管。目录侧 `track`（出厂轨道，`src/connectors/catalog.py` 单源）与行侧 `source`（既成事实）**不是一个东西**——`superseded` 判据 = 两者不符（`row_is_off_track`，双向：老直连行遇 composio 轨 / composio 行遇 direct 轨），换轨唯一出口 = `disconnect(purge=true)` 后重连（Composio 服务端 session/账号会残留，需去其控制台清）；直连轨**不套** Composio curated 白名单（清单以 `tools/list` 为准，故 `tool_count=null` 非 0）；BYOK gate 收窄成 **per-entry**（只罩 composio 轨，直连家没 key 也能连）；🔴 `registry.get_connector_def` 的 `row_lookup_ok` 不变量——「读不出行」≠「没有行」，异常路径下直连条目**不交出官方端点**（保证失败在本地、零出网）；配套 `should_mark_needs_reauth`（`service.py` 单源）把「端点未知/未配置」与「token 被拒」分开，前者不再把健康连接谎报成需重新授权。🔴 **08-06 独立配置台**：内置工具审批档（28 项）+ 外部连接合并成 `/connectors` 一个页（左栏内置功能域/外部连接，右栏按类别分组、**每类默认折叠**），设置-AI 那两个区降级成**深链**（同一份数据只有一个可写面，`ToolApprovalSection.tsx` 已删）。off = 全消费点字节级 inert（端点除 `oauth/callback` 外全 409、配置台「外部连接」段不渲染且零请求[内置工具段照常]、`buildGatewayTools` 与 Python LLM 工厂字节级回退），存量行与 grant 惰性无害。详见 [`mcp-connectors.md`](./docs/reference/llm-agent/mcp-connectors.md) |
| `MAILAGENT_MEMORY_LAYERS` | `false` | 阶段 0.5-③ 记忆分层（harness 扩展 epic，PR-1/PR-2；pydantic `memory_layers_enabled`，**Python 单载体**，翻需重启 serve-api）。on = memory.md auto-capture 走**分层抽取**：固定 5 个 h2 层（identity/preference/context/activity/experience）+ unsorted 兜底节，仍每轮**单次** LLM 调用（tool schema 五字段承载分层，模型只产纯内容、标题由 Python 常量拼装/解析），**按层预算确定性截断**（单层超预算只淘本层 —— 修「activity/项目类稳定吃掉 identity/people 类」的生产实证事故：51 版 history 差分出 88 次硬淘汰、41% 落在最该持久的层）；flag-on 后首轮对未分节旧文档走一次性迁移（heuristic 预分桶 + LLM 重排，快照全在 history 可 rollback），产出缺字段/非字符串 → fail-closed 不落库。off（默认）= 原单预算全文重写路径字节级不变，回退**零数据迁移**（分节文档在旧路径仍是合法 markdown）。🔴 **只门控 capture 写侧** —— PR-2 的读侧三处（`/chat/config.memorySummaryMeta.layers` 诊断 · `user_md_compiler._compile_source` 只把 identity+preference 喂进 user.md 编译 · Settings 分层预算条）判据是**文档结构**（`has_layer_structure`）而非本 flag：事后关 flag 只停止**产生**分层，不该让编译突然又把易过时的 activity 灌回恒注入的 user.md。层预算是代码常量不 env 化 |
| `MAILAGENT_SKILL_CATALOG_PROMPT` | `false` | 阶段 0.5-② 技能可发现性（harness 扩展 epic；Node `envBool`，`ai_gateway_lifecycle.ts` **main-env-only、不加 vite define**，**Node 单载体**，翻需重启 app）。渐进披露第一级：on 时把**技能名单**（名 + 一句话 + on/off/unavailable，🔴 **关掉的技能也在名单里**，只标 `[off]`）恒注入 system prompt 的**可缓存 stable 前缀**；要用时再 `skill_read(name)` 拿那份 SKILL.md 全文（阶段 0.5 起 6 个 builtin 的说明书也可读，此前只有已安装 skill 能读）。收的是「模型不知道自己有什么能力」这个缺口，**不是省 token**（名单约 +300 token）。**只进 manual chat**（`systemPrompt.ts` 另判 `!headlessAgentRun` —— headless 的工具集是服务端钉死的，给它名单也改不了任何事）。🔴 名单是 `/chat/config` 的 15s TTL 快照且活在可缓存前缀里（本轮之内不跟随用户刚做的 toggle），所以它是**导航用的能力事实、不是权威开关态** —— 模型要断言某 skill 开着必须先 `discover_skills` 核实（回归网 `AGT-SKILL-012` 专盯这个失败模式，`AGT-SKILL-005` 因此不放宽 must_use）。Python `/chat/config.skillCatalog` **恒发数据、不读这个 flag**；off → system prompt 字节级回退（有 vitest 断言） |
| `MAILAGENT_IM_FEISHU` | `true` | 飞书对话总闸（epic 08-01 **阶段 2** PR1-PR4，**默认开，cutover 2026-08-04**，owner dogfood 通过；env 显式 false 应急回退）。🔴 与现有**通知** bot 完全隔离（grill Q21=B）：独立自建应用、独立凭证（`external_credential` namespace `im:feishu`，env `FEISHU_IM_APP_ID/SECRET` 仅首次 seed）、独立长连接；通知链（重要邮件卡片 + Openclaw 按钮）一个字节不动。on = ① serve 进程起 `im_feishu` worker 建 lark-oapi **长连接**（零公网；建连前复用 `guards.check_pm2_conflict` 检测多实例，命中不建连 + episode 告警 + 状态可见 —— 飞书**不广播**，多实例会互相抢消息）② gateway 注册 `POST /api/ai/im-chat`（**唯一**断言 `im_chat` 的入口）+ `createImSession`（CHAT_DB `origin='im'`，桌面会话列表标注「来自飞书」）③ 设置-AI「飞书对话」区可生成绑定码。**双载体**（镜像 MCP connector）：pydantic `im_feishu_enabled`（serve，翻需重启后端）+ Node `envBool`（`ai_gateway_lifecycle.ts`，main-env-only **不加 vite define**，翻需重启 app）—— 🔴 两侧默认必须同为 true、回退也一起翻（闸 = `tests/config/test_flag_cross_language.py`）。**默认 on 但没配 `FEISHU_IM_APP_ID/SECRET` = 零启动零连接**（`feishu_im_ready` 在 spawn 前拦）。工具面按 grill Q10=A 的第四态矩阵裁剪：读免批 · `domain_write` **恒 HITL**（经飞书按钮审批卡）· `connector_write` 08-05 WP-10 起按 per-tool 三档（`ask` 走飞书按钮卡 + destructive 红警告随卡 / `auto` 免卡审计 `auto_tool_mode` / `off` 不注册）· `web` 由下面的独立开关门控 · `exec`/`capability_change`/`outbound` **根本不注册**（模型看不见）。off = 不建立任何连接、gateway 工具集字节级回退；🔴 **`/api/im/status` 与 `/api/im/approvals` 有意不挂此门**（「未启用」是要如实呈现的状态，整区 409 只会让设置页显示「加载失败」），`/api/im/pair` 挂门 409。详见 [`im-feishu-chat.md`](./docs/reference/llm-agent/im-feishu-chat.md) |
| `MAILAGENT_IM_WEB_ENABLED` | `false` | 飞书会话里允不允许 AI 上网（`web` 类工具 `web_search`/`web_fetch`，grill **Q19=A**）。**Node 单载体**（`ai_gateway_lifecycle.ts` `envBool` → `isToolClassAllowedInMode` 的 `venue` 参数，main-env-only 不加 vite define，翻需**重启 app**）。🔴 **是 venue 开关，不是 grant** —— `policy.ts::ImVenueSwitches` 的注释与 `policy.test.ts` 的 4×8 全矩阵双重锁死该方向：grant 是 per-agent 授权，而这是「飞书这个场地整体允不允许」。默认关：网页内容不可信，且飞书账号被盗 ≠ 电脑被盗；开了也**恒 HITL**（每次调用仍要在卡片上批）。UI = 设置-AI「飞书对话」区唯一的真开关（其余五行是矩阵陈述不是配置），已入两侧 `MANAGED_ENV_KEYS`（漏加 = 开关渲染正常但一点就 `E_INVALID_KEY`）。总闸 off 时本开关无消费点 |
| `MAILAGENT_PLAN_TOOL` | `true` | harness 优化 epic（task 08-07）**P0**：恢复 `plan_update` 零副作用 silent 工具（class read / CORE_UNGATED / 所有 Context Mode 含 headless，headless 交集豁免——它与能力授权面正交）+ 只读 PlanCard（G8：用户无编辑路径）。AGENT_TEMPLATE 更新为真实用法（复杂任务用/单检索总结翻译禁），配幂等迁移只升级**未编辑过**的默认 AGENT 文档（`WHERE content=旧默认逐字`，用户改过不动）。**默认 ON**（有意偏离 ship-off 惯例：P0 修「prompt 要求调不存在工具」bug，off=bug 继续存在）；显式 false = 不注册、ToolSet 字节级回 P0 前（`GATEWAY_PLAN_TOOL_OFF_NAMES` 钉死）。main-env-only 不加 vite define |
| `MAILAGENT_SESSION_PROVENANCE` | `true` | 同 epic **P1**（CHAT_DB v23→**24**）：① `ai_chat_sessions` + trigger_id/kind/fired_at 三列两索引（migration 与落列**不受 flag 控制**；trigger_id 恒 NULL 至 P6 多 Trigger v2——v1 trigger 无稳定 ID 不许发明）② headless System Prompt 注入 `<current_custom_agent>` 可信身份（仅从服务端 spec + createAgentSession 结果构造，XML 转义，不来自 request body）③ `chat_session_list/search` 组合过滤（契约=优化文档附录 A.1；triggerKind 枚举含 P7 calendar 两值，未知值 fail-open 筛空）+ 🔴 headless self-history 服务端强制 `agent_id=current`（`X-MailAgent-Agent-Id` header 由 gateway 可信代码设置，工具入参不可解除）、all-history 需 knowledge/sessions grant ④ `agent_catalog_list/get` 只读目录工具，仅 headless×provenance×grant 三条件与时注册 ⑤ Agents 导航/行未读红点（renderer 经 `/chat/config.sessionProvenanceEnabled` 热读投影取开关态——**不直读 env**，抄 execPolicyEnabled 先例）。默认 ON；显式 false = 身份不注入/查询参数回旧形/catalog 不注册/未读 UI 不渲染。main-env-only 不加 vite define |
| `MAILAGENT_CUSTOM_AGENT_CALL` | `true` | 同 epic **P2**（CHAT_DB v24→**25** + SyncStore v42→**43**，两 migration 均不受 flag 控制）：① `custom_agent_call` **仅 manual_chat**（capability_change 矩阵剥除 + 装配场地门双保险 = Custom Agent **递归禁止**，catalog `manual_only:true`）；G1 固定 180s 等待内部常量（`CUSTOM_AGENT_CALL_WAIT_MS`），schema/UI 不暴露，超时返 running 后台继续不唤醒父模型 ② 父子 Session：`ai_chat_sessions` + parent_session_id/parent_tool_call_id/invoked_by 三列一索引（无 FK 父删子留审计）；工具急切建子会话（(parent,tool_call) 查重无副作用重放）+ `fire_key=agent-call:<parent>:<toolcall>` 走 async_jobs 幂等 key（B.9），spec.sessionId 复用不二次建行，instruction 作受控 envelope 进 user message 不覆盖固定 Prompt ③ G2 动态外层卡：tool_prefs `configurable=false` ask 地板 + policyEvaluate 放行——只读型 agent auto（审计 `auto_delegation_readonly`）/ risky 默认卡 / `user_requested=true` 免外层卡（审计 `auto_user_requested`，**不改**子 ToolSet/ceiling/子审批）④ G3 审批 TTL 差异化（前置核对：原 30min 统一不符）：guard/stash per-entry——outbound 2h / 其余 24h；decide/resume 命中过期落列 `approval_status='approval_expired'` + 410；Python 读侧认 paused 响应透传的 `approvalTtlSec`（run_state 优先用，旧行回退 30min，不造第二真源）⑤ 停止通道：headless run 注册 ActiveRunRegistry（子 sessionId）→ 既有 `/api/ai/run/stop` 可停（abort 分 `E_RUN_STOPPED`/`E_BUDGET_TIME`），queued 走 `POST /api/agent-runs/{job_id}/cancel` CAS；⚠️ 配套 stale 驱逐收紧为「仅已 abort 条目可替换」（保 30min 预算窗内可停；manual 卡死 drain 恢复=停止钮）⑥ `report_agent.description`（≤1000 字符）进 Drawer/对话式创建/agent_catalog（P1 硬编码 null 接真值）；导入导出 JSON 留 P9 ⑦ 轮询单源：新 `GET /api/agent-runs/{job_id}`（run_state 9 值 + finalAnswer ≤10K），9→6 态映射 = `shared/lib/agentCallState.ts` 表现层投影（paused_approved/rejected→completed 为 v1 已知缝）。默认 ON；显式 false = 工具不注册 + TTL 回 30min 单值 + approvalTtlSec 不发，字节级回退。main-env-only 不加 vite define。详见 ai-sdk-gateway-architecture.md §13.24.3 |
| `FEISHU_NOTIFY_ENABLED` / `REDIS_EVENTS_ENABLED` / `ALERT_ENABLED` | `false` | 通知 / 事件消费 / 告警 |

完整配置（必填 + 全部可调项）见 [`.env.example`](./.env.example)。硬必填仅 1 项：`USER_EMAIL`（pydantic `Field(...)`）；`MAIL_ACCOUNT_NAME` 有默认 `Exchange` 但需与实际账户名一致。Notion 三键（`NOTION_TOKEN` / `EMAIL_DATABASE_ID` / `CALENDAR_DATABASE_ID`）**可选**——空 = 本地-only 模式（Notion 镜像/周报/日历同步停用，判据单源 `src/config.py` 的 `notion_enabled()` / `calendar_notion_enabled()`，task 07-12 P3b）。

## 命令速查

```bash
# 环境
source venv/bin/activate
pip install -e ".[cli,dev]"             # 装 mailagent CLI

# 运行服务
python3 main.py                          # 前台
pm2 start main.py --name mail-sync --interpreter ./venv/bin/python3  # PM2（必须用 venv python）
pm2 restart mail-sync && sleep 3 && pm2 logs mail-sync --lines 20 --nostream  # 部署后验证（详见 /deploy）

# 初始化同步
mailagent init fetch-cache --inbox-count 3000 --sent-count 500
mailagent init all --yes

# 排查
mailagent debug mail-structure           # 查看邮箱名称
mailagent admin health -o json | jq .data.healthy
tail -f logs/sync.log

# 部署 webhook-server 到远程
./scripts/deploy-webhook.sh
```

**部署环境**：本地 macOS（3.11+，main.py 主服务）· 远程 VPS `170.106.181.89`（3.9+，webhook-server FastAPI，PM2 `mailagent-webhook`，路径 `/opt/MailAgent/webhook-server`，SSH 公钥 `~/.ssh/id_ed25519`）。

## 打包 / 发布（桌面 App）

一体化 Electron 前端 + 内嵌 CPython 后端 → 单个 macOS `.app`。**全部在 `main` 上做**（前端是 `frontend/` 子目录，非独立 repo/submodule；打包/onboarding/auto-update 已全合入 main，feature 分支已删）。完整 runbook → [`docs/reference/packaging/packaging-release.md`](./docs/reference/packaging/packaging-release.md)。

- **版本 SSoT** = `frontend/package.json` 的 `version`（electron-builder 据此写 `Info.plist` + 产物名 + auto-update feed `latest-mac.yml`）。semver：`0.1.0`=首个 beta，bug 修复走 patch；功能性内容走 minor。已发至 **v1.2.1**（GitHub Releases published）。**🔴 对外发布流程（CI 驱动，实测 v0.6.3→v0.14.0 每次都这样）= bump `version` → 提交 → `git push origin main` → `git tag -a vX.Y.Z -m "…"` → `git push origin vX.Y.Z`**：CI `.github/workflows/build-mac.yml` 监听 `v*` tag **先过测试闸**（E0 起：`ci-test.yml` pytest 全量 + agent_eval 四道闸 + vitest，经 workflow_call 复用；测试红 = 不产 draft）再自动 build(macos-14 arm64) + `electron-builder --publish always` 把 5 件 feed（latest-mac.yml + zip/dmg + 各自 blockmap）**上传到一个 draft release**，`gh run watch <id> --exit-status` 盯完成（build ~3-4min + 测试闸 ~5min）。**🔴 CI 完成后 release 仍是 draft、`releases/latest` 不会更新 —— 必须转正式：推荐 Actions → Promote release（`promote-release.yml`，输入 tag，自动 edit 转正 + 置 latest + 校验 `releases/latest`==tag，E0 WP4）**；手动 fallback = `gh release edit vX.Y.Z --draft=false --latest --title "MailAgent vX.Y.Z" --notes-file <notes>`，再验 `gh api repos/ChenyqThu/MailAgent/releases/latest --jq .tag_name` == vX.Y.Z。本地 `pnpm build:mac` **仅用于 tag 前 dogfood**，CI 会从 tagged commit 重新构建发布字节。**不要手动 `gh release create`/上传产物**——push tag 已触发 CI 传 draft，手动 create 会与 CI 撞车。**🔴 装机三步 `quit→ditto→open` 必串行单线**——多个 ditto 覆盖 `/Applications/MailAgent.app` 期间被 open 拉起 = torn bundle → dyld `libffmpeg.dylib` missing → SIGABRT（易误判成「启动崩溃/DB 版本 bug」；判据=崩溃 .ips `termination.namespace=DYLD`「Library missing」+ `codesign --verify --deep --strict /Applications/MailAgent.app` 报「sealed resource is missing or invalid」，但 dist 源 app codesign OK ⇒ Release 产物干净、只 /Applications 副本 torn ⇒ `rm -rf`+单次 ditto 重装即修）。**🔴 勿改 package.json `name`（`mailagent-frontend`）**—— 它决定 userData 目录 `~/Library/Application Support/mailagent-frontend/`，改了已装用户数据/`.env` 易主。
- **前置**（`frontend/` 下，均 gitignored 本地产物）：`node_modules`（`pnpm install`）+ `resources/python`（`bash scripts/build-python-venv.sh`，~425M 可重定位嵌入式 CPython——mem0 epic 引入 onnxruntime/faiss 等本地 embedding 栈后的基线；本机已 provision，换机/新 clone 必先跑）。
- **构建**：本地装用 `pnpm run build && npx electron-builder --dir --arm64`（只出 `.app`，避开 flaky 的 dmg）；完整 feed 产物（dmg+zip+blockmap+latest-mac.yml）用 `pnpm build:mac`。**🔴 要含远程 web（`mail.chenge.ink/app`）必先 `pnpm build:web`**（出 `out/web` → electron-builder `from: out/web` 打进 `.app/Resources/web` → serve-api 经 `MAILAGENT_SPA_DIR` mount `/app`）；`pnpm run build` **不含** web SPA，漏跑则远程根 `/` 返 `{"detail":"Not Found"}`（`build:mac` 已含 `build:web`，仅 `--dir` 装机路径需手动补 `pnpm build:web &&`）。
- **🔴 头号坑①（python）**：`resources/python` 缺失 → afterPack（`scripts/afterPack.cjs`）**跳过整个签名** → `.app` 无后端 + `codesign` FAIL。build 前必确认它在。
- **🔴 头号坑②（ABI，0.2.3 踩过）**：build 前**绝不跑 `pnpm rebuild:node`**（把 better-sqlite3 编成 Node ABI）；electron-builder `npmRebuild:false` **不自动切回 Electron ABI** → 装进 app 的 `better_sqlite3.node` ABI 不匹配 → 所有 SQLite IPC（`email:listEnriched`）崩（renderer 报 `NODE_MODULE_VERSION`、界面全空）+ `probeDbReady` 失败致启动卡 120s。**跑过单测（`pnpm test` 含 rebuild:node）后 build 前必 `pnpm rebuild:electron`** —— 🔴 注意 `pnpm test` 本身就是 `pnpm rebuild:node && vitest run`，所以**任何一次跑完整前端测试都会把 ABI 翻回 Node**；「build 前重跑 rebuild:electron」不是一次性动作，是**每次 build 前都要确认**（2026-07-24 v1.19.0 dogfood 再次踩中：第一次 build 前做了，中途为验证另一条 lane 又跑了 `pnpm test`，第二次 build 前漏做）。

🔴 **验证方法必须真的触发原生加载** —— 旧记的 `electron -e "require('better-sqlite3')"` **是无效探针**：better-sqlite3 的 `.node` 是**懒加载**的，`require()` 成功时 `require.cache` 里根本没有 `.node`（实测），故该命令**无论 ABI 对错都通过**，给的是虚假安全感（这正是本坑反复出现的原因）。有效探针二选一：

```bash
# ① 源树：真的实例化一个 Database（会触发 dlopen）
ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron -e "new (require('better-sqlite3'))(':memory:')"

# ② 🔴 包内 / 装机副本（真正该验的那份 —— 源树对不代表打包带对）
N="/Applications/MailAgent.app/Contents/Resources/app.asar.unpacked/node_modules/better-sqlite3/build/Release/better_sqlite3.node"
ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron -e "process.dlopen({exports:{}}, '$N')"   # 成功 = Electron ABI
node -e "process.dlopen({exports:{}}, '$N')"                                                   # 必须失败 = 不是 Node ABI
```

双向都验（electron 成功 **且** node 失败）才算数：只验一边分不清「ABI 对」与「探针没生效」。
- **验证**（每次 build 后）：`codesign --verify --deep --strict <app>` 必 OK + `Info.plist` 版本号对 + `Resources/python/bin/python3.11` 在。
- **装机/升级**：退出旧 app → `ditto dist/mac-arm64/MailAgent.app /Applications/` → open。userData 跨重装保留 → 升级**跳过 onboarding**（detect `'configured'`）+ 后端启动自动 DB 迁移。用 `.app` 时 pm2 `mail-sync` 必须停（防双写）；davmail 用户 `davmail-poc` 留 pm2（EWS 桥，不打进 app）。
- **改 Python 后端**后：必先 `bash frontend/scripts/build-python-venv.sh` 重 provision 才进包；只改前端 TS/CSS 不用。**改 Python 依赖（requirements.txt / pyproject extras）必须重新生成 `requirements.lock.txt`**——E0 WP5 起 provision 只认 lock（108 包全 `==` pin，保打包再现性），漏生成 = 依赖改动不进包；生成方法见 lock 文件头注释。
- **自动更新**自 v1.0.0（P6）上线：Developer ID 签名 + 公证 + `AUTO_UPDATE_ENABLED` **packaged 默认开**（`readMasterFlag()` = `!is.dev`；`=0` 应急回退，仍保留检测提醒）。CI 全量 build 产 `latest-mac.yml` feed，正式 release 装机后自动检测/下载/安装。**🔴 例外：本地 `electron-builder --dir` dogfood 包不含 `app-update.yml`（走 ENOENT → `markUpdaterUnavailable`）+ 通常未公证 → 无法自更新、需手动替换，且装了 --dir 包 = 暂时脱离自更新轨道，需再装一次正式 CI/`build:mac` 包才恢复**。P6 见 [`docs/reference/packaging/05-auto-update-handoff.md`](./docs/reference/packaging/05-auto-update-handoff.md)。

## 官网（公开 Landing + 101）

仓库根 `site/` 是独立的公开官网（Astro 6 + Starlight，独立 pnpm 项目，Cloudflare Pages `mailagent-site`，线上 **https://mailagent.chenge.ink**；与 CF Access 后的 `mail.chenge.ink/app` 是两套独立部署）。内容结构 / 设计 token / 命令 / 部署与 🔴 wrangler token 坑 → 动 `site/` 时自动加载 [`site/CLAUDE.md`](./site/CLAUDE.md)。

## 调试 & 部署

调试服务按固定顺序排查（详见 `/debug` skill）：① `pm2 status` 进程存活 → ② `.env` token/secret → ③ Redis/webhook/代理 → ④ `pm2 logs <name> --lines 30 --nostream` → ⑤ `sqlite3 data/sync_store.db` 状态分布。**不要**：sudo / 交互式命令 / 没查基础项就改代码 / 错误 SSH 凭证重试。

部署后**必须**验证（详见 `/deploy` skill）：重启 → `pm2 status` online → 启动日志无 error → Redis consumer 连接 / SQLite 雷达 / webhook handler 已注册。不要假设部署成功 —— Pydantic schema 变更、handler 未注册、依赖缺失都可能静默失败。

```bash
# 死信 / 重试队列监控
sqlite3 data/sync_store.db "SELECT sync_status, COUNT(*) FROM email_metadata GROUP BY sync_status"
sqlite3 data/sync_store.db "SELECT COUNT(*) FROM email_metadata WHERE sync_status='dead_letter'"
```

## 模块地图

#### 邮件模块 (`src/mail/`)

| 模块 | 职责 |
|------|------|
| `new_watcher.py` | 主监听器，v3 主循环（SQLite 优先）+ LLM/项目周报/KOS hook 派发点 |
| `sqlite_radar.py` | SQLite 雷达：检测变化 + `get_new_emails()` |
| `applescript_arm.py` / `applescript.py` | AppleScript 机械臂（`fetch_email_content_by_id()`）+ 底层执行封装（fallback 路径）|
| `backend/` | Sprint 16 双 backend 抽象（`IMailBackend` / davmail / applescript / imap_client）|
| `sync_store.py` | SQLite 同步状态存储（internal_id 主键，DB schema 演进点）|
| `reader.py` | MIME 解析（HTML、附件、thread_id） |
| `meeting_sync.py` / `icalendar_parser.py` | 会议邀请检测 + iCalendar 解析 |
| `reverse_sync.py` | 反向同步（Notion → SQLite intent + outbox，Sprint 15 后不直调 AppleScript） |

#### 其他模块

| 目录/模块 | 职责 |
|------|------|
| `src/notify/feishu.py` / `alert.py` | 飞书应用机器人通知（Card 2.0 form 交互）/ 飞书告警机器人 |
| `src/events/redis_consumer.py` / `handlers.py` | Redis BLPOP 消费者 / Webhook 事件处理器（flag_changed/ai_reviewed/completed/create_draft/query_mail/fetch_mail_content/search_email_bodies/page_updated）|
| `src/notion/` | I-07 后 facade 拆分：`sync.py`(facade) + `client.py` + `pages.py` + `threads.py` + `queries.py` + `_common.py`。外部统一 `from src.notion.sync import NotionSync, CreateEmailFromSqliteResult, BEIJING_TZ`，勿直接 import 子组件 |
| `src/calendar_notion/` | `sync.py` 日历→Notion · `caldav_reader.py` CalDAV 读 · `meeting_sync.py` 邮件 .ics → calendar_event |
| `src/calendar_sync/` | Sprint 后新模块：repository / expander / reconciler / worker（CalDAV → SQLite SSoT） |
| `src/converter/` | `html_converter.py`(HTML→Notion blocks+内联图) · `eml_generator.py` · `office_converter.py` · `attachment_text.py`(附件文本化) · `html_to_markdown.py` |
| `src/repository/` | v4 `EmailRepository` / `AttachmentStore` / FTS5 搜索 |
| `src/llm_agent/` / `src/project_progress/` / `src/kos/` | 见对应下沉文档 |
| `src/agent_config/` | Capability & Context 配置面（Phase -1/0A，backend-owned `agent_config.db`）：`store`(统一 skill registry + Standing Context 文档 SOUL/AGENT/RULES/USER + history/rollback) · `templates`(seed) · `projections`(MEMORY/SKILLS 投影 + 配置快照 hash) · `validator`(RULES deny-list, negation-aware)。配 `src/skills/installed.py`(安装行→BoundSkill 投影，owner-only) + `src/api/routers/agent.py`(`/api/agent/*`)。见 [`capability-context-foundation.md`](./docs/reference/llm-agent/capability-context-foundation.md) |
| `src/reports/` | 报告 Agent 系统（日/周/月 + custom）：`models` 定义 18 种 ReportDoc block 的 Python canonical vocabulary，TS 镜像有跨语言抽取闸；`data`/`summarizer`/`assembler` 负责取数、LLM tool_use 与防幻觉回填；`worker` tick 走共享 `src/agents/schedule_rule`；`store` 管 report_agent+report，custom cadence 支持同日多份与 replace 稳定归宿。gateway `report_write` 是 artifact class，本地产物免审批；Python 边界再次验证块类型/数量、image 内部资源限制与 src 长度上限；`/reports/custom` 校验归属（真实 agent 或 manual chat 哨兵 `custom_ai`，两侧常量有闸）。见 [`docs/reference/remote-chat-report/report-agent-prd.md`](./docs/reference/remote-chat-report/report-agent-prd.md) |
| `src/agents/` | Custom agent 内核 + 产品化（flag `MAILAGENT_CUSTOM_AGENTS_ENABLED` 默认开）：`trigger` 解析 cron\|schedule\|email_filter、两项预算（runs/day + runtime；legacy `max_steps` 忽略）与 tool policy；`schedule_rule` 与 report worker 共用；`email_dispatch`+`run_queue` 负责幂等 enqueue、runs/day 门及可见 `skipped` 行；`run_worker` claim→gateway→终态（预算跳过写终态带 `expect_status='queued'` CAS —— 抢跑的 run 不会被记成 skipped）；`run_state` 是 paused_handoff/expired/skipped 的 9 值读态唯一入口（steps/tokens/duration 的投影在 `api/routers/agent_runs.py`，非 run_state）。产品面包括六能力卡→canonical 工具/grant 映射（🔴 档位向上取整：显示的档恒 ⊇ 实际启用工具，闸见 `frontend/tests/shared/customAgentCapabilities.test.ts`；工具词表与 `HEADLESS_TOOL_OPTIONS` 的相等闸见 `tests/config/test_agent_capability_parity.py`）、CHAT_DB v21 interactive/agent 会话隔离与 pin/star、Oreo `avatar_json` 身份、审批反查、per-agent web/skill grant，以及只注入 advertised code-owned `custom_agent` builtin skill 的对话式创建流程（**manual chat only** —— 其 CRUD 工具是 capability_change，headless 结构性拿不到）；🔴 **重复失败纪律恒注入 manual + headless**（08-02 F4 拍板：`MAILAGENT_CHAT_DETACHED_RUNS` 默认开后「manual 有人在环」不再成立，且 manual 与 headless 共用 `stepCountIs(10000)` 而无墙钟预算），仅「无人值守/不能反问」那一句 headless 专属；exec/outbound/capability_change 安全地板不变。见 ai-sdk-gateway-architecture.md §13.19-13.21 |
| `src/stats_reporter.py` | 定期上报运行统计到远程看板 |
| `webhook-server/` | FastAPI（接收 Notion Automation webhook → Redis 路由 + 看板 API，端口 8100）|

## CLI

`mailagent` CLI = agent-friendly 接口，14 个 group：`email` / `admin` / `attachment` / `llm` / `kos` / `notion` / `calendar` / `debug` / `backfill` / `project-progress` / `init` / `folder` / `report` / `api-key`。读命令无 auth，写命令需 token（`MAILAGENT_CLI_API_KEY` + `--api-key`，`--dry-run` 跳过）。Batch 写命令有长任务契约（SIGINT 二次 / 熔断 / checkpoint resume / PM2 检测）+ 退出码体系（0/1/2/4/5/6/7/8/9/130）。

**完整命令表 / 退出码 / schema 契约 / 调用样例** → [`docs/reference/cli/cli-reference.md`](./docs/reference/cli/cli-reference.md)。

```bash
mailagent -o json email get 53675 | jq .data.subject
mailagent -o json email search "redis timeout" --mailbox 收件箱 --limit 20
mailagent email resync 53675 --dry-run -o json
```

## Notion 数据库结构

**邮件数据库**必需字段：`Subject`(Title) · `Message ID`(Text,去重) · `Thread ID`(Text,线程) · `From`(Email) / `From Name`(Text) · `To` / `CC`(Text) · `Date`(Date) · `Parent Item`(Relation self,线程头) · `Mailbox`(Select) · `Is Read` / `Is Flagged` / `Has Attachments`(Checkbox) · `AI Action`(Select) · `AI Priority`(Select: Critical/Urgent/Important/Normal/Low) · `AI Review Status`(Select: Pending/Reviewed)。

**日历数据库**必需字段：`Title`(Title) · `Event ID`(Text,去重) · `Time`(Date,起止) · `URL`(URL,Teams) · `Location`(Text) · `Organizer`(Text) · `Status`(Select)。

> 改 email DB schema（加/改 select option）→ 同步改 `src/llm_agent/schema.py` 并跑 `pytest tests/llm_agent/test_schema.py`（有 `schema-consistency-reviewer` subagent 校验四处一致性）。

## 常见问题

- **邮箱名称错误**：`mailagent debug mail-structure`
- **SQLite 无法访问**：需 Full Disk Access（系统设置 → 隐私与安全 → 完全磁盘访问权限）
- **AppleScript 超时**：增大 `APPLESCRIPT_TIMEOUT`（默认 200 秒）

## 开发指南

- 改邮件解析：编辑 `src/mail/reader.py`，测 `python3 scripts/dev/test_mail_reader.py`
- **mailbox 判定/查询单源**：一律走 `src/mail/mailbox_semantics.py`（前端镜像 `frontend/src/shared/lib/mailboxSemantics.ts`）的常量/判定集/SQL 辅助，**禁止**新增 mailbox 字面量枚举比较（`== "发件箱"` 类，漏一种写法即静默 bug —— issue #42 C 案收敛，2026-07-17）。**列表/计数面**（含内建视图过滤、徽标聚合、view↔mailbox 映射）用 `filter_labels_for_mailbox()` / 前端 `mailboxFilterLabels()` 展开成变体集，**禁止**对内建 canonical 用 `= ?` 精确匹配（否则变体行在专属视图不可见、或徽标与列表口径分裂 —— issue #42 提交者后续反馈收敛，2026-07-20）；自定义文件夹维持精确匹配。两侧变体集成员由 `frontend/tests/shared/lib/mailboxSemantics.test.ts` 跨语言锁死，改集合两边同步
- 改会议检测：`src/mail/icalendar_parser.py` 或 `src/calendar_notion/description_parser.py`
- 加新配置：① `src/config.py` 加 Field → ② `.env.example` 加示例 → ③ 必要时更新本文件「关键开关现状」表
- **加新文档**（防再次乱套，完整规范见 [`docs/DOC-GUIDE.md`](./docs/DOC-GUIDE.md)）：先判类型 —— **常青参考**（描述系统"现在如何"、会反复读）放 `docs/reference/<子系统>/`，**且必须在上方「文档地图」加一行**（否则无人发现）；**过程产物**（handoff / complete / phaseN / prN / sprint / 验收 matrix / 交接 / dogfood）放 `.trellis/tasks/<task>/`，已成历史的归 `docs/archive/{年-月}/` —— **禁止堆回 `docs/` 顶层或 `docs/reference/`**。判据：*半年后还有人为"现在怎么回事"来读吗？* 是→reference，否→archive。
- SQLite schema 升级：用 `/db-migration` skill（bump DB_VERSION + idempotent migration + 一致性更新）。**bump `DB_VERSION` 必同步前端 `frontend/src/electron/main/backend_lifecycle.ts` 的 `EXPECTED_DB_VERSION`**（TS 手抄 Python 常量，漏改 → 打包 app 启动门控 `waitReady` 卡 120s 降级；判据已 `>=` 容错 + `frontend/tests/main/db_version_consistency.test.ts` 兜底）
- **跨边界手抄常量必建一致性闸**：一个常量 / 枚举 / 派生表 / 类型形状要在**第二处**手抄，先问能不能消灭镜像（单源导出 / **零依赖叶子模块** —— issue #68 一半工作量是这个，"不能 import 因为对方顶层拉了 electron/keytar/SyncStore" 的正解是下沉常量，不是照抄一份加句"同源"注释）；消灭不了才建闸。**现存二十闸（数量与清单以 internals 台账为准）**，边界不止语言（跨部署 `webhook-server` / 跨构件种类 Python↔JSON Schema / SQL `CHECK` 字符串 / 跨进程 main↔renderer 都算）。清单、canonical 源选法、**「抽取失败必须红」**、以及写抽取器的两个实战坑（部分抽取比抽不到更毒 / 同名结构不止一个）见 [`architecture/architecture-internals.md`](./docs/reference/architecture/architecture-internals.md)「跨语言手抄常量的一致性闸」。漏建 = 改一处漏一处、测试全绿运行时静默错。
- **改 agent 排程语义**（custom agent 定时触发 / 报告 Agent 排程 / 前端运行预览）：语义单源 = [`architecture/schedule-rule-contract.md`](./docs/reference/architecture/schedule-rule-contract.md)，**先改契约再两侧同步**；改完必跑黄金 fixture 闸（`pytest tests/agents/test_schedule_fixture.py tests/agents/test_schedule_rule.py` + `frontend` 的 `scheduleParity`），改实现忘了重新生成 fixture 会红。
- **跑 agent 行为回归网**：改 chat agent 的 prompt / 工具 / 编排引擎后**必跑** `venv/bin/python -m pytest tests/agent_eval -q`（零-LLM hard rules R1-R8 + 37 curated tasks[v0.13.0 baseline 27 + phase04b/selfmount/skillsupply/s4agents lanes 10] + **gateway↔catalog 完整性闸**[新 gateway 工具漏 `tool_catalog.json` 必红]，~0.1s）。这是「换 agent 引擎不回退」的金标准——回归闸 `python -m runner.run_baseline --compare`（在 `tests/agent_eval/` 下跑），总分不得低于 baseline。规格见 `tests/agent_eval/schema.md` + `recorder-contract.md`；**judge / live recorder 是 manual lane，不进 CI**（fixtures 全 `.test` 合成域，零真实 PII）。整合大版本规划见 `.trellis/tasks/06-23-agent-eval-memory-skill-assistant-ui-ai-sdk/`。

## 文件位置

- 日志：`logs/sync.log` · 数据库：`data/sync_store.db` · 附件：`data/attachments/{internal_id}/` · 临时附件：`/tmp/email-notion-sync/{md5}/` · 配置：`.env`（示例 `.env.example`）
- 优化文档（已归档）：`docs/archive/2026-01/applescript_id_optimization.md` · Webhook Server：`webhook-server/`（一键部署 `./scripts/deploy-webhook.sh`）
- 常青参考：`docs/reference/<子系统>/`（索引见各 `index.md` 与上方文档地图）· 过程归档：`docs/archive/{年-月}/` · 文档规范：`docs/DOC-GUIDE.md`

## 迁移与运维

```bash
# v3 架构迁移（v2 → internal_id 主键）
python3 scripts/migrate_sync_store_v3.py

# 监控重点
sqlite3 data/sync_store.db "SELECT sync_status, COUNT(*) FROM email_metadata GROUP BY sync_status"
sqlite3 data/sync_store.db "SELECT internal_id, sync_status, retry_count FROM email_metadata WHERE sync_status IN ('fetch_failed','failed')"
```

各子系统的运维 SQL / 验收命令 / 回滚开关 见对应下沉文档（见「文档地图」）。
