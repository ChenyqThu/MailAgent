# V2.1 远程 chat + report/agent — 能力矩阵 & 验收看板

> 配套设计：`~/.claude/plans/twinkling-floating-puppy.md`（方案 B-pure-unified）。
> 本文件是**活的执行/验收看板**，每阶段末更新。把「覆盖完整性」变成可机械核对的表，
> 专治系统改造里「哪个端点漏改 / 漏对接 / 漏测」。分支：`feat/v2.1-remote-chat-report`。
>
> 工程纪律（仿 backend service layer）：每阶段 **实现 → codex GPT-5.5 high review → 修 → 验收（pytest/build:web/curl）→ 矩阵标绿 + 进度日志**。

## 为什么需要这张表

V2.1 的风险 = 「某个能力只补了一半的层（serve-api 端点有、客户端没接 / 形状漂移 / 没测）」。靠人脑记必漏。
五招防漏：① 能力矩阵看板（本表）② schema 契约对齐 IPC 形状 ③ 删除驱动（删 main chat 直跑，编译器枚举调用点）④ 残留检测 ⑤ 每阶段 codex review + 验收 gate。

图例：✅ 完成 · 🟡 doing · ⬜ 待办 · ➖ 不适用

## 架构（B-pure-unified，零 parity）

harness 逻辑 `shared/chat/`（一份，renderer + browser 都跑）→ `httpPlatform`（一份）→ serve-api（一份后端，无 harness 逻辑）。本地 token 由 main `webRequest` 透明注入，对 harness 透明。详见 [plan](file://~/.claude/plans/twinkling-floating-puppy.md) + 最终架构文档 `docs/claude/remote-chat-report-architecture.md`（阶段 3 末产出）。

---

## 阶段 1 — Part A report/agent + 修 agents 慢 ✅ 完成（codex gpt-5.5 APPROVE）

| 能力 | serve-api 端点 | HttpApi 客户端 | 本地接线 | 单测/契约 | parity(==IPC 形状) |
|---|---|---|---|---|---|
| report list | ✅ `GET /api/reports` | ✅ | ➖ 本地直读保留 | ✅ | ✅ wire 单一真源 |
| report get | ✅ `GET /api/reports/{id}` | ✅ | ➖ 本地直读保留 | ✅ | ✅ |
| report getConfig（**修慢**） | ✅ `GET /api/report-agents` | ✅ | ✅ 本地走 serve-api（兜底直读） | ✅ | ✅ |
| report setConfig | ✅ `PUT /api/report-agents/{id}` | ✅ | ➖ 本地 fork CLI（经 wire 单一真源） | ✅ | ✅ |
| report runNow | ✅ `POST /api/report-agents/{id}/run` | ✅ | ➖ 本地 fork CLI（经 wire） | ✅ | ✅ |
| report delete | ✅ `DELETE /api/reports/{id}` | ✅ | ➖ 本地 fork CLI | ✅ | ✅ |

**横切（阶段 1）**：
- `src/reports/wire.py` 下沉（resolve_agent/parse_counts/report_to_list_item/report_to_detail/config_patch_to_db，CLI+serve-api 单一真源，仿 D2a wire.py）— ✅
- CLI `report.py` 重构调 wire（消手抄，行为零回归 557 passed）— ✅
- `deps.py` 加 `get_report_store()`（lru_cache 单例，对齐 get_job_repo）— ✅
- agents 慢根治（本地 getConfig 走 serve-api in-process resolve，~ms 取代 fork ~759ms；不可达兜底直读不 fork）— ✅

## 阶段 2 — Part B chat 只读历史 ✅ 完成（code-reviewer opus APPROVE WITH NITS）

| 能力 | serve-api 端点 | HttpApi 客户端 | 单测/契约 | parity |
|---|---|---|---|---|
| chat listSessions | ✅ `GET /api/chat/sessions?emailId=` | ✅ | ✅ | ✅ SQL verbatim |
| chat listAllSessions | ✅ `GET /api/chat/sessions/all`（join email_metadata）| ✅ | ✅ | ✅ |
| chat listMessages | ✅ `GET /api/chat/sessions/{id}/messages` | ✅ | ✅ | ✅ |
| chat listToolCalls | ✅ `GET /api/chat/messages/{id}/tool-calls` | ✅ | ✅ | ✅ |
| chat kosAvailable | ✅ `GET /api/chat/kos-available` | ✅ | ✅ | ✅ |
| ai_chat.db 连接（`src/chat/db.py`，`DATA_ROOT/frontend/ai_chat.db`，graceful 库不存在→[]）| ✅ | ➖ | ✅ | ➖ |

**本地 chat 读**：阶段 2 保留直读 ai_chat.db（better-sqlite3 快，对齐 report 读 replica 不变式 4）；阶段 3 整体 B-pure-unified 时本地统一走 serve-api。验收：`pytest tests/api` **237 passed**（含 chat 11 测试）+ ruff + 前端 typecheck/eslint 全绿。

## 阶段 3 — Part B chat 对话（B-pure-unified 核心）✅ 3a（313 绿）+ ✅ 3b-0 详设拍板（设计 [`§6`](./v2.1-stage3-chat-platform-design.md) 冻结）+ ✅ 3b-1（custom_api 下沉 shared + serve-api llm-proxy，codex REQUEST CHANGES→修→APPROVE WITH NITS，vitest 316 + pytest 247 绿）+ ✅ 3b-2（notion-agent serve-api 复刻，codex REQUEST CHANGES[idle watchdog]→修→APPROVE，pytest 793 + vitest 316 绿）+ ✅ 3b-3（chat 持久化写端点：ChatDb 加 8 写+3 单读 verbatim 镜像 + serve-api 9 写+3 单读端点，codex APPROVE WITH NITS[2 LOW/NIT→修]，pytest tests/api 306 / 全套 817 绿）+ ✅ 3b-4（工具端点 + KOS 代理 + 工具下沉 shared：20 工具下沉 `createBuiltinTools` 单一真源 + `ChatToolPlatform` 定形 + registry 注入式取代 module-global `defaultToolRegistry` + serve-api 3 端点[attachment/search·kos-call·save-to-kos] + Python `kos_save` 复刻 frontmatter 字节对齐，codex REQUEST CHANGES[2 MEDIUM+2 LOW+1 NIT parity]→修→APPROVE WITH NITS，typecheck+build:web+vitest chat 套 258+pytest tests/api tests/cli 847+ruff 全绿） + ✅ 3b-5（HttpChatPlatform 全四板 fetch + debounce + http notion-agent backend + 46 mock-fetch 单测，code-reviewer APPROVE[0 CRIT/HIGH/MEDIUM，2 LOW/3 NIT 全有意 parity/风格]，typecheck node+web + build:web 零 Electron + 全 chat 套 312 零回归）；🟢 **3b 全部完成** + ✅ **3c-0 详设拍板**（设计 [`§6.6`](./v2.1-stage3-chat-platform-design.md)，3 fork A/A/A：ChatRuntime / main webRequest token+CORS / `GET /api/chat/config`）+ ✅ 3c-1（webRequest token+CORS + `GET /api/chat/config`，codex APPROVE，commit `1bb1699`）+ ✅ 3c-2（shared `ChatRuntime` 组装 dispatcher+HttpChatPlatform+进程内 emitter sink 成完整 ChatApi + HttpApi.chat lazy getter 破循环 + serve-api `DELETE /chat/sessions/{id}`，codex 2 轮 APPROVE，pytest tests/api tests/cli 856 + vitest emitter 5/runtime 25）+ ✅ 3c-3（electron `ElectronApi.chat` 切 `createChatRuntime` + search query 注入 apiPort[三 load 入口同源 `resolveApiPort`] + confirmation UI 同进程零改动 + 删 `ElectronChatApi` orphan，codex REQUEST CHANGES[1 HIGH+1 MEDIUM]→修 MEDIUM+HIGH 裁定 backlog→复审 APPROVE，typecheck+build:web+chat 套 402+新 7+onboarding 36 零回归）→ 下一步 3c-4

> **3a 设计已落地**（ChatPlatform seam + 6 步抽取 + D1-D5 决策）。依赖面实测**仅 2 处硬 Electron/Node import**（dispatcher 的 WebContents type-only + notion_agent 的 execa/fs/os/path）→ 下沉从「大改」降级为「定义 ChatPlatform + 搬逻辑 + ElectronChatPlatform 包既有实现（零回归）」。
> 关键决策：**D1** 持久化 cadence 由 platform 控（streamContent debounce / finalizeMessage flush，守 harness 单一真源）· **D2** custom-api 解析留 shared / notion-agent 复刻 Python（按后端性质分，各单实现）· **D3** 3a 保 main 执行（零回归）3c 才搬 renderer · **D4** notion-agent 统一进 harness 删 legacy 单遍 · **D5** 本地也走 serve-api。

| 能力 | serve-api | shared/httpPlatform | 本地接线 | 单测 | parity |
|---|---|---|---|---|---|
| 3a ChatPlatform 分层接口（platform.ts，step3 ✅）| ➖ | ✅ | ✅ ElectronChatPlatform | ✅ typecheck | ➖ |
| 3a harness 下沉 shared（零 Electron，step4 ✅）| ➖ | ✅ | ✅ ElectronChatPlatform | ✅ build:web | ✅ 313 |
| 3a dispatcher 下沉 + handlers 接线（step5-6 ✅）| ➖ | ✅ createChatDispatcher | ✅ 注入 electronChatPlatform+getChatBackend | ✅ 313 | ✅ 零回归 |
| 3b-0 详设拍板（板定形+端点契约+归属+debounce，设计 §6）✅ | ➖ | ✅ 接口冻结 | ➖ | ✅ 4 fork 拍板 | ➖ |
| 3b-1 custom_api 下沉 shared + llm-proxy ✅（codex APPROVE WITH NITS） | ✅ `POST /api/chat/llm-proxy`(原始 SSE 透传/注入 key/仅 2xx passthrough+broad cleanup) | ✅ ChatModelPlatform + createCustomApiBackend factory | ✅ electron llmFetch 本地注入 key/UA | ✅ vitest 316（electron llmFetch 3）+ pytest 247（llm-proxy 10） | ✅ 零回归 |
| 3b-2 notion-agent serve-api 复刻 ✅（codex REQUEST CHANGES[idle watchdog]→修→APPROVE） | ✅ `POST /api/chat/notion-agent`(语义 event SSE，asyncio spawn 复刻 exit 75/77/127 + idle 看门狗覆盖读+wait 双相 + 串行 gate) | ✅ `ChatNotionAgentPlatform` 接口定形（notionAgentStream impl 归 3b-5，仅 http） | ➖ electron `NotionAgentBackend` execa 留 main（零回归 316） | ✅ Python 35 测（exit 75/77/127 + thread 探测 + gate 串行/abort + safeErrorMessage 不泄漏 + idle 双相） | ✅ vs notion_agent.ts |
| 3b-3 chat 持久化写端点 ✅（codex APPROVE WITH NITS[2 LOW/NIT→修]） | ✅ 9 写+3 单读端点（envelope；ChatDb 8 写+3 单读 verbatim 镜像 chat_db.ts；单读返 row\|null 不 404；PATCH 缺 body→E_INVALID_ARG/显式 {} no-op；CORS 补 PATCH） | ✅ HttpChatPlatform.persist(streamContent debounce/finalize flush，3b-5 落地) | ➖ electron chat_db 直写（零回归，3b 不接 renderer→生产单 writer） | ✅ pytest tests/api 306（+24 chat 写：每端点写后读回 + 边界 + key-presence + missing-body 400）；debounce 正确性归 3b-5 | ✅ ChatDb SQL/语义 vs chat_db.ts（codex 逐函数确认 IS NULL/model 刷新/>=/pending,streaming/NULL 初始化全对齐） |
| 3b-4 工具端点 + KOS 代理 + 工具下沉 shared ✅（codex REQUEST CHANGES[5 parity]→修→APPROVE WITH NITS） | ✅ 补 `GET /attachment/search`(repo.search_attachment_texts graceful empty) + `POST /api/chat/kos-call`(KOSClient.call_tool run_in_threadpool→KOSError 502) + `save-to-kos`(Python kos_save 复刻 frontmatter 字节对齐+summarize fallback transcript)；复用 email/*(已有)；draft_reply 远程推迟 | ✅ ChatToolPlatform 定形 + createBuiltinTools(20 工具下沉单一真源 + kos_rerank；registry 注入式取代 defaultToolRegistry) | ✅ electron 工具板扩 electronChatPlatform(8 读直调 handlers/flagEmail·draftReply/kosConfig·kosCallTool·saveToKos)+接线(dispatcher deps+harness registry 必填+handlers 填充+删 main builtin/client.ts rerank orphan) | ✅ vitest chat 套 258(builtin/builtin_kos 注入 mock platform)+pytest tests/api(kos_save 19 frontmatter 字节+kos-call/save-to-kos+attachment search) | ✅ vs main 工具+kos_save.ts(codex 逐点 parity 确认 5 修：全角/contentBytes/put_page 兜底/nullish/毫秒) |
| 3b-5 HttpChatPlatform 组装 + mock-fetch 单测 ✅（归 3b，非 3c；code-reviewer APPROVE[0 HIGH/MEDIUM]） | ➖ | ✅ `shared/chat/http_platform.ts` 全四板（Infra+Model①+NotionAgent②+Tool，构造 `(httpApi,baseUrl,config?)` 可选远程默认快照 `DEFAULT_HTTP_CONFIG`）+ `backends/notion_agent_http.ts`（`createHttpNotionAgentBackend`；custom-api 复用既有 `createCustomApiBackend`） | ➖ | ✅ 46 mock-fetch 单测（persist 12 法端点/debounce 合并·flush 先于终态·abort 清/llmFetch 4 分支/SSE 跨边界·abort·error/工具委托/config 快照/backend factory）+ 全 chat 套 312 零回归 | ✅ ==serve-api 契约（persist path/body camelCase·{deleted}/{aborted}/{id} 投影/llmFetch E_NO_LLM_KEY 500+body 不双消费/notion SSE 对称/工具 8 读零投影；debounce 无竞态无泄漏） |
| 3c-0 详设拍板（3 fork：ChatRuntime/webRequest/config 端点，设计 §6.6）✅ | ➖ | ✅ 接口冻结 | ➖ | ✅ 3 fork A/A/A 拍板 | ➖ |
| 3c-1 webRequest token+CORS（main）+ `GET /api/chat/config` ✅（codex APPROVE） | ✅ GET /chat/config（8 字段快照+归一化对齐 electron getter） | ➖ | ✅ chat_local_bridge（token 总注+CORS 仅打包） | ✅ pytest 57+vitest 9 | ➖ |
| 3c-2 ChatRuntime（shared/chat/runtime.ts）+ HttpApi.chat 切 ✅（codex 2 轮 APPROVE） | ✅ `DELETE /chat/sessions/{id}`（顺手补，envelope {deleted}）+ backendKind enum 校验 | ✅ createChatRuntime（dispatcher+HttpChatPlatform+进程内 emitter sink+完整 ChatApi；engine lazy await /chat/config；confirmTool 同进程 resolveConfirmation） | ✅ HttpApi.chat lazy getter 破循环（web 先通；electron 3c-3 切） | ✅ emitter 5 + runtime 25 mock-fetch | ✅ ==dispatcher/persist/读端点契约 |
| 3c-3 electron 切 ChatRuntime + confirmation UI 同进程 + 本地 e2e ✅（codex REQUEST CHANGES[HIGH backlog+MEDIUM 修]→APPROVE） | ➖ | ➖ | ✅ `ElectronApi.chat=createElectronChatRuntime`（reads `new HttpApi(loopback)`+openPopout override IPC）+ 三 load 入口注 `?apiPort=`（createWindow/popout/onboarding reloadToMain，同源 resolveApiPort）；confirmation 零改动（emitter 同步 fan-out+同进程 resolveConfirmation） | ✅ electron_api_chat 7（端口注入/回退/openPopout/形状）+ chat 套 402+onboarding 36 零回归 | 🟡 软件全绿+codex APPROVE；真 e2e 留真机 |
| 3c-4 删 main 直跑 + D4 删 legacy + migrate bootstrap ✅（codex 1 MEDIUM[finalContent parity]→修→复审 APPROVE no findings） | ➖ | ➖ | ✅ 删 7 生产+4 测试 + index `getChatDb()` bootstrap + `resolveNotionAgentBin` 搬 notion_agent/config + 替身 test_chat_platform/test_backend_registry | ✅ chat 套 16 文件 311 + typecheck node+web + build:web 零 Electron + 残留检测核心干净 | ✅ D4 harness 吸收 notion-agent metadata/tool_call/finalContent 等价 legacy |
| 3c-5a lint+orphan 清理 ✅（全仓 120→0 error，commit `0841ba9`+`2942abd`）| ➖ | ➖ | ➖ | ✅ typecheck+lint+chat 套 325+build:web | ➖ |
| 3c-5b 文档归档 ✅（architecture 文档 + CLAUDE.md 指针 + 看板 gate）| ➖ | ➖ | ➖ | ✅ | ➖ |
| 3c-5c 远程 + 本地真机 e2e（**最后硬缺口**）| ➖ | ➖ | ➖ | ⬜ build feature .app + 部署 + 真机 | ➖ |

---

## 残留检测（每阶段末跑）

```bash
# 阶段 1: serve-api reports 端点 in-process（不 fork CLI）
grep -c "run_cli\|cli_runner" src/api/routers/reports.py   # = 0
# resolve 单一真源: CLI + serve-api 都调 src/reports/wire.py，不各自手抄
grep -rn "get_default_prompt" src/cli/commands/report.py src/api/routers/reports.py  # 0（都经 wire）
grep -c "def resolve_agent\|def config_patch_to_db" src/reports/wire.py  # ≥2（单一真源）
# 本地 getConfig 不再 fork CLI 取默认 prompt
grep -c "loadDefaultPrompts\|report.*config-get" frontend/src/electron/main/handlers/report.ts  # 0（改走 serve-api）

# 阶段 3: shared/chat 零 Electron 依赖（方案成败根本）
grep -rn "from 'electron'\|better-sqlite3\|execa\|handlers/" frontend/src/shared/chat/  # 空
# 删 main chat 直跑
grep -rn "runHarness\|backend.stream" frontend/src/electron/main/chat/  # 阶段 3c 后应空（迁 shared）
```

## 验收 gate

- [ ] 阶段 1：能力矩阵 report 6 端点全绿（serve-api+HttpApi+本地+单测+parity）；`pytest tests/api tests/cli` 全绿；本地 /agents **秒开**实测（fork→in-process）；codex review APPROVE
- [ ] 阶段 2：chat 读 5 端点全绿；远程能看历史；codex review APPROVE
- [ ] 阶段 3：custom-api（读+写工具）+ notion-agent 远程多轮对话；`pnpm test`+`pnpm build:web`（无 Electron 泄漏）；删 main chat 直跑；本地 chat 走 serve-api 行为对齐；codex review APPROVE
- [ ] 真机 e2e（**3c-5c 转用户自验**，清单 [`docs/v2.1-stage3-3c-5c-e2e-checklist.md`](./v2.1-stage3-3c-5c-e2e-checklist.md)）：feature `.app` 已 build+装机 + **serve-api 层 e2e ✅**（/chat/config 200 带 token + 远程 tunnel live）；renderer chat 全链路（custom-api/notion-agent/写工具确认/远程 parity）+ react-hooks 重构回归 = **用户手动验证**（computer-use 缺 Accessibility 受阻转人工）
- [x] 文档（3c-5b ✅）：`docs/claude/remote-chat-report-architecture.md` 新建 + CLAUDE.md 文档地图指针 + 本看板 3c-5a/5b 进度日志/gate；整体 archive 待真机 e2e 后
- [x] **lint 全量修复（3c-5a ✅，用户拍板「全仓真正零 error」）**：全仓 120 errors → **0**。手法见进度日志 3c-5a（cutover orphan 删除 + eslint `_` 豁免 + docs ignore + 删 stale 产物 `frontend/index.js` + react-hooks 真重构/深水区 disable + 设计系统 coral/raw-hex/cjk）。

## 进度日志

- **阶段 1（✅ 完成，codex gpt-5.5 APPROVE）** `feat/v2.1-remote-chat-report`：report/agent serve-api 端点 + 修 agents 慢。
  - **后端**：新建 `src/reports/wire.py`（resolve_agent/parse_counts/report_to_list_item/report_to_detail/config_patch_to_db — CLI+serve-api 读形状/patch 单一真源，仿 D2a）；CLI `report.py` 重构调 wire（删本地手抄）；新建 `src/api/routers/reports.py`（6 端点 in-process ReportStore 不 fork；runNow 走 `asyncio.to_thread(asyncio.run, run_report_once)` 独立 loop 对齐 CLI）；`deps.py` 加 `get_report_store()`；`app.py` 注册 + 全局 `RequestValidationError` handler（422→envelope）。
  - **前端**：`HttpApi.ReportApi` 去 stub 接 6 端点（读 catch 返 []/null 守契约，写 req() 解包失败 throw）；本地 `report.ts` getConfig 改走 serve-api（in-process resolve 含默认 prompt ~ms 取代 fork CLI config-get ~759ms = agents 慢根因）+ 删 `loadDefaultPrompts`，不可达兜底直读。
  - **codex review（gpt-5.5 high）**：首轮 REQUEST CHANGES 2 MEDIUM —— ① `wire.resolve_agent` 坏数据（JSON array schedule → `.get()` 崩 + trigger_mode 未 clamp）② `body: Optional[dict]` malformed → FastAPI 默认 422 非 envelope。两个均修（schedule 强制 dict+默认 / trigger_mode clamp 对齐 TS / 全局 RequestValidationError handler 422→E_INVALID_ARG envelope）+ 加 4 测试。**re-review APPROVE，Findings none**。
  - **验收**：`pytest tests/cli tests/api` **737 passed**（含 19 reports 端点测试 + wire 边界 + 422 envelope）；`tests/reports` 全过；ruff + 前端 typecheck/eslint 全绿。
  - **隔离**：staged 仅 9 个 V2.1 文件，用户在途 LLM client.py 工作（CRS UA/streaming）严格排除。
  - **附带修复**：`~/.codex/config.toml` 的 `service_tier="default"`（codex 只认 fast/flex，否则所有调用 json decode error）已注释。

- **阶段 2（✅ 完成，code-reviewer opus APPROVE WITH NITS）** chat 只读历史。
  - **后端**：新建 `src/chat/db.py`（ChatDb 只读 ai_chat.db，读函数 SQL **verbatim 镜像** chat_db.ts，graceful 库不存在→[] 不建空库）+ `src/api/routers/chat.py`（5 读端点：listSessions/listAllSessions/listMessages/listToolCalls/kosAvailable；listAllSessions join sync_store.db email_metadata best-effort）+ `deps.py` 加 `get_chat_db()` + `app.py` 注册。ai_chat.db 路径 = `DATA_ROOT/frontend/ai_chat.db`（serve-api 注入 MAILAGENT_DATA_ROOT，对齐 chat_db.ts resolveChatDbPath）。
  - **前端**：`HttpApi` chat 读方法去 stub（catch 返 []/false 守契约）；写/流方法留阶段 3。本地 chat 读保留直读 replica（不变式 4），阶段 3 整体走 serve-api。
  - **review（gpt-5.5 high 初轮）**：REQUEST CHANGES 2 finding —— ① `_email_meta_for_sessions` 无 exists 保护（sync_store.db 不存在会建空库）② `sender_name or sender` ≠ chat.ts `??`（空字符串边缘）。两个均修（os.path.exists 短路 + email_ids 空不调 get_settings / `sender_name is not None`）+ 加 2 单元测。**codex 用量限额无法 re-review → code-reviewer subagent（opus）替代确认：APPROVE WITH NITS，2 finding 修复正确完整、SQL parity verbatim、0 CRITICAL/HIGH**（4 nits cosmetic/cohesion 非阻塞）。
  - **验收**：`pytest tests/api` **239 passed**（含 chat 13 测试）+ ruff + 前端 typecheck/eslint 全绿。

- **阶段 3 — 3a 设计 + steps 1-2b（✅ 类型+tools 下沉，codex gpt-5.5 APPROVE WITH NITS）** chat 对话 B-pure-unified。
  - **理解**：精确测绘 chat 模块 5536 行（types/harness/dispatcher/config/registry/dispatch/confirmation/tool-registry/handlers-chat + Explore 摸 backends/custom_api·notion_agent·kos_save）。依赖面实测**仅 2 处硬 Electron/Node import**（dispatcher WebContents type-only + notion_agent execa/fs/os/path）；其余跨模块依赖（持久化/邮件上下文/LLM fetch/工具→handlers/KOS/sender digest/config）皆注入式或可抽象。代码本就良好分层（sink 注入、confirmation module map、tools registry、`surface` 字段为下沉预留）→ 下沉从「大改」降级为「定义 seam + 搬逻辑 + ElectronChatPlatform 零回归」。
  - **设计**：`docs/v2.1-stage3-chat-platform-design.md`（ChatPlatform 接口 + 3a 6 步抽取 + 3b/3c 概要 + 矩阵行 + D1-D5 决策 + 风险）。核心难题=持久化 cadence（harness 每 chunk 同步 `updateMessage`，直接 HTTP 化会几百 HTTP/响应）→ D1 解：拆 `streamContent`(debounced)/`finalizeMessage`(flush)，cadence 进 platform 层守 harness 单一真源。
  - **附带修复**：`HttpApi.notImplemented` 同步 throw → reject（远程「AI 未配置→前往设置」降级 toast 不再 ErrorBoundary 崩，commit `c1fbe7b`）。真机验证发现：远程「未配置」空状态=预期阶段 3 边界，硬崩溃=已修 bug。
  - **step 1（`787470e`）**：chat_db.ts 数据模型类型（BackendKind/ChatMessage/ChatSession/ConfirmationTier/ChatToolCall/输入 patch 等 14 个）→ `shared/chat/model.ts`（零 import 纯类型），chat_db.ts import+re-export 保 6 importer 路径不变。
  - **step 2a（`fa06eb2`）**：git mv `chat/types.ts` → `shared/chat/types.ts`（`from '../chat_db'`→`./model`），6 源 importer 改指 `@shared/chat/types`。
  - **step 2b（`e3e8004`）**：git mv `tools/{registry,confirmation,dispatch}.ts` → `shared/chat/tools/`，11 源 importer（含 builtin×5 留 main）+ 7 测试改指；纯搬不 dedup ConfirmationTier。纯 tools 测试 42 passed。
  - **codex review（gpt-5.5 high）**：**REQUEST CHANGES** 1 HIGH（step 2a 漏改 tests/ 里 5 个 import `chat/types` 的测试 — typecheck 只覆盖 tsconfig.node/web 不含 tests、纯 build gate 抓不到 test-time 断裂）+ 1 LOW（ConfirmationTier 与 model.ts 重复，认同保纯度暂不 dedup）。HIGH 修（`a38d212`，5 测试 import 改指 `src/shared/chat/types`）→ 验收 `rebuild:node` + **全 chat 测试套 15 文件 312 passed**（零回归基线达成）→ **re-review APPROVE WITH NITS**（仅 LOW 留 cleanup）。
  - **教训**：移文件的 importer grep 必须含 `tests/`（typecheck 只覆盖 tsconfig.node/web，纯类型/逻辑搬迁靠 typecheck+build:web 验时，test-time import 断裂不暴露 → 必跑 `rebuild:node`+vitest 或全量 grep tests）。
  - **附带修复**：`HttpApi.notImplemented` 同步 throw → reject（远程「AI 未配置→前往设置」降级 toast 不再崩，`c1fbe7b`）。
  - **下一步 → steps 3-4 已完成（见下条目）**。

- **阶段 3 — 3a steps 3-4（✅ 分层接口 + harness 下沉，全 chat 套 312 绿）** chat 对话 B-pure-unified。
  - **step 3（`5743fe3`）**：`shared/chat/platform.ts` **分层接线板**（用户拍板）。`ChatInfraPlatform`（基础设施板 3a 定形）= persist（`ChatPersistPort` 12 法，updateMessage 拆 streamContent fire-and-forget / finalizeMessage await，D1）+ loadEmailContext + resolveConfig（env 快照）+ prefetchSenderDigest；模型板/工具板仅占位注释（3b 按真实形状定形，不写无消费方假线）。`backendSupportsTools` 下沉 model.ts（config.ts re-export）。设计文档 §3 单接口（9 原语）→ 分层（§3.1-3.5 + 演进记录）。grep 实测 harness+dispatcher 外部调用只落这 4 类，另 5 原语消费方 3a 留 main 不调 platform。
  - **step 4（`d737a09`）**：git mv harness → `shared/chat/harness.ts` 全路由 `args.platform`（streamContent 守同步写零回归 / 终态·工具·abort await）。新建 `electron_platform.ts` **ElectronChatPlatform**（persist 转发 chat_db / loadEmailContext 移入单一真源 / resolveConfig 读 config / prefetch 转发 kos，导出单例）。dispatcher 接线（runHarness 传 platform + loadEmailContext 改 platform；**主体 startChat/legacy/abort 仍直调 chat_db，step5 再 platform 化**）。harness.test 注入 electronChatPlatform。typecheck+build:web（shared/chat 零 Electron）+ 全 chat 套 15 文件 **312 passed**（零回归基线）。
  - **教训 tickAsync**：platform seam 后 chat:start→事件 drain 异步链变深，chat_dispatcher.test「sink 顺序」`tickAsync(6)` 固定 microtask burst 不够（done 未达）→ 改轮询 done 落地（仿测试文件 2026-05-26 happy-path 既定模式），不耦合 microtask-chain 深度。功能零回归。
  - **codex GPT-5.5 xhigh review（`28e0a3a`）= APPROVE WITH NITS（无阻塞）**：零回归核心确认（streamContent 同步先于 finalize / 返回类型协变 / persist 无 this / shared 零 Electron / 无循环依赖）。2 MEDIUM 趁热闭环：**MEDIUM-1** while 顶部 abort 原 `break` 落 E_MAX_ITER 误报（本增量 await resolveConfig 放大窗口）→ 改 `abortStreamingMessages+return` + 补 abort-before-iter 回归测试；**MEDIUM-2** 非 complete 终态不带 content + 契约「取消增量」→ http debounce 丢尾段 → platform 注释改 **flush**（非 cancel）+ harness 所有终态带 `content:buffer`（electron 同值零回归，http 双保险）。全 chat 套 **313 passed**（+1 测试）。
  - **下一步 step 5-6（fresh context，「多文件重连宜 fresh」+ dispatcher 半截即红）**：dispatcher 主体下沉（建议 `createChatDispatcher({platform,getBackend})` factory，legacy 单遍全 platform 化，`drainNotionAgentGate` 留 main wrapper，`makeWebContentsSink`/WebContents 留 main）+ handlers/chat 构造 dispatcher + chat_dispatcher.test（767 行 23+ test）driver 改 factory + 312 绿 → **3a 出口 codex review**。详见 [`v2.1-stage3-step3-handoff.md`](./v2.1-stage3-step3-handoff.md) §2。

- **阶段 3 — 3a step 5-6（✅ dispatcher 下沉 + 3a 出口，codex xhigh REQUEST CHANGES → 修 → 复审 APPROVE WITH NITS，313 绿）= 3a 全部完成** chat 对话 B-pure-unified。
  - **step 5（`43d3ea3`）**：dispatcher 主体下沉 `shared/chat/dispatcher.ts`，`createChatDispatcher({platform,getBackend})` **factory**（`_inflight` 进 closure，per 实例隔离，3c renderer 可注入 HttpChatPlatform）。startChat / editChatMessage / runStream(legacy 单遍) 全 platform 化（`chat_db.*`→`deps.platform.persist.*` / `getChatBackend`→`deps.getBackend` / `isHarnessEnabled`→`resolveConfig().harnessEnabled` / loadEmailContext 经 platform）。legacy 终态对齐 steps3-4 review（abort 标 aborted + error 终态带 `content:buffer`）。`makeWebContentsSink`+WebContents 留 main（新建 `chat/web_contents_sink.ts`）；`abortAllChatSessions` 拆（shared 只 abort+clear `_inflight` / main handlers wrapper 补 `drainNotionAgentGate`，`index.ts:15/465` 路径不变）。handlers/chat.ts 模块级构造 dispatcher 注入 `electronChatPlatform`+`getChatBackend`。删旧 main `chat/dispatcher.ts`。
  - **step 6（`43d3ea3`）**：chat_dispatcher.test driver 改 `createChatDispatcher` 注入 electronChatPlatform；`tickAsync` 默认 6→50（dispatcher gate 多一层 `await resolveConfig` + persist Promise 化拉长 legacy for-await 链；对 blocking backend test 仅空转无副作用）；abortChatSession 调用点 async 化。全 chat 套 15 文件 **313 passed**（3a 零回归基线）。
  - **codex GPT-5.5 xhigh review（修复 `aeff4d8`）= REQUEST CHANGES → 复审 APPROVE WITH NITS**：4 findings 全修。**HIGH** runStream 的 gate(resolveConfig)+harness 移进统一 try/catch/finally（原在 try 外 + startChat/editChatMessage 的 `void runStream` fire-and-forget → resolveConfig/runHarness 抛会致未捕获 rejection + assistant 永久 streaming + `_inflight` 不清；electron 实现不抛，3c HttpChatPlatform fetch 会）：catch 兜底 finalize error + finally 无条件按当前 AC 清。**MEDIUM** startChat pre-empt（abort 旧 AC）移到 append 新消息前（防 rapid resend 期间旧 stream 在 await 让步窗口跑完错标 complete，破坏 rapid-click guard 零回归）。**MEDIUM** chat:abort 的 `void dispatcher.abortChatSession()` 补 `.catch(warn)`。**LOW** test 加 `waitForAssistant` 轮询 helper，3 个 notion-agent legacy 终态断言（tool_call/metadata×2，microtask 链最长）改轮询去 tickAsync 耦合。**复审 LOW** 两个 `void runStream` 补 `.catch(warn)`（收口 catch-path finalizeMessage 自身 reject 的残留 rejection，关闭 HIGH 兜底尾巴）。**复审 NIT（暂不处理，记已知）** pre-empt 前剩 getSession/getOrCreateSession 1 个 await 窗口（electron 1 microtask 不触发旧 stream 完成；no-sessionId 路径 inherent，收益微小）。
  - **3a 完成判据达成**：harness（step3-4）+ dispatcher（step5-6）全部下沉 `shared/chat/` 经 ElectronChatPlatform，**字节级零回归**（typecheck node+web + build:web shared/chat 零 Electron + 全 chat 套 313 绿 + grep importer 干净）。backends（custom_api/notion_agent）按设计留 main 经 `args.backend`/`getChatBackend` 注入（下沉归 3b 模型板）。
  - **下一步 3b**（设计 §6 + 矩阵 3b 行）：serve-api chat 后端端点（`POST /api/chat/llm-proxy` 透传 LLM 注入 key / `POST /api/chat/notion-agent` asyncio spawn 复刻 / chat 持久化写端点 / 工具端点 / KOS 代理）+ `HttpChatPlatform`（shared/web fetch 上述端点，streamContent debounce 在此）+ custom_api/notion_agent 下沉模型板（`ChatModelPlatform` 按真实形状定形）。

- **阶段 3 — 3b-0 详设（✅ 拍板，设计 §6 冻结 + 矩阵 3b 行细化）** chat 对话 B-pure-unified。
  - **ground truth 全摸排**：读透 chat 全栈（shared platform 三板/harness/dispatcher · 两 backend custom_api 双协议 SSE/notion_agent execa · serve-api chat+reports router/ChatDb/deps/app/auth · HttpApi/http_client · 20 工具 builtin/registry/dispatch · kos_save/KOSClient/llm_settings/config · write_ops/draft handler）。
  - **6 关键发现**：① 写工具 electron 已走 serve-api（D1 `runEmailFlag`=daemonRequest，flag/archive 只差 baseUrl/auth，零 parity 天然达成）② 8 读工具 + flag 的 serve-api 端点已全覆盖（email/email_views/attachment router）③ 仅 2 工具端点缺（`email_search_attachments` + `email_draft_reply` host-local AppleScript）④ `ChatDb` 现纯读，持久化写要加 8 写方法（verbatim 镜像 chat_db.ts）⑤ serve-api `config.llm_api_key` + `src/kos/client.py` 后端凭据就绪 ⑥ custom_api 解析全 `__testing` 导出（纯逻辑搬迁友好）。
  - **板定形（拆 3 板，对接 3a 占位 line 102-114）**：**ChatModelPlatform**（custom_api：`llmFetch`[原始 SSE Response，key 不进 shared]/`getCachedSenderDigest`/`modelConfig`[defaultModel+kosConsumerEnabled+kosL1HotBlockEnabled 同步]）· **ChatNotionAgentPlatform**（`notionAgentStream`，仅 http 实现，electron execa 不碰）· **ChatToolPlatform**（8 读+3 写原语+`kosCallTool`[9 工具收敛 tools/call]+`kosConfig`+`saveToKos`；工具逻辑下沉 shared `createBuiltinTools` 单一真源，零 parity）。
  - **4 fork 拍板（全采纳推荐）**：① 模型板拆两块独立小板（custom_api 与 notion_agent 需求不相交 + electron notion-agent execa 不经 platform，避免占位假线）② HttpChatPlatform 落 `shared/chat/http_platform.ts`（就近 platform.ts，避新建 shared/web）③ `email_draft_reply` 远程推迟（host-local GUI backlog 裁定，http throw E_NOT_IMPLEMENTED）+ `email_search_attachments` 补端点 ④ custom_api config 经 `modelConfig()` 同步 getter 进模型板（不改 ChatStreamRequest 契约）。
  - **serve-api 端点契约**：`POST /api/chat/llm-proxy`（原始上游 SSE 透传，非 envelope，注入 key）· `POST /api/chat/notion-agent`（语义 event SSE，asyncio spawn 复刻 exit 75/77/127）· chat 持久化 8 写+3 单读端点（ChatDb 镜像）· 工具复用 email/* + 补 `GET /attachment/search` · `POST /api/chat/kos-call`+`save-to-kos`（复用 src/kos/client.py）。
  - **debounce（D1）**：http persist.streamContent fire-and-forget → per-messageId {pending 累积 buffer 覆盖, ~1s trailing-edge timer} PATCH；finalizeMessage 先 flush 再写终态（防覆盖，harness 终态带 content=buffer 双保险）；electron 仍同步直写零回归。
  - **5 子步**（每步 codex xhigh review）：3b-1 custom_api 下沉+llm-proxy（半截即红）→ 3b-2 notion-agent 复刻 → 3b-3 持久化写+debounce → 3b-4 工具端点+KOS+工具下沉 → 3b-5 HttpChatPlatform 组装+mock-fetch 单测。**httpPlatform 归 3b-5（非 3c），3c 只接 renderer transport。**
  - **下一步 3b-1**：custom_api 解析下沉 `shared/chat/backends/custom_api.ts`（调 platform.llmFetch）+ `createCustomApiBackend` factory + electronChatPlatform 加模型板①实现 + serve-api llm-proxy + custom_api_backend.test 改 shared 路径注入 mock。⚠️ 半截即红一次搬完+改测试。

- **阶段 3 — 3b-1（✅ custom_api 下沉 shared + serve-api llm-proxy，codex REQUEST CHANGES → 修 → 复审 APPROVE WITH NITS，vitest 316 + pytest 247 绿）** chat 对话 B-pure-unified。
  - **实现（commit `9f40667`，半截即红一次搬完）**：custom-api SSE 解析（双协议 Anthropic/OpenAI）`git mv` electron/main/chat/backends → `shared/chat/backends/custom_api.ts`（parser/state-machine 字节级不动）。改造：buildSystemBlocks/buildStableSystemPrompt/buildSystemPrompt 接 `cfg+getCachedSenderDigest` 参数（取代直读 env+main kos cache）；anthropic/openaiStream 的 key-check+直 fetch → `platform.llmFetch`（key 不进 shared）；`CustomApiBackend` class → `createCustomApiBackend(platform)` factory；零 main import（不变式 1）。`platform.ts` 定 `ChatModelPlatform`（llmFetch[原始 SSE Response]/getCachedSenderDigest/modelConfig[defaultModel+kosConsumer+kosL1 同步]）+ LlmFetchRequest/ChatModelConfig（notion-agent/工具板仍占位）。`electron_platform.ts` 扩 `ChatInfraPlatform & ChatModelPlatform`（llmFetch 本地 fetch 注入 key/UA/baseUrl + getCachedSenderDigest`??null` + modelConfig 读 env getter）。`index.ts` boot 改 `createCustomApiBackend(electronChatPlatform)`（notion-agent execa 不动）。serve-api `POST /api/chat/llm-proxy`（httpx.AsyncClient stream 透传上游 SSE + 注入 `config.llm_api_key` + protocol 路由 endpoint/header；非 envelope）。
  - **codex GPT-5.5 review REQUEST CHANGES → 修（commit `9f7424b`）→ 复审 APPROVE WITH NITS**：**MEDIUM** llm-proxy 只挡 `>=400`，3xx 被当 streaming success 转发 redirect body → 改 `<200 or >=300`（仅 2xx passthrough）。**LOW** pre-flight `except httpx.HTTPError` 漏 `httpx.InvalidURL`（malformed LLM_API_BASE，非 HTTPError）→ 跳 aclose 泄漏 + 500 → broad `except Exception` 兜底 aclose + 502。**MEDIUM** abort/key-check order：原 electron key-check 先于 abort，下沉后 key 进 llmFetch、shared abort-check 先 →「aborted+缺 key」竞态 E_NO_LLM_KEY→E_ABORTED → bless 新契约（abort 优先 key，语义更优 + 竞态罕见）+ anthropic/openai 两处注释（逻辑未改，非回归）。**Note** electron llmFetch 无测 → 新建 `electron_platform.test`（钉 anthropic→/v1/messages+x-api-key+anthropic-version+UA / openai→/v1/chat/completions+Bearer / key 不进 body / key 缺失 throw）。复审 nit 顺手闭：test_chat 加 302 + InvalidURL/aclose 回归测试。
  - **验收**：typecheck(node+web) + ruff + build:web（shared 零 Electron）+ 残留检测干净（仅注释）+ vitest 16 文件 **316**（313 基线 + electron llmFetch 3，L1 cache-miss/null-entry 合并 + 补 kosConsumer gate 净持平）+ pytest tests/api **247**（llm-proxy 10）全绿。
  - **autoflake 陷阱（已修）**：先 Edit imports 加 APIError/Response/StreamingResponse 时端点未引用 → ruff 删 → NameError 500；端点引用后加回不再删（memory 记载的 Python 编辑纪律：先写使用方或同次落地）。
  - **下一步 3b-2**：notion-agent serve-api `POST /api/chat/notion-agent`（asyncio spawn 复刻 thread 探测/gate/exit 75/77/127 分类）+ `ChatNotionAgentPlatform.notionAgentStream`（HttpChatPlatform 实现；electron `NotionAgentBackend` execa 留 main 不动）+ Python 测对齐 notion_agent_gate。

- **阶段 3 — 3b-2（✅ notion-agent serve-api 复刻，codex REQUEST CHANGES[1 HIGH idle watchdog] → 修 → 复审 APPROVE，pytest 793 + vitest 316 绿）** chat 对话 B-pure-unified。
  - **实现**：新建 `src/chat/notion_agent.py`（Python 复刻 `notion_agent.ts` 全语义：extract_turn[metadata.thread_id 优先 legacy model] / format_email_context_header[notionPageId→notion.so URL 去 dash，首轮注入] / classify_exit[75→RATE_LIMIT/77→AUTH/127→NOT_INSTALLED authoritative + stderr trust-rule-denied/token_v2/cloudflare 兜底] / safe_error_message[token_v2 cookie 不泄漏 client，C-04] / snapshot_thread_files + detect_new_thread_id[mtime 最新] / run_notion_agent async generator[逐 chunk 增量 decoder + idle 看门狗 + 串行 gate] / `_SubprocessHandle`[asyncio subprocess 适配，stderr 后台 drain 防死锁、stdin write+close 不 drain 防死锁] / sse_encode[语义 event SSE，camelCase 对齐 ChatStreamEvent]）+ `src/chat/notion_agent_gate.py`（`NotionAgentSerialGate`：asyncio.Lock mutex + 从起点测的 min-interval，注入 now/sleep seam；abort=asyncio 任务取消，Python 习惯用法对应 TS AbortSignal）+ serve-api `POST /api/chat/notion-agent`（StreamingResponse 语义 event SSE，非 envelope；malformed body→APIError，空 history/exit 分类→in-stream error 事件）+ `platform.ts` 定 `ChatNotionAgentPlatform` 接口（notionAgentStream，仅 http；electron 不实现，execa 留 main）。**electron `NotionAgentBackend` execa + index.ts 完全不动（零回归 316）。client `notionAgentStream` impl 推 3b-5 组装**（与 HttpChatPlatform）。
  - **codex GPT-5.5 review REQUEST CHANGES → 修 → 复审 APPROVE**：**HIGH** idle 看门狗在 stdout EOF 后失效 —— `read_chunk()` 返 b"" 跳出循环后 `await proc.wait()` 无超时，子进程关 stdout 但 hang 在 cleanup/网络/只留 stderr 不退 → 永久 wedge 串行 gate 阻死后续所有调用（TS 原版 idle timer 持续 armed 到 `await child` 之后才 disarm，notion_agent.ts:339-340）。修：wait 阶段包 `asyncio.wait_for(proc.wait(), timeout=idle_s)`，超时→kill+E_NOTION_AGENT_TIMEOUT；读阶段已 kill 则直接 reap（SIGKILL 后即退，unbounded 匹配 TS）+ 回归测试 `test_idle_timeout_on_hung_wait_after_eof`（EOF 后 wait hang→看门狗覆盖）。**复审确认 HIGH 解决 + reap 安全 + finally kill 幂等 + stderr 不越界，无残留 LOW/MEDIUM → APPROVE**。
  - **验收**：`pytest tests/api tests/cli` **793 passed**（+35 notion-agent：exit 75/77/127 + thread 探测/snapshot + extract_turn 4 路 + email header + gate 串行/abort/min-interval/幂等 + idle 双相 + safeErrorMessage 不泄漏 + 端点 SSE/malformed）+ ruff + typecheck(node+web) + `build:web`（shared/chat 零 Electron，含新接口）+ chat vitest 16 文件 **316**（electron 路径零回归）+ 残留检测干净（shared/chat 仅注释提及 execa；端点不 fork mailagent CLI）。
  - **附带修复**：`~/.codex/config.toml` 的 `service_tier = "default"`（活跃行，codex 只认 fast/flex → json decode error 阻塞所有调用）再次注释（memory/handoff 记载过的复发坑）。
  - **下一步 3b-3**：chat 持久化写端点 —— `ChatDb` 加 8 写 + 3 单读方法（SQL verbatim 镜像 chat_db.ts）+ serve-api 写端点（getOrCreate/createNew/append/streamContent/finalize/delete-from/abort/append-tool-call/update-tool-call + 单读 getSession/getMessage/getToolCallByUseId）。client `HttpChatPlatform.persist`（12 法 fetch + streamContent debounce ~1/s + finalizeMessage flush，D1）按 3b-2 先例推 3b-5 组装（serve-api 子步独立 pytest 可验，client 板 + debounce + mock-fetch 测集中 3b-5）。详见设计 §6.5 3b-3 + handoff。

- **阶段 3 — 3b-3（✅ chat 持久化写端点 + ChatDb 写方法，codex GPT-5.5 APPROVE WITH NITS[2 LOW/NIT → 修]，pytest tests/api 306 / 全套 817 绿）** chat 对话 B-pure-unified。
  - **实现**：`src/chat/db.py` 的 `ChatDb`（阶段 2 纯读）加 **8 写**（`get_or_create_session`/`create_new_session`/`append_message`/`update_message`/`delete_messages_from_id`/`abort_streaming_messages`/`append_tool_call`/`update_tool_call`）+ **3 单读**（`get_session`/`get_message`/`get_tool_call_by_use_id`），SQL **逐字镜像** `chat_db.ts` 对应函数（`IS NULL` vs `= ?` 分支 / model 刷新 `{**existing,...}` / append 后 bump session 同 now 同事务 / `>= ?` / `status IN ('pending','streaming')` / tool_call NULL 初始化）；`update_message`/`update_tool_call` 用 `key in patch` 复刻 TS `!== undefined`（wire 省略 key→不更，显式 null→更为 NULL，字段映射常量 `_MESSAGE_PATCH_FIELDS`/`_TOOL_CALL_PATCH_FIELDS` 顺序对齐 chat_db.ts）+ `_write_connection`（clean exit commit/异常 rollback/总 close/FK ON，per-call 短命 WAL 安全）+ `_read_one`（graceful 同读路径）+ `_now_ms`。serve-api `src/api/routers/chat.py` 加 **9 写 + 3 单读端点**（envelope；`POST /sessions`·`/sessions/new`·`/sessions/{id}/messages`·`PATCH /messages/{id}/stream`·`PATCH /messages/{id}`·`DELETE …/messages/from/{msgId}`·`POST /sessions/{id}/abort`·`POST /messages/{id}/tool-calls`·`PATCH /tool-calls/{id}` + `GET /sessions/{id}`·`/messages/{id}`·`/messages/{id}/tool-calls/{useId}`）。`app.py` CORS `allow_methods` 补 `PATCH`（PATCH 端点远程 browser preflight，3c transport 用）。**零 TS 改动**（不碰 electron persist/platform.ts；`HttpChatPlatform.persist`+debounce 归 3b-5）。
  - **关键裁定（codex 确认全合理）**：① 单读端点返 `success_envelope(row|null)` **不 404** —— `ChatPersistPort` 契约是 `…|null`，null 是正常结果（尤 `getToolCallByUseId`「没见过此 tool_use_id」）。② 写路径**不**做「库不存在 graceful」—— schema 归前端 owns（chat_db.ts migrate 唯一真源），Python 绝不建表；3c 须保证首次 HTTP 写前前端已 migrate/bootstrap（codex 附注）。③ CORS 补 PATCH 非 scope 越界（codex：是 PATCH 端点的 required plumbing）。④ 3b 不接 renderer（http persist 仅 3b-5 mock-fetch）→ **生产单 writer**，3c（D5 本地走 serve-api）后亦单 writer，短暂双写 WAL 安全。
  - **codex GPT-5.5 review APPROVE WITH NITS → 修**：**LOW** `finalize_message`/`update_tool_call` 把缺 body（None/JSON null）`or {}` 当空 patch 静默 no-op success，违「body 校验缺失→E_INVALID_ARG」纪律（其余写端点有必填字段校验已挡 None，唯这俩 patch 端点无必填字段漏网）→ 修：`if body is None: raise APIError(E_INVALID_ARG)`，显式 `{}` 仍合法 no-op（对齐 chat_db.ts 无字段早返），实测 missing→400 / `{}`→200。**NIT** `update_tool_call` 缺 key-presence 直测（`update_message` 有）→ 补 `test_update_tool_call_key_presence_semantics`（空 patch no-op / 显式 None 置 NULL / 省略 key 不动）。codex **Checks 全绿**：SQL/语义 parity 逐函数对齐、routing `:int` 防 `/sessions/all`·`/sessions/new` 被吞、三裁定成立。
  - **验收**：`pytest tests/api` **306 passed**（+24 chat 写：sessions getOrCreate 复用/model 刷新/新建、append+读回+session bump、stream content-only、finalize 全/部分/空 patch、delete-from count、abort pending+streaming、tool_call append+by-use-id null、update_tool_call、key-presence 直测×2、missing-body 400×2、各端点缺字段 400）+ 全套 `tests/api tests/cli` **817**（零回归）+ ruff check 全过 + **零 TS 改动**（typecheck/vitest/build:web 按构造不受影响）。
  - **下一步 3b-4**：工具端点 + KOS 代理 + 工具下沉 shared —— `shared/chat/tools/builtin/`（`createBuiltinTools` + 20 工具逻辑搬迁，handler 改调 `toolPlatform`）+ `ChatToolPlatform` electron 实现（读直调 handlers / 写 `runEmailFlag`·createDraft / `kosCallTool`·`saveToKos`）+ serve-api 补 `GET /attachment/search`·`POST /api/chat/kos-call`·`/save-to-kos`（`email_draft_reply` 远程推迟 throw `E_NOT_IMPLEMENTED`）+ HttpChatPlatform 工具板。详见设计 §6.5 3b-4 + handoff。

- **阶段 3 — 3b-4（✅ 工具端点 + KOS 代理 + 工具下沉 shared，codex REQUEST CHANGES[5 parity]→修→APPROVE WITH NITS）** —— 详细记录见 [handoff](./v2.1-stage3-3b-handoff.md) §进度 3b-4 + 表格 line 66（20 工具下沉 `createBuiltinTools` 单一真源 + registry 注入式 + serve-api attachment/search·kos-call·save-to-kos[Python kos_save frontmatter 字节对齐]）。

- **阶段 3 — 3b-5（✅ HttpChatPlatform 组装 + mock-fetch 单测，code-reviewer APPROVE[0 CRIT/HIGH/MEDIUM·2 LOW·3 NIT 全不阻塞]）** chat 对话 B-pure-unified。
  - **实现**：新建 `frontend/src/shared/chat/http_platform.ts`（`HttpChatPlatform` class 实现**全四板** Infra+Model+NotionAgent+Tool，构造 `(httpApi, baseUrl, config?: Partial<HttpPlatformConfig>)` —— 可选第三参带远程默认快照 `DEFAULT_HTTP_CONFIG` 对齐 electron chat/config.ts[harness ON/timeDecay ON/maxIter 8/maxCostUsd 0.5/sonnet/kosConsumer·L1·kosConfigured off]）：
    - **Infra 板** persist 12 法 fetch 3b-3 端点（getOrCreate/createNew→POST /chat/sessions[/new]、单读 GET、listLastNMessages=GET 全量+client `slice(-limit)`、appendMessage 去 sessionId 进 body、deleteMessagesFromId→{deleted}、abort→{aborted}、appendToolCall 去 messageId 返{id}、updateToolCall PATCH、getToolCallByUseId `encodeURIComponent`→{id}|null）；**streamContent debounce**（per-messageId trailing throttle ~1s，累积 buffer 覆盖+仅空闲 arm timer，fire→PATCH /stream+disarm）；**finalizeMessage 先 flush 待发增量（clear timer+await PATCH latest）再 await PATCH 终态**（防晚到 stream 覆盖终态，codex MEDIUM-2 双保险+终态带 content=buffer）；abort 清所有 pending timer 防泄漏；PATCH /stream 失败 warn 不阻断。loadEmailContext=GET /email/{id}+/body 投影（ai_priority/action/processing **degrade null**，serve-api /email/{id} 不返回 email_metadata 列，handoff 冻结；body reject 不致命保元数据）。prefetch/getCachedSenderDigest no-op（L1 OFF）。
    - **Model 板** llmFetch→POST /chat/llm-proxy（`!resp.ok` 时 `clone().json()` peek error.code，**E_NO_LLM_KEY[HTTP 500 envelope]→throw Error&{code}**，其余返回原 resp 让 custom_api 读 status 分类 429→E_QUOTA/else→E_UPSTREAM；custom_api 对 !ok 仅读 status 不读 body→**无双消费**）。
    - **NotionAgent 板** notionAgentStream→POST /chat/notion-agent（body 去 signal/tools/iterHistory）+ `parseSemanticEvent`（`data: {json}\n\n`→JSON.parse，跨 read() 边界 buffer 重组，abort 静默，pre-stream !ok→yield error，fetch 失败非 abort→E_NETWORK，reader finally 释放）。与 `sse_encode` 对称。
    - **Tool 板** 8 读**零投影**委托 httpApi.email.*/attachment.list（api/types=cli.gen 别名/超集：EmailMeta=EmailList_EmailListItem 等）+ searchAttachments→GET /attachment/search（q/mailbox/since/until/limit）；flagEmail→httpApi.email.flag；draftReply→reject E_NOT_IMPLEMENTED（host-local 远程推迟）；kosConfig→快照；kosCallTool/saveToKos→POST /chat/kos-call·/save-to-kos（KOS 错误 502 envelope code=E_KOS_*→ApiError{code} 工具 duck-type fallback）。
    新建 `frontend/src/shared/chat/backends/notion_agent_http.ts`（`createHttpNotionAgentBackend(ChatNotionAgentPlatform)→ChatBackend` 薄包；custom-api backend 复用既有 `createCustomApiBackend(httpPlatform)`）。**零 main 生产文件改动**（3b-5 不接 renderer，electron 路径零回归；3c 才接 transport）。
  - **关键决策（用户拍板）**：① 工作流按 handoff 纪律（非 Trellis）。② config 快照 = **可选第三参带远程默认**（3c renderer 预取 `await kosAvailable` 等传真实快照覆盖；kosConfigured 需真实值决定 `createBuiltinTools` 是否注册 9 KOS 工具）。
  - **code-reviewer（opus 独立 lane）= APPROVE**：codex GPT-5.5 用量超限+token 刷新失败（invalid_grant）→ 用户选 Claude code-reviewer 替代（非自审）。独立验证 typecheck:web exit 0 / 46 测试绿 / 零 Electron import；逐项确认 parity（persist 12 法 path/body·投影、llmFetch E_NO_LLM_KEY 边界+body 不双消费[读 custom_api !ok 仅 status]、notionAgentStream SSE 跨边界·abort·reader 释放、工具 8 读零投影别名/超集、config 默认对齐）+ debounce 无竞态无泄漏（fire 回调 re-fetch+`if(!e)return` 守卫 arm-vs-delete 竞态、flush-before-finalize、abort 清所有 pending 不过激[单活跃 stream+消费方 abort 守卫]）。findings：**0 CRIT/HIGH/MEDIUM·2 LOW·3 NIT 全有意 parity 决策/benign 风格**；采纳 NIT-1（parseSemanticEvent 加 cf 指向 custom_api.parseSseChunk 注释），其余设计有意不动。
  - **验收**：`pnpm run typecheck`（node+web 绿）+ `pnpm build:web`（shared/chat 零 Electron 泄漏 ✓ 8.44s）+ `vitest run http_platform.test.ts` **46 passed** + 全 chat 套（tests/main/chat + chat_db + chat_dispatcher + chat-attachments + http_platform）**15 文件 312 passed 零回归** + 残留检测仅注释命中。
  - **下一步 3c**：transport 接线（renderer 注入 HttpChatPlatform 跑 shared harness + getBackend 注入 createCustomApiBackend/createHttpNotionAgentBackend）+ 写工具 confirmation UI（同进程）+ 本地 token webRequest 注入 + CORS + **删 main chat 直跑路径**（含 D4 删 legacy 单遍）+ 本地/远程 e2e。

- **阶段 3 — 3c-0（✅ 详设拍板，3 fork A/A/A，设计 §6.6 冻结 + 矩阵 3c 行细化）** chat 对话 B-pure-unified cutover。
  - **ground truth 全摸排**：现有 IPC 路径（useEmailChat→mailApi.chat→ElectronApi IPC→handlers/chat.ts 模块级 dispatcher→webContents.send；confirmation 经 chat:confirmTool→resolveConfirmation）· 本地 token=`daemonRequest` 显式加 header（main 进程，`local_token.ts` 单例）**无 webRequest 拦截器** · 打包态 renderer=`file://`（Origin null）fetch loopback 8200 跨 origin、`ALLOWED_ORIGINS` 不含 null→CORS preflight 必失败 · `auth.py` 本地 token 腿（compare_digest）先于 CF JWT 就绪 · ai_chat.db schema owner=前端 `chat_db.ts`（CHAT_DB_VERSION=4，serve-api 绝不建表）· 3b-5 HttpChatPlatform 全四板就绪可直喂 dispatcher。
  - **3 fork 拍板（全采纳推荐 A）**：**D-3c-1** 提取 shared `ChatRuntime`（`shared/chat/runtime.ts`，封装 dispatcher+HttpChatPlatform+emitter sink+完整 ChatApi），electron+web 都走 serve-api（读写引擎单一路径零 parity，cutover 后 ElectronApi.chat 极简）· **D-3c-2** main `webRequest` 拦截 loopback 8200（onBeforeSendHeaders 注 token + onHeadersReceived 注 CORS 含 OPTIONS，对 serve-api+harness 透明）· **D-3c-3** 新建 `GET /api/chat/config`（serve-api 暴露 chat 运行配置快照，前端构造 HttpChatPlatform 前预取，D5 配置以 serve-api 为准）。
  - **ChatRuntime 接口**：构造 `(reads:MailApi, baseUrl)`；emitter+sink 构造期建（onStream 早于 start），dispatcher/platform/registry/backends lazy（await /chat/config）；跑方法→dispatcher、confirmTool→**同进程** resolveConfirmation、读→fetch baseUrl/chat/*（迁阶段 2 HttpApi 逻辑）、onStream→emitter.subscribe；web=HttpApi 构造 `this.chat=createChatRuntime({reads:this,baseUrl})`，electron=`{reads:new HttpApi(loopback),baseUrl:loopback}`；HttpApi.chat 改 lazy getter 破循环。
  - **cutover 枚举（3c-4）**：删 handlers/chat.ts chat:* IPC + dispatcher + web_contents_sink + ElectronChatApi + index.ts 后端注册 + ElectronChatPlatform（chat 唯一消费方 orphan）+ notion-agent main execa+gate+drainNotionAgentGate（serve-api asyncio spawn 已接管）；保留 chat_db migrate bootstrap（3c-4 main boot 显式 `getDb()` 建表）；D4 删 dispatcher legacy 单遍（notion-agent 走 harness empty-tools 首轮 end_turn 等价）。
  - **5 子步**：3c-1 webRequest+CORS+config 端点 → 3c-2 ChatRuntime+HttpApi.chat 切 → 3c-3 electron 切+confirmation+本地 e2e → 3c-4 删 main 直跑+D4+migrate → 3c-5 远程 e2e+文档+lint 收尾。
  - **下一步 3c-1**：main `chat_local_bridge.ts`（webRequest 注 token+CORS）+ serve-api `GET /api/chat/config` + HttpApi config 读方法（纯加法零回归）。

- **阶段 3 — 3c-1（✅ webRequest token+CORS + GET /api/chat/config，codex GPT-5.5 APPROVE[0 CRIT/HIGH；MEDIUM 归一化+LOW/NIT 注释→修→复审 APPROVE]，commit `1bb1699`）** chat cutover 第一子步（纯加法零回归：electron chat 仍走 IPC，renderer 未打 8200）。
  - **实现**：① `config.py` 加 6 chat-agent Field（agent_max_iter/cost/harness + kos consumer/l1/time_decay，env 名/默认逐一对齐 electron `chat/config.ts` getter；走 **pydantic env_file 读 .env** —— `serve_api()` 不 load_dotenv、os.environ 无 .env-only flag，故不用 os.environ.get，robust）② serve-api `GET /api/chat/config`（HttpPlatformConfig 8 字段快照，renderer 构造 HttpChatPlatform 前预取覆盖 DEFAULT_HTTP_CONFIG；归一化对齐 electron getter maxIter≥1/maxCostUsd>0/llm_model 空 fallback；`kosConfigured = kos_consumer_enabled` gate 9 KOS 工具，**非** OAuth `_kos_available`）③ `chat_local_bridge.ts`（新，main webRequest 拦截 loopback 8200：onBeforeSendHeaders 注本地 token[renderer 无 token，覆盖同名头防伪造，dev/打包都注] + onHeadersReceived 注 CORS[ACAO=null+credentials（credentials:include 禁 `*`）/ OPTIONS preflight 400→200（Starlette disallowed origin 返 400）/ 去重 access-control-*，**仅打包态**；dev renderer=5173 走 serve-api _DEV_CORS]）+ index.ts whenReady 注册 ④ 修 http_platform.ts kosConfigured 注释（契约对齐新语义）。
  - **codex review APPROVE（首轮）→ 修 3 finding → 复审 APPROVE**：MEDIUM（config 端点返 raw pydantic，electron getter 有归一化 → malformed env 过渡期漂移）→ 加 maxIter/maxCostUsd/llm_model 归一化 + `test_chat_config_normalizes_malformed_env`；LOW（http_platform kosConfigured 注释旧契约 kos-available）+ NIT（chat_local_bridge token 注释说不覆盖、实际覆盖=防伪造）→ 改注释。codex 确认 CORS/security sound（ACAO=null+credentials 对 file://、OPTIONS rewrite 因 Starlette preflight 先于 route auth、token scoped loopback、邮件 iframe 无 script 触达不了 null CORS）；bool 字段 pydantic 解析略宽于 readEnvBool（yes/on）记已知微差（cutover 后单源无漂移）。
  - **config 预取归属调整**：config 读由 3c-2 `ChatRuntime` 内 `request(baseUrl,'GET','/chat/config')` 做，**不**加 HttpApi.chat config 方法（避免改 ChatApi 接口；3c-1 只落后端端点 + main 桥）。
  - **验收**：ruff + pytest tests/api tests/cli **851** + test_chat **57**（config 默认 8 字段/kosConfigured==consumer parity/归一化/非默认透传）+ typecheck node+web + vitest chat_local_bridge **9** + http_platform **46** 零回归。改动 8 V2.1 文件，landing 在途隔离。
  - **下一步 3c-2**：`shared/chat/runtime.ts` `createChatRuntime`（dispatcher+HttpChatPlatform+emitter sink+完整 ChatApi+confirmTool 同进程 resolveConfirmation）+ HttpApi.chat 切（web 路径先通）+ mock-fetch 单测；HttpApi.chat 改 lazy getter 破循环。

- **阶段 3 — 3c-2（✅ ChatRuntime 组装 + HttpApi.chat lazy getter 破循环 + serve-api delete 端点，codex GPT-5.5 2 轮 APPROVE[初审 0 CRIT/HIGH·2 MEDIUM+1 LOW → 全修 → 复审新 LOW-2 → 修 → 终审 APPROVE no new findings]）** chat cutover 第二子步（web 路径先通；electron 切 + confirmation UI 留 3c-3）。
  - **实现**：新建 `shared/chat/emitter.ts`（`ChatStreamEmitter` 进程内 sink fan-out，快照遍历 + handler 隔离）+ `shared/chat/runtime.ts`（`createChatRuntime(deps): ChatApi` 组装 dispatcher + HttpChatPlatform + 进程内 emitter sink 成完整 ChatApi）。emitter+sink **构造期建**（`onStream(h)=emitter.subscribe(h)`，订阅早于 start；两个 `ChatStreamEnvelope` 结构等价直接订阅）；engine **lazy**（`ensureEngine`：promise 缓存幂等[并发首跑去重] + 失败清缓存重试 → `buildEngine`：await `GET /chat/config`[data 形状==HttpPlatformConfig 恒等覆盖] → 预取失败 graceful `DEFAULT_HTTP_CONFIG`+warn → `new HttpChatPlatform(reads,baseUrl,snapshot)` → `createToolRegistry()`+`createBuiltinTools(platform)` 逐个 register[kosConfigured gate 9 KOS 工具] → backends `{custom-api:createCustomApiBackend, notion-agent:createHttpNotionAgentBackend}` → `createChatDispatcher`）。方法映射：**写走 engine**（start/editMessage 前置 validateStartOpts/validateEditOpts → dispatcher.startChat/editChatMessage(map(opts),sink)，try/catch→normalizeDispatchError）；newSession→platform.persist.createNewSession；saveToKos→platform.saveToKos。**不触发 engine**：abort（非法 id guard + enginePromise null 则 return）/confirmTool（同进程 resolveConfirmation，空→E_INVALID_ARG/无 pending→E_NOT_PENDING/有 pending resolve）/deleteSession（fire-and-forget `DELETE /chat/sessions/{id}`，非法 id guard）/openPopout（no-op，Electron-only，3c-3 由 ElectronApi override）/读（直接 request + graceful 返 []/false）。`HttpApi.ts` chat 块（阶段 2 只读 stub 对象）→ **lazy getter** `get chat()` 返回 `createChatRuntime({reads:this, baseUrl})`，**破循环**（electron 注入的 `new HttpApi(loopback)` 只取 email/attachment 不碰 .chat）。**顺手补端点（用户拍板）**：serve-api `DELETE /api/chat/sessions/{id}`（`ChatDb.delete_session` verbatim 镜像 chat_db.ts deleteSession，`DELETE FROM ai_chat_sessions WHERE id=?`+FK CASCADE，envelope {deleted:True}）+ open_session/new_session backendKind enum 校验。
  - **codex review（gpt-5.5 high，read-only sandbox）= 2 轮 APPROVE**：初审 0 CRIT/HIGH·2 MEDIUM+1 LOW 全修——**MEDIUM-1** start/editMessage dispatcher reject 无 code → 加 normalizeDispatchError + return await 进 catch；**MEDIUM-2** serve-api open_session/new_session 未校验 backendKind enum → 两处加 `E_INVALID_ARG`；**LOW-1** abort 缺非法 id guard → Number.isInteger gate。复审确认修复 + **新 LOW-2**（normalizeDispatchError 未对齐 Electron 的 E_INVALID_ARG，dispatcher 内部 bad-args 抛无 code → 归 E_DISPATCH）→ 修：加 validateStartOpts/validateEditOpts 前置校验（复刻 handlers/chat.ts）。**终审 APPROVE，no new findings**。
  - **flaky 加固**：runtime.test 4 处 `vi.waitFor` 加 `{ timeout: 5000 }`（默认 1000ms 在并发 21 文件 + CPU 竞争下偶发超时，曾 1-failed 重跑未复现；根因=背景 runStream 完成等待超时）。
  - **验收**：`pytest tests/api tests/cli` **856 passed**（+4 chat：delete_session / delete_nonexistent / open_session·new_session invalid backendKind）+ ruff + typecheck node+web + `build:web` 零 Electron 泄漏（runtime/emitter import 全 shared）+ 残留检测 `grep "from 'electron'\|better-sqlite3\|execa" src/shared/chat/` 仅注释命中 + 全 chat 套 21 文件 **438 passed**（emitter 5 + runtime 25 新增）。
  - **下一步 3c-3**：electron 切 `ElectronApi.chat=createChatRuntime({reads:new HttpApi(loopback),baseUrl:loopback})`（openPopout override 回 `window:openChatPopout` IPC）+ ConfirmToolDialog 同进程接线（pending_confirmation→弹窗→confirmTool→resolveConfirmation）+ 本地 e2e。详见设计 §6.6.6。

- **阶段 3 — 3c-3（✅ electron 切 ChatRuntime + confirmation 同进程 + 端口注入，codex GPT-5.5 REQUEST CHANGES[1 HIGH+1 MEDIUM]→修 MEDIUM + HIGH 裁定非阻塞 backlog→复审 APPROVE）** chat cutover 第三子步（electron renderer 进程内跑 ChatRuntime；main `chat:*` IPC handler 暂留 dead code，3c-4 删）。
  - **实现**：① `ElectronApi.chat`（`shared/api/ElectronApi.ts`）从 `new ElectronChatApi()`（IPC `chat:*`→main dispatcher）切到 `createElectronChatRuntime()` = `createChatRuntime({reads:new HttpApi(loopback),baseUrl:loopback})` + spread override `openPopout`（shared runtime no-op → 回 main `window:openChatPopout` IPC）；删 `ElectronChatApi` class + `ChatStartEnvelope` type + 9 个 orphan chat type import（保留 `ChatApi`）。② **端口透传**：`loopbackChatBaseUrl()` 读 `window.location.search` 的 `apiPort` 构造 `http://127.0.0.1:<port>/api`，缺省/非法回退 8200（renderer 进程无 `process.env`）；main **三 load 入口**注入 `?apiPort=<resolveApiPort()>`——`index.ts` createWindow（`URLSearchParams`+可选 onboarding）+ createPopoutWindow + `handlers/onboarding.ts` reloadToMain（onboarding→主界面收敛点：complete/finalize/legacyMigrate/enterApp 全经它）；`backend_lifecycle.resolveApiPort` 改 export（**单一真源** = serve-api 实际端口 = chat_local_bridge filter 端口）。③ **confirmation UI 零代码改动**（transport 铁律）：cutover 后 harness 在 renderer 同进程跑，`pending_confirmation` 事件经 runtime emitter **同步 fan-out** 给 `useEmailChat.onStream` → `ConfirmToolDialog` → `confirmTool` → runtime 同进程 `resolveConfirmation`（链路各环节已被 runtime 25 测 + useEmailChat 测 + dispatcher 测覆盖）。
  - **codex review（gpt-5.5，read-only sandbox）= REQUEST CHANGES→修→APPROVE**：**MEDIUM**（已修）= apiPort 仅在 createWindow 注入，onboarding 完成的 reloadToMain 直 reload 不带 search → `MAILAGENT_API_PORT` 覆盖时从 onboarding 进入静默回退 8200 → 修：reloadToMain 也注入 apiPort（收敛点全覆盖，现三 load 入口齐）。**HIGH**（裁定非 3c-3 阻塞 backlog，codex 接受）= cutover 后 chat 恒依赖 loopback serve-api，`MAILAGENT_REMOTE_ACCESS_ENABLED=false` 关 serve-api → chat 失效；裁定依据：serve-api 在 **D1 已是 Electron 本地写面**（write_ops/draft/chat 写工具经 daemon_api 转发它，`=false` 在 D1 就让本地写挂，**非 3c-3 新增失败类别**）+ 设计 §8 不变式明确「3c cutover 后本地依赖 serve-api 在跑（C2 软门控+崩溃自拉起兜底）」+ 默认 serve-api 起、缺失时 chat **graceful 降级**（读返 []/写 throw E_DISPATCH→toast，不白屏 crash）；codex 点出的真正价值 = flag 命名 `MAILAGENT_REMOTE_ACCESS_ENABLED` 误导（名为远程访问、实为本地 daemon 总开关）→ **记 backlog**（3c-5 文档澄清 +「serve-api 不可达」状态提示可选改善）。
  - **验收**：typecheck node+web 绿 + `build:web` 零 Electron（ElectronApi 新增 import `createChatRuntime`/`HttpApi` 全 shared）+ chat 套 20 文件 **402 passed** 零回归 + 新 `tests/shared/electron_api_chat.test.ts` **7 passed**（端口注入 9300/缺省/非数字/<=0 回退 + openPopout IPC + guard + 完整 ChatApi 形状）+ `onboarding-ipc/detect` **36 passed** 零回归。**e2e 尽力**：现有 `.app`（main/v0.3.0）serve-api smoke 证明部署模型 live（`/api/health` 200 + `/api/chat/sessions/all` 端点存在+auth 生效；`/api/chat/config` 404 = 旧版无 3c-1 端点，符合预期）；本分支 serve-api dev 直跑需完整 `.env`（CLI 不 auto-load）+ 隔离库避迁移真实库风险，端点逻辑已 `pytest tests/api` 覆盖；**3c-3 renderer 真 e2e（custom-api 多轮/写工具确认/notion-agent + 打包态 file:// CORS 腿）留真机 dogfood**。
  - **下一步 3c-4**：删 main chat 直跑（`handlers/chat.ts` `chat:*` IPC + 模块级 dispatcher + `web_contents_sink` + `index.ts` 后端注册 + `electron_platform.ts` ElectronChatPlatform orphan + notion-agent main execa+gate+drain）+ **D4** 删 dispatcher legacy 单遍（notion-agent 走 harness empty-tools 等价）+ main boot 显式 `getDb()` migrate bootstrap（保 ai_chat.db schema，serve-api 绝不建表）+ 残留检测。详见设计 §6.6.5。

- **阶段 3 — 3c-4（✅ 删 main chat 直跑 + D4 删 legacy 单遍 + migrate bootstrap，codex GPT-5.5 read-only 1 MEDIUM[finalContent parity]→修→复审 APPROVE no findings）** chat cutover 第四子步（删除为主，staged +368/-3753）。
  - **删 7 生产文件**：`handlers/chat.ts`（12 chat:* IPC + 模块级 dispatcher + registry 填充 + abortAllChatSessions）/ `chat/web_contents_sink.ts` / `chat/electron_platform.ts`（ElectronChatPlatform orphan）/ `chat/registry.ts`（backend 注册移入 runtime.buildEngine）/ `chat/backends/notion_agent.ts`（execa NotionAgentBackend）/ `notion_agent_gate.ts`（serve-api asyncio spawn 接管）/ `chat/kos_save.ts`（Python kos_save 复刻接管）+ **删 4 测试**（electron_platform/notion_agent_backend/notion_agent_gate/kos_save，等价语义由 Python pytest 覆盖）。
  - **D4 删 dispatcher legacy 单遍 + harnessEnabled gate**：runStream 无条件 `runHarness`（删 backendSupportsTools gate）—— notion-agent（不 emit tool_use、http backend 剥离 tools）走 harness 首轮 collected 空 = 等价单遍。**harness 吸收 3 处 legacy notion-agent 专属语义**：① usage/done metadata 累积 + complete/error 终态持久化（thread_id 多轮续接）② tool_call event → role=tool 消息行（子进程工具展示）③ **单遍 finalContent**（complete `iter===1 ? lastFinalContent||buffer : buffer`，notion-agent finalContent 已 trim 尾换行；多轮 buffer 累积）。**语义变化**：删 harnessEnabled gate → harness-off 逃生阀消失（生产 MAILAGENT_AGENT_HARNESS 默认 ON，无实际影响）。
  - **index.ts boot 显式 `getChatDb()` bootstrap**（try/catch warn 不阻断；chat_db.ts 是 ai_chat.db schema owner，serve-api 绝不建表）+ 删 chat 后端注册/registerChatHandlers/before-quit abortAllChatSessions；**保留** `registerChatLocalBridge`（3c-1）。**注**：设计 §6.6.5 写的 `getDb()` 是笔误，实际入口 `getChatDb()`（getDb=sync_store.db）。
  - **`resolveNotionAgentBin` 搬 chat/backends/notion_agent.ts → notion_agent/config.ts**（删除暴露活跃非 chat 链：handlers/notion_agent → notion_agent/config.ts 用它做 agent 列表/binary 探测；env→which→pipx fallback + _binCache 保留）+ 测试迁 `tests/main/notion_agent/config.test.ts`（3 测试）。
  - **测试替身**（用户拍板「轻量替身」）：`tests/main/chat/_fixtures/test_chat_platform.ts`（ChatInfraPlatform & ChatToolPlatform：persist 转发真实 chat_db + resolveConfig 读 env + loadEmailContext null + 工具板 stub throw）+ `test_backend_registry.ts` → chat_dispatcher.test/harness.test 切替身（解耦 shared 回归与 electron 实现）。
  - **codex review = 1 MEDIUM → 修 → 复审 APPROVE no findings**：MEDIUM = harness complete 用 buffer 忽略 done.finalContent ≠ 被删 legacy（finalContent||buffer）—— notion-agent serve-api chunk 含 CLI 尾换行、finalContent 已 trim（`notion_agent.py:511`+pytest 断言），cutover 后多尾换行。修 = 单遍 `iter===1 ? lastFinalContent||buffer : buffer`（custom_api finalContent=accumulated=buffer 零影响，仅 notion-agent 恢复 trim parity）+ dispatcher.test 加 parity 回归。复审确认 (a) 解决 (b) 多轮 custom-api buffer 累积未破坏 (c) error/cost/max_iter 无需改 (d) 无新 finding。
  - **验收**：typecheck node+web + `build:web` 零 Electron + 全 chat 套 **16 文件 311 passed**（删 4 测试 402→311，+ 新 parity/config 测试）+ 残留检测核心干净（`grep runHarness|backend.stream src/electron/main/chat/` 空、`grep from 'electron'|better-sqlite3|execa src/shared/chat/` 仅注释）。**零 Python 改动**（serve-api 不变，pytest 跳过）。staged 隔离 landing 在途。
  - **留 3c-5 cleanup（连带 orphan，已标注）**：chat/config.ts 整体 cutover 后生产 orphan（仅替身用 4 getter）+ 内部 orphan getter（backendSupportsTools re-export/getNotionAgentMinIntervalMs/isKos*）+ kos/client+slug+sender_digest_cache（删 electron_platform/kos_save 后 orphan，有独立单测自洽，KOS 基础设施层）+ shared/chat 注释里过时 chat:* IPC 提及 + lint 全量。
  - **下一步 3c-5**：远程 e2e（mail.chenge.ink/app chat 全链路）+ 文档归档（`docs/claude/remote-chat-report-architecture.md` + CLAUDE.md 指针）+ **lint 全量收尾**（含上述连带 orphan）+ `MAILAGENT_REMOTE_ACCESS_ENABLED` flag 命名澄清（3c-3 backlog）。

- **阶段 3 — 3c-5a（✅ lint + orphan 清理，全仓 120 → 0 error，2 commit `0841ba9` + `2942abd`）** chat cutover 收尾第一步。
  - **cutover orphan 删除**（grep 确认无活跃消费方 + 连带删独立测试）：删 `kos/{client,sender_digest_cache,slug}.ts`（shared KOS 工具经 platform 注入不直 import；消费方 kos_save/electron_platform 已 3c-4 删）+ 连带删 3 测试（`rerank.test` 保留=测 shared `kos_rerank` 活跃）；删 `chat/config.ts`（dispatcher 下沉 shared 后唯一 importer=替身，替身 `test_chat_platform` 内联 readEnvBool/readEnvNumber 复刻 4 flag 默认值与边界）。
  - **eslint config**：`no-unused-vars` 加 args/vars/caughtErrors/destructured `IgnorePattern:'^_'`（业界约定，一处解决 22 个 chat 工具 `_ctx`/`_senderAddr` 有意 unused，不删签名参数）+ `docs/**` 归 ignore（一次性审计脚本，同 scripts/**）。
  - **全仓历史 lint 清零（用户拍板「全仓真正零 error」）**：删 stale 产物 `frontend/index.js`（untracked，electron-vite build 误落根目录，52 errors）。**react-hooks（用户拍板「逐个真重构」能安全的，深水区 disable）**：useMediaQuery/useReducedMotion→`useSyncExternalStore`、useNowTick→`useState` lazy init、DayView/useExitAnimation/EventFormModal 清错→render 期间 derived（React 官方「adjusting state during render」，lint 实测接受 render 期间 setState）；**深水区 11 个本质合理 effect**（模态预填/async load/Toast 离场状态机/localStorage/lightbox 拖拽）→ `eslint-disable`+注释标 React Compiler 迁移债（用户接受 push back：真重构需 key 重置/react-query/状态机重写，复杂度 > 收益）。设计系统：coral-flood `bg-coral`→`/100`（零视觉变化）、raw-hex 调色板/swatch disable、cjk-mono `text-meta`→`text-aux`。
  - **验收**：typecheck node+web 0 + 全量 `pnpm lint` **0 error**（120→0）+ chat 套 16 文件 325 passed + tests/main/kos（rerank）+ `build:web` 零 Electron。**react-hooks 真重构行为验证 piggyback 3c-5c 真机 e2e**（避免起 app 逐个验 15 组件）。commit 隔离 landing 在途。

- **阶段 3 — 3c-5b（✅ 文档归档）** chat cutover 收尾第二步。
  - 新建 `docs/claude/remote-chat-report-architecture.md`（B-pure-unified 全貌：三层[引擎 `shared/chat` + 传输 `HttpChatPlatform` + 后端 serve-api] + cutover 后数据流[流式 token 进程内 emitter 直驱 React 无 IPC] + 本地 token webRequest / 远程 CF cookie 鉴权 + `ChatRuntime` 组装 + 两后端不对称 + D1-D5/D-3c 决策 + 不变式[含 **D4 语义变化** harness-off 逃生阀消失 + **flag 命名澄清** `MAILAGENT_REMOTE_ACCESS_ENABLED` 实为本地总开关] + 关键文件索引 + 阶段 1/2 摘要）。
  - CLAUDE.md 文档地图加 V2.1 行指针；`backend_lifecycle.ts:serveApiEnabled` 注释更新（cutover 后整个 chat 引擎经 serve-api + flag 命名澄清 + 指向 architecture 文档 §8）。
  - 本看板：3c-5 行拆 a/b/c + 验收 gate（lint ✅/文档 ✅）+ 本进度日志。
  - **下一步 3c-5c**：远程 + 本地真机 e2e（build feature `.app` + 部署 feature serve-api → `mail.chenge.ink/app` + electron renderer 跑 chat 全链路 + react-hooks 重构行为验证）。build 三坑：ABI `rebuild:electron`（本 session 跑过 vitest=Node ABI 必切回）/ `build:web` 含 SPA / `resources/python` 在。现有装机 `.app`=v0.3.0（无 3c chat，`/chat/config` 404）必须重 build。

- **阶段 3 — 3c-5c（🟡 进行中：serve-api 层 e2e ✅ / renderer 真 e2e 待 Accessibility 重启后 computer-use 重试）** chat cutover 收尾第三步（真机 e2e）。
  - **feature `.app` 已 build + 装机** `/Applications/MailAgent.app`（含 3c cutover renderer + 重 provision venv）：序列 `build:web → rebuild:electron → run build → electron-builder --dir --arm64`，验证 codesign OK + `Resources/web` SPA + python3.11 + better-sqlite3 Electron ABI。装机=退旧 v0.3.0 app + `ditto` → open（serve-api 14-16s 自起）。
  - **🔴 venv 旧 provision 坑（已修，重要教训）**：首次 build 用的 `resources/python` venv 是 **3c-1 前 provision**（有 chat.py/db.py 但缺 `/chat/config` 等 3b/3c 端点）→ feature app serve-api `/chat/config` **404**。根因 = feature 分支 3b/3c 改了大量 serve-api Python（chat 端点/notion_agent.py/ChatDb），但 venv 未在该改动后重 provision（一开始误判「3c-5 纯前端跳过 provision」漏了**分支整体** Python 变更）。修 = `bash frontend/scripts/build-python-venv.sh arm64`（含最新 src，命中 CPython 缓存 ~分钟）→ 重 `electron-builder` → 重装。**印证 CLAUDE.md「分支大改 src/ 后 build 前必 build-python-venv.sh」铁律 —— 判据是「分支相对上次 provision 是否改过 Python」，非「本子步是否改 Python」。**
  - **serve-api 层 e2e ✅**（feature 部署成功，独立验证）：`/api/chat/config` 404→（重 provision 后）401（无 token，端点 live）→ 带本地 token **200 + 完整 8 字段快照**（harnessEnabled true / maxIter 8 / defaultModel claude-opus-4-8 / kosConfigured false）= 端点功能 + auth 本地腿（`X-MailAgent-Local-Token` compare_digest）全 work；远程 `mail.chenge.ink/app` **302 CF Access** = tunnel + cloudflared live。
  - **renderer 真 e2e 受阻 → 重启重试**：computer-use 驱动本地 electron 所有 click 报「程序坞」（osascript 确认 frontmost=MailAgent + `-1719 不允许辅助访问`）= **Claude Code 缺 Accessibility 权限**（screenshot 走屏幕录制权限能用；click 走 Accessibility 不能）。chrome MCP 远程无连接（`list_connected_browsers` []）。用户授权 Accessibility 后 click 仍失败（macOS TCC 权限进程启动时检查）→ **用户拍板重启 Claude Code 让 Accessibility 生效后重试**。
  - **▶ 重启后续（新 session 说「继续 3c-5c renderer e2e」）**：① `open ink.chenge.mailagent`（bundle id，display name "MailAgent" Launch Services 没匹配）+ screenshot ② computer-use 点邮件详情右上角 ✦ chat 入口（~1245,82）开 chat 面板 ③ 跑 custom-api 多轮 + 读工具 + **写工具确认弹窗（同进程 resolveConfirmation）** / notion-agent 多轮+thread / KOS / abort/编辑/新建。若 click 仍报 Dock（Retina XDR bug 而非权限）→ 转 chrome MCP 远程（连 Claude in Chrome 扩展 + CF 登录 mail.chenge.ink/app）。**e2e 通过 = 阶段 3 出口**。
  - **▶ 最终决定（用户拍板）= renderer e2e 转用户自验**：computer-use Accessibility 授权后 click 仍失败（疑 Retina XDR hit-test bug），用户决定自己手动验证 renderer chat 全链路。Claude 软件部分收尾完成（3c-5a lint + 3c-5b 文档 + 3c-5c serve-api 层 e2e + feature `.app` build/装机），renderer e2e 清单 = [`docs/v2.1-stage3-3c-5c-e2e-checklist.md`](./v2.1-stage3-3c-5c-e2e-checklist.md)（custom-api/notion-agent/写工具确认/远程 parity + cutover 重点 + react-hooks 回归 + 出口判据 + 回退步骤）。用户验证通过 = 阶段 3 出口 → feature 分支落 main + tag。
