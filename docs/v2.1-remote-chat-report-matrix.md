# V2.1 远程 chat + report/agent — 能力矩阵 & 验收看板

> 配套设计：`~/.claude/plans/twinkling-floating-puppy.md`（方案 B-pure-unified）。
> 本文件是**活的执行/验收看板**，每阶段末更新。把「覆盖完整性」变成可机械核对的表，
> 专治系统改造里「哪个端点漏改 / 漏对接 / 漏测」。分支：`feat/v2.1-remote-chat-report`。
>
> 工程纪律（仿 backend service layer）：每阶段 **实现 → codex GPT-5.5 high review → 修 → 验收（pytest/build:web/curl）→ 矩阵标绿 + 进度日志**。

## 为什么需要这张表

V2.1 的风险 = 「某个能力只补了一半的层（serve-api 端点有、客户端没接 / 形状漂移 / 没测）」。靠人脑记必漏。
五招防漏：① 能力矩阵看板（本表）② schema 契约对齐 IPC 形状 ③ 删除驱动（删 main chat 直跑，编译器枚举调用点）④ 残留检测 ⑤ 每阶段 codex review + 验收 gate。

图例：✅ 完成 · 🟡 doing · ⬜ 待办 · ➖ 不适用

## 架构（B-pure-unified，零 parity）

harness 逻辑 `shared/chat/`（一份，renderer + browser 都跑）→ `httpPlatform`（一份）→ serve-api（一份后端，无 harness 逻辑）。本地 token 由 main `webRequest` 透明注入，对 harness 透明。详见 [plan](file://~/.claude/plans/twinkling-floating-puppy.md) + 最终架构文档 `docs/claude/remote-chat-report-architecture.md`（阶段 3 末产出）。

---

## 阶段 1 — Part A report/agent + 修 agents 慢 ✅ 完成（codex gpt-5.5 APPROVE）

| 能力 | serve-api 端点 | HttpApi 客户端 | 本地接线 | 单测/契约 | parity(==IPC 形状) |
|---|---|---|---|---|---|
| report list | ✅ `GET /api/reports` | ✅ | ➖ 本地直读保留 | ✅ | ✅ wire 单一真源 |
| report get | ✅ `GET /api/reports/{id}` | ✅ | ➖ 本地直读保留 | ✅ | ✅ |
| report getConfig（**修慢**） | ✅ `GET /api/report-agents` | ✅ | ✅ 本地走 serve-api（兜底直读） | ✅ | ✅ |
| report setConfig | ✅ `PUT /api/report-agents/{id}` | ✅ | ➖ 本地 fork CLI（经 wire 单一真源） | ✅ | ✅ |
| report runNow | ✅ `POST /api/report-agents/{id}/run` | ✅ | ➖ 本地 fork CLI（经 wire） | ✅ | ✅ |
| report delete | ✅ `DELETE /api/reports/{id}` | ✅ | ➖ 本地 fork CLI | ✅ | ✅ |

**横切（阶段 1）**：
- `src/reports/wire.py` 下沉（resolve_agent/parse_counts/report_to_list_item/report_to_detail/config_patch_to_db，CLI+serve-api 单一真源，仿 D2a wire.py）— ✅
- CLI `report.py` 重构调 wire（消手抄，行为零回归 557 passed）— ✅
- `deps.py` 加 `get_report_store()`（lru_cache 单例，对齐 get_job_repo）— ✅
- agents 慢根治（本地 getConfig 走 serve-api in-process resolve，~ms 取代 fork ~759ms；不可达兜底直读不 fork）— ✅

## 阶段 2 — Part B chat 只读历史 ✅ 完成（code-reviewer opus APPROVE WITH NITS）

| 能力 | serve-api 端点 | HttpApi 客户端 | 单测/契约 | parity |
|---|---|---|---|---|
| chat listSessions | ✅ `GET /api/chat/sessions?emailId=` | ✅ | ✅ | ✅ SQL verbatim |
| chat listAllSessions | ✅ `GET /api/chat/sessions/all`（join email_metadata）| ✅ | ✅ | ✅ |
| chat listMessages | ✅ `GET /api/chat/sessions/{id}/messages` | ✅ | ✅ | ✅ |
| chat listToolCalls | ✅ `GET /api/chat/messages/{id}/tool-calls` | ✅ | ✅ | ✅ |
| chat kosAvailable | ✅ `GET /api/chat/kos-available` | ✅ | ✅ | ✅ |
| ai_chat.db 连接（`src/chat/db.py`，`DATA_ROOT/frontend/ai_chat.db`，graceful 库不存在→[]）| ✅ | ➖ | ✅ | ➖ |

**本地 chat 读**：阶段 2 保留直读 ai_chat.db（better-sqlite3 快，对齐 report 读 replica 不变式 4）；阶段 3 整体 B-pure-unified 时本地统一走 serve-api。验收：`pytest tests/api` **237 passed**（含 chat 11 测试）+ ruff + 前端 typecheck/eslint 全绿。

## 阶段 3 — Part B chat 对话（B-pure-unified 核心）🟡 3a steps 1-4 完成（类型/tools 下沉 + 分层接口 + harness 下沉，312 绿；step 5-6 待 fresh session）（详设 → [`docs/v2.1-stage3-chat-platform-design.md`](./v2.1-stage3-chat-platform-design.md)）

> **3a 设计已落地**（ChatPlatform seam + 6 步抽取 + D1-D5 决策）。依赖面实测**仅 2 处硬 Electron/Node import**（dispatcher 的 WebContents type-only + notion_agent 的 execa/fs/os/path）→ 下沉从「大改」降级为「定义 ChatPlatform + 搬逻辑 + ElectronChatPlatform 包既有实现（零回归）」。
> 关键决策：**D1** 持久化 cadence 由 platform 控（streamContent debounce / finalizeMessage flush，守 harness 单一真源）· **D2** custom-api 解析留 shared / notion-agent 复刻 Python（按后端性质分，各单实现）· **D3** 3a 保 main 执行（零回归）3c 才搬 renderer · **D4** notion-agent 统一进 harness 删 legacy 单遍 · **D5** 本地也走 serve-api。

| 能力 | serve-api | shared/httpPlatform | 本地接线 | 单测 | parity |
|---|---|---|---|---|---|
| 3a ChatPlatform 分层接口（platform.ts，step3 ✅）| ➖ | ✅ | ✅ ElectronChatPlatform | ✅ typecheck | ➖ |
| 3a harness 下沉 shared（零 Electron，step4 ✅）| ➖ | ✅ | 🟡 dispatcher step5 | ✅ build:web | ✅ 312 |
| 3a dispatcher 下沉 + handlers 接线（step5）| ➖ | ⬜ | ⬜ | ⬜ | ⬜ |
| 3b llm-proxy（透传 LLM，注入 key） | `POST /api/chat/llm-proxy` | ⬜ | ➖ | ⬜ | ➖ |
| 3b notion-agent spawn 透传 | `POST /api/chat/notion-agent` | ⬜ | ➖ | ⬜ | ⬜ |
| 3b chat 持久化端点 | `POST/PATCH /api/chat/*` | ⬜ | ➖ | ⬜ | ⬜ |
| 3b KOS 读写代理 | `POST /api/chat/kos-*` | ⬜ | ➖ | ⬜ | ➖ |
| 3b 写工具端点（flag/archive/draft/kos_put） | `/api/email/*`(D1有)+kos | ⬜ | ➖ | ⬜ | ⬜ |
| 3c httpPlatform 实现 | ➖ | ⬜ | ⬜ | ⬜ | ⬜ |
| 3c 本地 token 注入（webRequest）+ CORS | serve-api CORS | ➖ | ⬜ main | ⬜ | ➖ |
| 3c api.chat transport（ElectronApi/HttpApi） | ➖ | ⬜ | ⬜ | ⬜ | ⬜ |
| 3c 写工具 confirmation UI（同进程） | ➖ | ⬜ | ⬜ | ⬜ | ➖ |
| 3c 删 main chat 直跑路径 | ➖ | ➖ | ⬜ | ⬜ build | ➖ |

---

## 残留检测（每阶段末跑）

```bash
# 阶段 1: serve-api reports 端点 in-process（不 fork CLI）
grep -c "run_cli\|cli_runner" src/api/routers/reports.py   # = 0
# resolve 单一真源: CLI + serve-api 都调 src/reports/wire.py，不各自手抄
grep -rn "get_default_prompt" src/cli/commands/report.py src/api/routers/reports.py  # 0（都经 wire）
grep -c "def resolve_agent\|def config_patch_to_db" src/reports/wire.py  # ≥2（单一真源）
# 本地 getConfig 不再 fork CLI 取默认 prompt
grep -c "loadDefaultPrompts\|report.*config-get" frontend/src/electron/main/handlers/report.ts  # 0（改走 serve-api）

# 阶段 3: shared/chat 零 Electron 依赖（方案成败根本）
grep -rn "from 'electron'\|better-sqlite3\|execa\|handlers/" frontend/src/shared/chat/  # 空
# 删 main chat 直跑
grep -rn "runHarness\|backend.stream" frontend/src/electron/main/chat/  # 阶段 3c 后应空（迁 shared）
```

## 验收 gate

- [ ] 阶段 1：能力矩阵 report 6 端点全绿（serve-api+HttpApi+本地+单测+parity）；`pytest tests/api tests/cli` 全绿；本地 /agents **秒开**实测（fork→in-process）；codex review APPROVE
- [ ] 阶段 2：chat 读 5 端点全绿；远程能看历史；codex review APPROVE
- [ ] 阶段 3：custom-api（读+写工具）+ notion-agent 远程多轮对话；`pnpm test`+`pnpm build:web`（无 Electron 泄漏）；删 main chat 直跑；本地 chat 走 serve-api 行为对齐；codex review APPROVE
- [ ] 真机 e2e：feature 分支 build .app → 装机 → 远程跑全链路（对照本地 parity）
- [ ] 文档：本看板归档 + `docs/claude/remote-chat-report-architecture.md` + CLAUDE.md 文档地图指针
- [ ] **lint 全量修复（用户要求，最后统一做）**：chat 模块预存 lint 债（`kos.ts` `_ctx` unused ×3 + `confirmation.ts`/`dispatch.ts` prettier）+ 全仓 `pnpm lint` 清零。放 stage 3 收尾，避免和下沉 refactor 混在一起污染 diff。

## 进度日志

- **阶段 1（✅ 完成，codex gpt-5.5 APPROVE）** `feat/v2.1-remote-chat-report`：report/agent serve-api 端点 + 修 agents 慢。
  - **后端**：新建 `src/reports/wire.py`（resolve_agent/parse_counts/report_to_list_item/report_to_detail/config_patch_to_db — CLI+serve-api 读形状/patch 单一真源，仿 D2a）；CLI `report.py` 重构调 wire（删本地手抄）；新建 `src/api/routers/reports.py`（6 端点 in-process ReportStore 不 fork；runNow 走 `asyncio.to_thread(asyncio.run, run_report_once)` 独立 loop 对齐 CLI）；`deps.py` 加 `get_report_store()`；`app.py` 注册 + 全局 `RequestValidationError` handler（422→envelope）。
  - **前端**：`HttpApi.ReportApi` 去 stub 接 6 端点（读 catch 返 []/null 守契约，写 req() 解包失败 throw）；本地 `report.ts` getConfig 改走 serve-api（in-process resolve 含默认 prompt ~ms 取代 fork CLI config-get ~759ms = agents 慢根因）+ 删 `loadDefaultPrompts`，不可达兜底直读。
  - **codex review（gpt-5.5 high）**：首轮 REQUEST CHANGES 2 MEDIUM —— ① `wire.resolve_agent` 坏数据（JSON array schedule → `.get()` 崩 + trigger_mode 未 clamp）② `body: Optional[dict]` malformed → FastAPI 默认 422 非 envelope。两个均修（schedule 强制 dict+默认 / trigger_mode clamp 对齐 TS / 全局 RequestValidationError handler 422→E_INVALID_ARG envelope）+ 加 4 测试。**re-review APPROVE，Findings none**。
  - **验收**：`pytest tests/cli tests/api` **737 passed**（含 19 reports 端点测试 + wire 边界 + 422 envelope）；`tests/reports` 全过；ruff + 前端 typecheck/eslint 全绿。
  - **隔离**：staged 仅 9 个 V2.1 文件，用户在途 LLM client.py 工作（CRS UA/streaming）严格排除。
  - **附带修复**：`~/.codex/config.toml` 的 `service_tier="default"`（codex 只认 fast/flex，否则所有调用 json decode error）已注释。

- **阶段 2（✅ 完成，code-reviewer opus APPROVE WITH NITS）** chat 只读历史。
  - **后端**：新建 `src/chat/db.py`（ChatDb 只读 ai_chat.db，读函数 SQL **verbatim 镜像** chat_db.ts，graceful 库不存在→[] 不建空库）+ `src/api/routers/chat.py`（5 读端点：listSessions/listAllSessions/listMessages/listToolCalls/kosAvailable；listAllSessions join sync_store.db email_metadata best-effort）+ `deps.py` 加 `get_chat_db()` + `app.py` 注册。ai_chat.db 路径 = `DATA_ROOT/frontend/ai_chat.db`（serve-api 注入 MAILAGENT_DATA_ROOT，对齐 chat_db.ts resolveChatDbPath）。
  - **前端**：`HttpApi` chat 读方法去 stub（catch 返 []/false 守契约）；写/流方法留阶段 3。本地 chat 读保留直读 replica（不变式 4），阶段 3 整体走 serve-api。
  - **review（gpt-5.5 high 初轮）**：REQUEST CHANGES 2 finding —— ① `_email_meta_for_sessions` 无 exists 保护（sync_store.db 不存在会建空库）② `sender_name or sender` ≠ chat.ts `??`（空字符串边缘）。两个均修（os.path.exists 短路 + email_ids 空不调 get_settings / `sender_name is not None`）+ 加 2 单元测。**codex 用量限额无法 re-review → code-reviewer subagent（opus）替代确认：APPROVE WITH NITS，2 finding 修复正确完整、SQL parity verbatim、0 CRITICAL/HIGH**（4 nits cosmetic/cohesion 非阻塞）。
  - **验收**：`pytest tests/api` **239 passed**（含 chat 13 测试）+ ruff + 前端 typecheck/eslint 全绿。

- **阶段 3 — 3a 设计 + steps 1-2b（✅ 类型+tools 下沉，codex gpt-5.5 APPROVE WITH NITS）** chat 对话 B-pure-unified。
  - **理解**：精确测绘 chat 模块 5536 行（types/harness/dispatcher/config/registry/dispatch/confirmation/tool-registry/handlers-chat + Explore 摸 backends/custom_api·notion_agent·kos_save）。依赖面实测**仅 2 处硬 Electron/Node import**（dispatcher WebContents type-only + notion_agent execa/fs/os/path）；其余跨模块依赖（持久化/邮件上下文/LLM fetch/工具→handlers/KOS/sender digest/config）皆注入式或可抽象。代码本就良好分层（sink 注入、confirmation module map、tools registry、`surface` 字段为下沉预留）→ 下沉从「大改」降级为「定义 seam + 搬逻辑 + ElectronChatPlatform 零回归」。
  - **设计**：`docs/v2.1-stage3-chat-platform-design.md`（ChatPlatform 接口 + 3a 6 步抽取 + 3b/3c 概要 + 矩阵行 + D1-D5 决策 + 风险）。核心难题=持久化 cadence（harness 每 chunk 同步 `updateMessage`，直接 HTTP 化会几百 HTTP/响应）→ D1 解：拆 `streamContent`(debounced)/`finalizeMessage`(flush)，cadence 进 platform 层守 harness 单一真源。
  - **附带修复**：`HttpApi.notImplemented` 同步 throw → reject（远程「AI 未配置→前往设置」降级 toast 不再 ErrorBoundary 崩，commit `c1fbe7b`）。真机验证发现：远程「未配置」空状态=预期阶段 3 边界，硬崩溃=已修 bug。
  - **step 1（`787470e`）**：chat_db.ts 数据模型类型（BackendKind/ChatMessage/ChatSession/ConfirmationTier/ChatToolCall/输入 patch 等 14 个）→ `shared/chat/model.ts`（零 import 纯类型），chat_db.ts import+re-export 保 6 importer 路径不变。
  - **step 2a（`fa06eb2`）**：git mv `chat/types.ts` → `shared/chat/types.ts`（`from '../chat_db'`→`./model`），6 源 importer 改指 `@shared/chat/types`。
  - **step 2b（`e3e8004`）**：git mv `tools/{registry,confirmation,dispatch}.ts` → `shared/chat/tools/`，11 源 importer（含 builtin×5 留 main）+ 7 测试改指；纯搬不 dedup ConfirmationTier。纯 tools 测试 42 passed。
  - **codex review（gpt-5.5 high）**：**REQUEST CHANGES** 1 HIGH（step 2a 漏改 tests/ 里 5 个 import `chat/types` 的测试 — typecheck 只覆盖 tsconfig.node/web 不含 tests、纯 build gate 抓不到 test-time 断裂）+ 1 LOW（ConfirmationTier 与 model.ts 重复，认同保纯度暂不 dedup）。HIGH 修（`a38d212`，5 测试 import 改指 `src/shared/chat/types`）→ 验收 `rebuild:node` + **全 chat 测试套 15 文件 312 passed**（零回归基线达成）→ **re-review APPROVE WITH NITS**（仅 LOW 留 cleanup）。
  - **教训**：移文件的 importer grep 必须含 `tests/`（typecheck 只覆盖 tsconfig.node/web，纯类型/逻辑搬迁靠 typecheck+build:web 验时，test-time import 断裂不暴露 → 必跑 `rebuild:node`+vitest 或全量 grep tests）。
  - **附带修复**：`HttpApi.notImplemented` 同步 throw → reject（远程「AI 未配置→前往设置」降级 toast 不再崩，`c1fbe7b`）。
  - **下一步 → steps 3-4 已完成（见下条目）**。

- **阶段 3 — 3a steps 3-4（✅ 分层接口 + harness 下沉，全 chat 套 312 绿）** chat 对话 B-pure-unified。
  - **step 3（`5743fe3`）**：`shared/chat/platform.ts` **分层接线板**（用户拍板）。`ChatInfraPlatform`（基础设施板 3a 定形）= persist（`ChatPersistPort` 12 法，updateMessage 拆 streamContent fire-and-forget / finalizeMessage await，D1）+ loadEmailContext + resolveConfig（env 快照）+ prefetchSenderDigest；模型板/工具板仅占位注释（3b 按真实形状定形，不写无消费方假线）。`backendSupportsTools` 下沉 model.ts（config.ts re-export）。设计文档 §3 单接口（9 原语）→ 分层（§3.1-3.5 + 演进记录）。grep 实测 harness+dispatcher 外部调用只落这 4 类，另 5 原语消费方 3a 留 main 不调 platform。
  - **step 4（`d737a09`）**：git mv harness → `shared/chat/harness.ts` 全路由 `args.platform`（streamContent 守同步写零回归 / 终态·工具·abort await）。新建 `electron_platform.ts` **ElectronChatPlatform**（persist 转发 chat_db / loadEmailContext 移入单一真源 / resolveConfig 读 config / prefetch 转发 kos，导出单例）。dispatcher 接线（runHarness 传 platform + loadEmailContext 改 platform；**主体 startChat/legacy/abort 仍直调 chat_db，step5 再 platform 化**）。harness.test 注入 electronChatPlatform。typecheck+build:web（shared/chat 零 Electron）+ 全 chat 套 15 文件 **312 passed**（零回归基线）。
  - **教训 tickAsync**：platform seam 后 chat:start→事件 drain 异步链变深，chat_dispatcher.test「sink 顺序」`tickAsync(6)` 固定 microtask burst 不够（done 未达）→ 改轮询 done 落地（仿测试文件 2026-05-26 happy-path 既定模式），不耦合 microtask-chain 深度。功能零回归。
  - **下一步 step 5-6（fresh context，「多文件重连宜 fresh」+ dispatcher 半截即红）**：dispatcher 主体下沉（建议 `createChatDispatcher({platform,getBackend})` factory，legacy 单遍全 platform 化，`drainNotionAgentGate` 留 main wrapper，`makeWebContentsSink`/WebContents 留 main）+ handlers/chat 构造 dispatcher + chat_dispatcher.test（767 行 23+ test）driver 改 factory + 312 绿 → **3a 出口 codex review**。详见 [`v2.1-stage3-step3-handoff.md`](./v2.1-stage3-step3-handoff.md) §2。
