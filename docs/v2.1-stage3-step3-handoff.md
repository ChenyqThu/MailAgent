# V2.1 阶段 3 — step 5-6 启动 handoff（新 session 一打开照此干）

> 配套：[`v2.1-stage3-chat-platform-design.md`](./v2.1-stage3-chat-platform-design.md)（§3 分层接线板接口）+ memory `project_v2_1_remote_chat_report.md` + [`v2.1-remote-chat-report-matrix.md`](./v2.1-remote-chat-report-matrix.md)（看板）。
> 分支 `feat/v2.1-remote-chat-report`。本文件 = step 5-6 的「立即可执行」交接。
> **steps 1-4 已完成（commit 在案 + codex GPT-5.5 xhigh review APPROVE WITH NITS、2 MEDIUM 已修，全 chat 套 15 文件 313 绿）；本文件聚焦剩余 step 5-6。**

## 0. 现在在哪（steps 1-4 ✅）

`shared/chat/` 已就位、零 Electron import、全 chat 套 313 passed：
- `model.ts` — chat 数据模型类型 + `backendSupportsTools`（step3 下沉，纯 BackendKind→bool 能力映射）。
- `types.ts` — stream events / EmailContext / ChatBackend / ChatStreamRequest / Anthropic*。
- `tools/{registry,confirmation,dispatch}.ts` — 工具子系统（纯逻辑）。
- `platform.ts`（step3 `5743fe3`）— **分层接线板**：`ChatInfraPlatform`（基础设施板，3a 定形）= `persist`（`ChatPersistPort` 12 法）+ `loadEmailContext` + `resolveConfig`（`ChatRuntimeConfig`）+ `prefetchSenderDigest`；模型板 `ChatModelPlatform` / 工具板 `ChatToolPlatform` **仅占位注释**（3b 定形）。
- `harness.ts`（step4 `d737a09` + review fixup `28e0a3a`）— 已从 main 搬来，外部调用全走 `args.platform`（ChatInfraPlatform）；纯 helper 逐字保留。**codex review 修**：while 顶部 abort 正确标 aborted（非 E_MAX_ITER 误报）+ 所有终态 patch 带 `content:buffer`（http 不丢 partial）。step 5 搬 dispatcher 的 legacy 单遍同理处理 abort/终态。

main 侧：
- `chat/electron_platform.ts`（step4）— **ElectronChatPlatform** 实现 ChatInfraPlatform（persist 转发 chat_db / loadEmailContext 从 dispatcher 移入单一真源 / resolveConfig 读 config / prefetch 转发 kos），导出单例 `electronChatPlatform`。
- `chat/dispatcher.ts` — **仍在 main**，已部分接线（runHarness import `@shared/chat/harness` + 传 platform；loadEmailContext 改 `await electronChatPlatform.loadEmailContext`）。**但主体 startChat / editChatMessage / runStream(legacy 单遍) / abort* 仍直调 chat_db** —— step 5 就是把它搬 shared + 全 platform 化。
- `handlers/chat.ts` — 仍 import dispatcher 的 module functions（startChat/editChatMessage/abortChatSession/makeWebContentsSink + re-export abortAllChatSessions）。step 5 改用 factory。

## 1. 🔴 三个必记的坑

1. **ABI**：本/上 session 跑过 `pnpm rebuild:node`（better-sqlite3 = Node ABI 供 vitest）。**下次 build .app 前必 `pnpm rebuild:electron`**（CLAUDE.md 打包坑②）。step 5-6 不 build .app，但记着。
2. **不变式 1**：`shared/chat/` 零 Electron/Node-only import。每步 `pnpm build:web` 验。dispatcher 搬 shared 后，对 `../chat_db` / `../db` / `./config`(env) / `electron`(WebContents) / `./registry` / `./electron_platform` / `./backends/notion_agent_gate` 的直接 import **必须全消除**（改注入 / 留 main wrapper）。
3. **移文件 importer grep 必含 `tests/`**（typecheck 只覆盖 tsconfig.node/web 不含 tests，纯 build gate 抓不到 test-time import 断裂）。搬 dispatcher 后 `grep -rn "chat/dispatcher'" src tests` + 跑全 chat 套兜底。

## 2. step 5-6 计划

### step 5 — 搬 dispatcher 主体 → `shared/chat/dispatcher.ts` + handlers 接线

**核心难点：dispatcher 搬 shared 后不能 import `electron_platform`（main）/ `registry`（main）/ `WebContents`（electron）/ `notion_agent_gate`（main）。** 建议 **factory 注入**（_inflight 进 closure，3c renderer 可注入 HttpChatPlatform）：

```typescript
// shared/chat/dispatcher.ts
export interface ChatDispatcherDeps {
  platform: ChatInfraPlatform
  getBackend: (kind: BackendKind) => ChatBackend
}
export interface ChatDispatcher {
  startChat(input: StartChatInput, sink: StreamSink): Promise<StartChatResult>
  editChatMessage(input: EditChatInput, sink: StreamSink): Promise<StartChatResult>
  abortChatSession(sessionId: number): Promise<number>  // abortStreamingMessages 现 async
  abortAllChatSessions(): void   // 只 abort+clear _inflight（drain 由 main wrapper 补）
  __resetForTests(): void        // 清 _inflight（替代旧 __resetChatDispatcher）
}
export function createChatDispatcher(deps: ChatDispatcherDeps): ChatDispatcher {
  const _inflight = new Map<number, AbortController>()
  /* startChat / editChatMessage / runStream / abortChatSession / abortAllChatSessions */
}
```

**dispatcher 外部调用 → 改法**（逐一）：
| 当前 | 改成 |
|---|---|
| `../chat_db`: appendMessage / getOrCreateSession / getSession / getMessage / listLastNMessages / deleteMessagesFromId / updateMessage / abortStreamingMessages | `await deps.platform.persist.*`（**legacy 单遍**：chunk `updateMessage({content})`→`streamContent` / done·error `updateMessage`→`finalizeMessage` / tool_call `appendMessage`→`appendMessage`）|
| `./config`: isHarnessEnabled | `(await deps.platform.resolveConfig()).harnessEnabled` |
| backendSupportsTools | `import { backendSupportsTools } from './model'`（已 shared，纯函数）|
| `./registry`: getChatBackend | `deps.getBackend(kind)` |
| `electron`: WebContents + `makeWebContentsSink` | **留 main**（见下）|
| `./backends/notion_agent_gate`: drainNotionAgentGate | **留 main**（abortAll 拆，见下）|
| `./electron_platform`: electronChatPlatform | **删**（loadEmailContext 改 `await deps.platform.loadEmailContext`）|
| `@shared/chat/harness`: runHarness | 不变 |
| `@shared/chat/tools/confirmation`: cancelConfirmationsForSession | 改相对 `./tools/confirmation` |

- **runStream gate**：`const cfg = await deps.platform.resolveConfig(); if (cfg.harnessEnabled && backendSupportsTools(kind)) return runHarness({...,platform:deps.platform}); /* else legacy */`。**legacy 单遍整段保留 + platform.persist 化**（字节级零回归 + 既有 legacy/notion-agent 测试绿；D4 删 legacy 推迟 3c）。注意 legacy 每 chunk 同步写用 `streamContent`（fire-and-forget，零回归），done/error 用 `await finalizeMessage`。
- **abortAllChatSessions 拆**：shared 版只 `for (ac of _inflight.values()) ac.abort(); _inflight.clear()`（**去掉 drainNotionAgentGate**）。main `handlers/chat.ts` wrapper：`export function abortAllChatSessions() { dispatcher.abortAllChatSessions(); drainNotionAgentGate() }`（import drain from `../chat/backends/notion_agent_gate`）。**index.ts:15/465 不变**（仍 `from './handlers/chat'`）。
- **StreamSink**：纯类型（`send(ChatStreamEnvelope)`），放 shared dispatcher（或复用 harness 的 `HarnessSink`，二者同构）。`makeWebContentsSink`（用 WebContents）**留 main** —— 移到 `handlers/chat.ts` 顶部或新建 `chat/web_contents_sink.ts`。
- **handlers/chat.ts 改造**：模块级 `const dispatcher = createChatDispatcher({ platform: electronChatPlatform, getBackend: getChatBackend })`；chat:start / editMessage / abort 改调 `dispatcher.*`（注意 abortChatSession 现 async，chat:abort 的 `ipcMain.on` fire-and-forget 不 await OK）；`makeWebContentsSink` 本地定义/import；`abortAllChatSessions` wrapper（+drain）re-export 给 index.ts。

### step 6 — chat_dispatcher.test driver + 全绿 + codex review

- `chat_dispatcher.test.ts`（767 行，23+ test）：现 import dispatcher 的 module functions（startChat/editChatMessage/abortChatSession/abortAllChatSessions/__resetChatDispatcher）。改为 `beforeEach` 构造 `const d = createChatDispatcher({ platform: electronChatPlatform, getBackend: getChatBackend })`（复用真 chat_db 临时库 + `__resetBackendRegistry`/`registerChatBackend` 注册 mock backend，与 harness.test 同模式）。调用改 `d.startChat(...)` 等；`__resetChatDispatcher()` → `d.__resetForTests()`（或每个 test 重新构造）。`tickAsync` 既有轮询模式已就位。
- 跑全 chat 套（rebuild:node + vitest）**15 文件 312 绿** = 3a 零回归基线达成。
- **3a 出口 codex gpt-5.5 high review**（`collaborating-with-codex` skill，read-only sandbox）。

> **3a 不变式**：harness/dispatcher 仍跑在 main（经 ElectronChatPlatform，字节级零回归）。3c 才把执行搬进 renderer（HttpChatPlatform）+ 删 main 直跑路径。别在 3a 提前搬执行。

## 3. 每步验证协议

- 搬 dispatcher 后：`pnpm run typecheck`（node+web）+ `pnpm build:web`（shared/chat 零 Electron）+ `grep -rn "chat/dispatcher'\|electron/main/chat/dispatcher" src tests`（importer 全改）+ **全 chat 套**：
  `pnpm rebuild:node && npx vitest run tests/main/chat tests/main/chat_db.test.ts tests/main/chat_dispatcher.test.ts tests/main/custom_api_backend.test.ts tests/main/notion_agent_backend.test.ts tests/shared/chat-attachments.test.ts`（基线 15 文件 312 passed）。
- 3a 出口：codex gpt-5.5 high review。

## 4. 待办 / 已知

- **ConfirmationTier 重复**（model.ts + tools/registry.ts，codex LOW）：保 3a 纯度暂不 dedup，留 cleanup 归一。
- **lint 全量修复**（用户要求，**最后统一做**）：chat 模块预存债（kos.ts `_ctx` unused×3 + confirmation/dispatch prettier + 全仓）。不要 step 5-6 顺手清（污染 diff）。
- **3b/3c**：见设计文档 §6 + §3.3（模型板/工具板 3b 按真实形状定形）。

## 5. 新 session 开局动作（按序）

1. 读 memory（自动注入）+ 本文件 + 设计文档 §3。
2. 重读源码 ground truth：`shared/chat/platform.ts`（接口）、`chat/electron_platform.ts`（已实现 infra 板，step 5 直接复用其单例）、`chat/dispatcher.ts`（待搬，主体仍直调 chat_db）、`handlers/chat.ts`（待改）、`chat_dispatcher.test.ts`（待改 driver，参考 harness.test 注入 electronChatPlatform 的模式）。
3. step 5：建 `createChatDispatcher`（搬 dispatcher + platform 化含 legacy）→ handlers 接线（构造 + abortAll wrapper + sink）→ typecheck + build:web + grep。
4. step 6：改 dispatcher.test driver → 全 chat 套 312 绿 → 3a 出口 codex review。
