// Sprint 4 §6 — chat panel barrel. S3 W2: the legacy panel pieces (AIChatPanel
// dispatch / Composer / MessageList / QuickActions) were deleted with the
// legacy runtime; what survives are the live building blocks consumed by the
// ai-sdk panel + agent view.
export { BackendSelector, type BackendChoice } from './BackendSelector'
export { ContextChips } from './ContextChips'
