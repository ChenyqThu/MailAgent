# Frontend NOTES

> Small TODOs / issues / questions surfaced during day-to-day Sprint work.
> One line per entry, leading date in ISO. Cross-session blockers / things
> that need backend coordination → `gh issue create` instead.

## TODO

- 2026-05-17 — `src/shared/types/cli.gen.ts` is gitignored (codegen output); fresh checkout must run `pnpm gen:types` once before `pnpm typecheck` works. Consider wiring it into `postinstall` (alongside `electron-builder install-app-deps`) so new clones never see a typecheck miss.
- 2026-05-17 — `EmailGet_EmailRecord.cc_addr` schema declares it as required string but the SQLite column is nullable; DAO substitutes `''` for `null`. If a callsite ever needs to distinguish "no CC" from "empty CC string" we'll have to widen the schema. Low priority — Sprint 2 EmailDetail just renders the string.
- 2026-05-17 — `notion_url` is built as `https://www.notion.so/<id-no-dashes>` because the workspace prefix is private. Sprint 6 SettingsPage should let the user set a workspace-scoped prefix (`/omadanetworks/...`) for direct deep-links.
- 2026-05-17 — `vitest 4.1.6` deprecation: `test.poolOptions` was lifted to top-level; we just removed our `poolOptions.forks.singleFork`. If tests start interfering across files re-add it as the new top-level shape.
- 2026-05-17 — `eslint-plugin-local-rules` is in devDependencies but unused — we load `./eslint-rules/index.cjs` directly via `createRequire`. Remove on next dep-bump pass.
- 2026-05-17 — `no-cjk-in-mono-size` only catches CJK in JSX text nodes; it doesn't resolve `t('key')` against zh-CN locale JSON. If we start using `text-meta` className over a `{t()}` child, the i18n review at sprint close has to be the safety net.
- 2026-05-17 — Light mode visual spot-check still pending (REVIEW-LOG C-08). Once the app launches with `themeMode='light'` from Settings (Sprint 6), the 5 core components need a side-by-side dark/light diff: EmailRow / AIBadge / Toolbar / Composer / Sidebar — but Sprint 1 only ships TitleBar / StatusBar / Sidebar / InboxLayout. The other three appear in Sprint 2-4; carry the spot-check forward. **Sprint 2 update**: EmailRow now has 8-combo snapshot tests (dark/light × selected/unselected × unread/read × failed/normal) — CI gates the structural side; the visual rendering still needs human eyes on light mode once Settings exposes the toggle in Sprint 6.
- 2026-05-17 — Sprint 1 regression: `cli_runner.ts` imported `which@7` as ESM-named `{ sync }` but the package is pure CJS — `pnpm dev` boot blew up with `SyntaxError: Named export 'sync' not found`. Sprint 1's 47-test vitest run mocked `which` away and never tripped this. Fixed by extracting `whichSync` into `src/electron/main/bin_resolver.ts` (createRequire interop) and mocking that module in `tests/main/cli_runner.test.ts`.
- 2026-05-17 — `BACKEND-INTERFACES.md §8` lists `Sentiment` as a V1 AI field, but the actual `labels_json` blob written by `src/llm_agent/processor.py` has no `sentiment` key. AIFieldsBlock renders `—` placeholder in that cell so the grid shape stays stable. If the agent later starts emitting it, `mapSentiment()` in `handlers/ai_mapping.ts` is already a passthrough — no FE change needed.
- 2026-05-17 — `labels_json.action_type` is Chinese ("需要回复" / "仅供参考"). DESIGN.md §14 #2 + §16.6 forbid CJK at `text-micro` (11px mono). EmailRow chip renders an ASCII short-code from `mapActionLabel` in `@shared/lib/ai_labels` (REPLY / DECIDE / MEETING / REVIEW / FYI / FOLLOWUP / WAITING / DONE), with the full Chinese on `title=` for hover-reveal. AIFieldsBlock cell uses `text-aux` (14px sans, CN-safe) so it shows the Chinese verbatim.
- 2026-05-17 — happy-dom 20.x has a `<embed>` parser quirk: even with `USE_PROFILES: { html: true } + FORBID_TAGS: ['embed']`, the opener `<embed src="...">` survives the sanitize call. Sister tags `<object>` / `<iframe>` strip correctly. This is a test-env-only issue; the same DOMPurify config in the real Electron renderer (Chromium DOM) strips `<embed>` correctly. `tests/components/dompurify_xss.test.ts` keeps the `<embed>` assertion as `test.skip` with a note to re-enable when happy-dom fixes its parser.
- 2026-05-17 — Sprint 2 added 3 frontend-only IPCs (`email:listEnriched`, `email:listMailboxes`, `email:aiFields`) that join `email_metadata` + `email_body` (snippet) + `llm_processing.labels_json` (AI labels). These deliberately live outside `cli.gen.ts` so the Sprint 1 schema-conformance tests stay valid; shared types in `@shared/api/types.ts` (`EnrichedEmailMeta` / `MailboxSummary` / `AIFields`) are the single declaration.
- 2026-05-17 — Sprint 2 added `happy-dom` + `@testing-library/react` + `@testing-library/jest-dom` devDeps for the React 19 snapshot tests. The vitest config gained `esbuild: { jsx: 'automatic' }` for `.tsx` test files. Per-file `// @vitest-environment happy-dom` directives opt specific suites in; everything else stays on the node pool with better-sqlite3 native bindings.
- 2026-05-17 — `mailagent` CLI is in the project venv, not the global PATH. Sprint 5 will be the first to call `cli_runner.callCli(...)` for real (writes); developers must either activate the venv before `pnpm dev` or set `MAILAGENT_BIN` env. Today's vitest mocks `whichSync` so this doesn't bite tests, only manual writes once Sprint 5 lands.
- 2026-05-17 — Sprint 2 hotfix surfaced THREE more Sprint 1 boot bugs that no test caught because Sprint 1 never really ran the app: (1) `out/preload/index.mjs` is ESM but `main/index.ts:19` loaded `'../preload/index.js'` → preload silently failed → `window.electron` undefined → every IPC threw "ipcRenderer.invoke missing"; (2) `state/appearance.ts` called `window.electron.send(...)` but `@electron-toolkit/preload`'s electronAPI exposes `ipcRenderer.send`, not `.send` directly → TypeError on every theme/accent commit, crashing the renderer tree; (3) `index.html` inline FOUC bootstrap was blocked by `script-src 'self'` CSP → no theme bootstrap on first paint. Fix: change preload path to `.mjs`; use `ipcRenderer.send`; add the sha256 hash of the FOUC script to script-src CSP.
- 2026-05-17 — `pnpm exec electron-builder install-app-deps` claims success for `better-sqlite3` but does NOT actually replace the prebuilt `.node` binary when there's a NODE_MODULE_VERSION mismatch (Electron 39 ships Node v22.16 = ABI 140; pnpm's prebuilt was for Node 22.20 = ABI 141). The runtime throws `ERR_DLOPEN_FAILED`. Workaround: run `pnpm exec prebuild-install --runtime=electron --target=39.8.10 --arch=arm64` from inside `node_modules/.pnpm/better-sqlite3@.../node_modules/better-sqlite3/`. Fresh clones may bite this — bake into `pnpm postinstall` if it recurs.
- 2026-05-17 — DevTools auto-opens in `pnpm dev` (`webContents.openDevTools({ mode: 'detach' })`) so renderer crashes are visible without Cmd+Opt+I. `console-message` event in main also forwards renderer errors/warnings to the pnpm dev stdout (`[renderer:error] ...`). Strip both for production build (Sprint 7).
- 2026-05-17 — `app.setName('MailAgent')` set before `app.whenReady()` to fix macOS menu bar showing "Electron" in dev (the Electron binary's CFBundleName is "Electron"; production signed .app already says "MailAgent" via electron-builder.yml productName). `BrowserWindow` also gets `title: 'MailAgent'` though `titleBarStyle: 'hiddenInset'` hides the visible title bar.
- 2026-05-17 — Renderer wrapped in `ErrorBoundary` (`@shared/components/ErrorBoundary`) so React 19 render errors no longer blank the window — they show the error message, stack, and component stack so the user can copy-paste to a bug report. Sprint 7 visual polish for the error pane.
- 2026-05-17 — Sprint 3 a11y baseline was 335 WCAG AA color-contrast violations across 12 combinations. **Sprint 4 C-token carry-forward took the count to 0** (7 iteration rounds; index.css chip palette moved to dark/light CSS variables, ink-fg ramp lifted in dark + darkened in light, per-mode accent overrides for all 6 swatches, new `--c-accent-fg` token for CTA-on-accent CTA buttons, `<mark>` pinned to amber/dark text in both modes, and one fixture inline override removed). `pnpm a11y:contrast` is now **`--strict` by default** (CI gate); `pnpm a11y:contrast:json` keeps the dev-mode machine-readable output for debugging.
- 2026-05-17 — Sprint 3 review (codex + opus 4.7 max-effort) carry-forwards — **all 3 resolved in Sprint 4 Day 1**:
  - **(opus H-1) ✓ resolved** — Replaced `useGlobalShortcuts` with `useShortcut(spec, handler, opts)` over a single module-level document.keydown bus (LIFO precedence, opt-in editable-target gating, macOS dead-key alt-glyph normalization). 23 unit tests cover the contract. Sprint 4 AI Chat ⌘L / ⌘↩ / ⌥A / ⌥B / ⌘N can now stack without per-call-site listener bookkeeping.
  - **(opus M-3) ✓ resolved** — Added `tests/components/EmailDetail.test.tsx` covering 6 switch-email cases (101→102, unmount, null→id, id→null, rapid 101→102→103) so a future refactor that drops the `[internalId, mailApi]` dep array fails loudly. mock `useMailApi` returns a spy on `ai.abortTranslate`; the test pins the existing `useEffect` cleanup.
  - Sprint 3 review also surfaced + fixed inline: codex M-1 (AbortController delete race), M-2 (toggle/dismiss abort), M-3 (IPC error envelope so `E_NO_LLM_KEY` survives `ipcRenderer.invoke`), M-4 (DOMPurify `USE_PROFILES` drop + `ALLOWED_URI_REGEXP=/^https?:\/\//`); opus H-2 (60s fetch deadline); opus M-1 (i18n the inline-CTA + retry strings). Plus `electron.vite.config.ts` was missing `@shared` alias for the main + preload builds — production bundle would have failed on Sprint 2 code that already imported `@shared/lib/ai_mapping`. Caught at Sprint 3 ship review.
- 2026-05-17 — Sprint 4 ship (4 commits 30e5578 / 343eb05 / 8d00d81 / 745e36a). Headlines: AI Chat panel (Notion Agent + Custom API dual backends, both wired through main-process IPC stream sink), per-email ai_chat.db (independent SQLite at `~/.mailagent/frontend/`), 8 chat components + i18n (zh-CN + en-US, ICU plurals), keydown bus refactor (useShortcut single document.keydown listener with LIFO precedence), a11y from 335 → 0 violations (chip palette CSS variables + per-mode accent + `--c-accent-fg`), production grep verified 0 hits across 23 sensitive patterns in renderer + preload bundles. Test count 175 → 296 (+121 new across chat_db / dispatcher / backends / useEmailChat / useShortcut / EmailDetail abort).
- 2026-05-17 — Sprint 4 review (codex + opus 4.7 max-effort) carry-forwards — **Sprint 5 will close**:
  - **(codex High)** `useEmailChat` stale-send: if `chat.start()` resolves AFTER an email switch or panel unmount, the stale path can still mutate `activeSessionRef` + accept stream events for the wrong email. Add a generation/current-email ref, and `abortChatSession(result.sessionId)` when state has moved on. **File**: `src/shared/hooks/useEmailChat.ts:226-243`.
  - **(codex M)** `abortCurrent()` only sends the IPC abort; it never clears local `streamingMessageId` or refreshes the aborted row. Some UI states stay stuck in `isStreaming: true` until the next chat event lands. Fix: `setStreamingMessageId(null); refresh(activeSessionRef.current)` inside `abortCurrent()`. Or have main emit a `chat:stream` `{type:'aborted'}` event the hook can react to. **File**: `src/shared/hooks/useEmailChat.ts:242`.
  - **(codex Low)** `AIChatPanel` reads `localStorage.mailagent.notionAgent.pageId` on every render without subscribing to changes — Sprint 6 SettingsPage edits won't reach an already-mounted panel. Wrap with `useSyncExternalStore` or move agent binding into a shared zustand store. **File**: `src/shared/components/chat/AIChatPanel.tsx:43-50`.
  - **(opus M)** `Composer` `useShortcut('cmd+enter', …, { enabled: focused })` — ⌘↩ only fires when focus is inside the textarea. If user clicks a tool row or BackendSelector first, the shortcut goes dark. Either drop `enabled: focused` (rely on `allowInEditable: true`) or scope-match against `aria-label="ai-chat-panel"`. **File**: `src/shared/components/chat/Composer.tsx:53`.
  - **(opus M)** i18n CJK at 11/12px mono: `chat.toolCall.{running,ok,error}`, `chat.truncated`, `chat.status.thinking` render Chinese (`执行中` / `已截断早期 1 条消息` / `AI 思考中…`) at `text-micro` / `text-meta`. DESIGN.md §14 #2 lint rule covers source literals; loaded i18n strings escape it. Either bump those keys' rendering to `text-aux` when locale is CJK, or pick a CJK-readable mono. **File**: `src/shared/components/chat/MessageList.tsx:54,82,164,207,238; ContextChips.tsx:34,46; BackendSelector.tsx:60,94; Composer.tsx:90`.
  - **(opus L)** `notion-agent` backend stuffs `thread_id` into the assistant row's `model` column as `notion-agent:<id>`. Works (the prefix disambiguates) but collides with any future "show real model name" UI. Sprint 5 schema v2 should add a dedicated `metadata JSON` column or `thread_id TEXT`. **File**: `src/electron/main/chat/backends/notion_agent.ts:91-105`.
  - **(opus L)** `MessageList` `scrollIntoView({behavior: 'smooth'})` fires per chunk during streaming — hijacks manual scroll if user reads earlier content. Fix: only auto-scroll if within ~80px of the bottom. **File**: `src/shared/components/chat/MessageList.tsx:195-197`.
  - **(codex N)** `dispatcher.runStream` continues the for-await loop after a backend `error` event. Today neither backend emits `error` then `done`, but if one ever did, the DB would flip `error → complete`. Defensive: set a sticky `sawError` flag and break, or stop forwarding non-error events after first error. **File**: `src/electron/main/chat/dispatcher.ts:218-223`.
- 2026-05-17 — Sprint 5 ship (3 commits): `2d7a128` (Day 1 — 8 Sprint 4 review carry-forwards), `0533eba` (Day 2-4 — Mail.app write commands + BatchActionBar + state machines), `c94d2b0` (ship-review follow-ups closing opus MEDIUM #1 + 2 LOW). Test count 296 → 385 (+89). Headlines: 4 write IPC handlers (createDraft via AppleScript + resync/llm:run/notion:updateFlag via cli_runner), Toast + ToastContainer + zustand store, EmailToolbar 4 ghost buttons接通 + 三按钮 resync confirm dialog, BatchActionBar 52px (DESIGN.md §5.4 — 3 coral AI ops + 3 ghost maintenance), useBatchOps 顺序 loop runner + 两段式 cancel + 进度 toast, useEmailChat 状态机 #3 (retry CTA on retriable errors) + #4 (E_QUOTA 5min cooldown auto-clear timer), AIChatPanel localStorage agentPageId via useSyncExternalStore + QuotaCooldownTimer subcomponent, chat_db v2 schema migration (ALTER ADD metadata JSON column with v1 backcompat read in notion_agent extractTurn).
- 2026-05-17 — Sprint 5 review (opus 4.7 max-effort, codex parked) carry-forwards — **Sprint 6 will close**:
  - **(opus LOW)** `RETRIABLE_ERROR_CODES` missing `E_NOTION_AGENT_FAIL` + Anthropic mid-stream raw `error.type` values (`overloaded_error` / `rate_limit_error` / `api_error`). When CRS returns mid-stream "Claude is overloaded", retryLast doesn't surface even though a retry would likely succeed. Add the codes + emit-site verification. **File**: `src/shared/hooks/useEmailChat.ts:RETRIABLE_ERROR_CODES`.
  - **(opus LOW)** `quotaCooldownUntil` lives in-memory only. App restart inside the 5-min window resets it → user hammers CRS again → upstream re-engages cooldown. Cosmetic, no functional break. Persist via `localStorage('mailagent.chat.quotaCooldownUntil')`. **File**: `src/shared/hooks/useEmailChat.ts:81,263-274`.
  - **(opus LOW)** `useBatchOps` cancelStage 2 — currently dead semantic (loop only checks `>=1`). Wire force-stop via renderer-side `Promise.race` against the in-flight unit, OR delete stage 2 and collapse to binary. **File**: `src/shared/hooks/useBatchOps.ts:84-89,131-140`.
  - **(opus LOW)** Toast TTL closure stays alive past manual dismiss. `push()` schedules `setTimeout(...)` that fires + no-ops if the toast was already dismissed. Track `timerId` on the Toast model + cancel in `dismiss()`. ~5 lines. **File**: `src/shared/state/toast.ts:79-85`.
  - **(opus LOW)** `lastFailedInput` keeps prior message in memory across email switches (set on send, cleared on done). Stranded-session error filtering means retryLast stays correctly null after switch, but the captured input is logically dead. Explicit `setLastFailedInput(null)` in the stranded-session branch of `send()`. **File**: `src/shared/hooks/useEmailChat.ts:312-323` (the !mountedRef || emailIdRef!==myEmail branch).
  - **(opus Nit)** `ResyncConfirmDialog` has no Tab-trap — Tab from "Cancel" escapes to underlying toolbar buttons. For V1 acceptable; for full keyboard a11y add focus-trap (e.g. `react-focus-lock`). **File**: `src/shared/components/email/EmailToolbar.tsx:ResyncConfirmDialog`.
  - **(opus Nit)** `ResyncConfirmDialog` rendered inline inside toolbar — parent unmount during dialog open silently drops it. Portal via `createPortal(..., document.body)` for full lifecycle isolation. Cheap. **File**: same.
  - **(opus Open)** Backend verify: `mailagent email resync --help` to confirm `--no-parent` is the actual flag name and absence means "skip parent lookup" (per CLAUDE.md "默认 `skip_parent_lookup=True`"). Tests assert against the mock; need to verify against the real CLI surface. If CLI flag is actually `--parent` (opt-in), `resyncArgs` in `write_ops.ts:95` is inverted.
- 2026-05-17 — Sprint 6 ship (2 commits): `2dab813` (Day 1 — 6 Sprint 5 review carry-forwards) + `911b337` (main course — 4 secondary routes /admin /llm /calendar /settings). Test count 394 → 432 (+38 handler specs). Headlines: 4 IPC handler files (admin / llm_stats / calendar / settings), keytar extended with llm-api-key + custom-api-key slots, MailApi extended with admin / calendar / settings sub-APIs (ElectronApi wired, HttpApi V2 stubs), 4 Page components + PageFrame shared wrapper, router routes registered, Sidebar onClick navigate접통, 137 i18n keys per locale (zh-CN + en-US), 6 .swatch-* CSS classes in index.css (DESIGN.md §14 #1 — keep .tsx no-raw-hex compliant), production grep clean (keytar 0 / MAILAGENT_CLI_API_KEY only in i18n hint).
- 2026-05-17 — Sprint 6 review (opus 4.7 max-effort, **APPROVE-with-follow-ups**, 0 CRITICAL / 0 HIGH / 4 MEDIUM / 9 LOW / 4 Nit) — **closed inline**: opus MEDIUM #2 (SecretInput captures + clears value synchronously before await, was leaking on rejection), opus MEDIUM #4 (replaced `await import('./llm_stats')` in settings.ts with static import — eagerly loaded anyway via index.ts, dynamic was dead ceremony). **Remaining for Sprint 7**:
  - **(opus MEDIUM #1)** `sanitize()` in settings.ts accepts arbitrary `dbPath` / `attachmentDir` strings; Sprint 7 wires these into `better-sqlite3.Database(s.dbPath)` and an unchecked path becomes directory-traversal. Add `path.isAbsolute() + !contains('..')` check before persisting. **File**: `src/electron/main/handlers/settings.ts:69-92` (sanitize).
  - **(opus MEDIUM #3)** `useBatchOps` dangling `.catch` on stage-2 force-stop: after `Promise.race` resolves with FORCE_STOP, the original `args.unit(id)` may still reject and the `.catch((): UnitResult => ...)` returns an object that's GC'd. Not a correctness bug (CLI subprocess keeps running by contract). Sprint 7: either `void unitPromise` after `break` to await the catch, or add an explicit code comment documenting the dropped-promise contract. **File**: `src/shared/hooks/useBatchOps.ts:116-130`.
  - **(opus LOW)** `WriteEnvelope<T>` + `envelopeFromCli` + `ensureInternalId` duplicated 3× across admin.ts / calendar.ts / write_ops.ts. Extract to `src/electron/main/lib/envelope.ts`. **Files**: `handlers/admin.ts:20-46,128-142` / `handlers/calendar.ts:13-30` / `handlers/write_ops.ts:54-87`.
  - **(opus LOW)** `STORAGE_AGENT_ID` / `STORAGE_AGENT_NAME` / `STORAGE_CHANGE_EVENT` constants duplicated in SettingsPage.tsx + AIChatPanel.tsx — typo drift will silently break live-update contract. Extract to `src/shared/state/notion-agent-storage.ts`. **Files**: `SettingsPage.tsx:43-45` + `AIChatPanel.tsx:26-32`.
  - **(opus LOW)** `AdminPage.formatRelative` hardcoded "Xs ago" / "Xm ago" etc. — i18n miss. Add `admin.timeAgo.{seconds,minutes,hours,days}` keys with ICU template. **File**: `AdminPage.tsx:29-42`.
  - **(opus LOW)** `LlmDashboardPage` cache-token sub-labels "creation" / "read" hardcoded English. Add `llm.cacheTokensCreation` / `llm.cacheTokensRead` keys. **File**: `LlmDashboardPage.tsx:316,322`.
  - **(opus LOW)** `CalendarPage.offsetIsoDate` uses `setUTCDate`/`toISOString` — TZ skew vs CLI's local-time `--since` window at midnight rollover. Switch to local-date construction or document the UTC convention. **File**: `CalendarPage.tsx:25-29`.
  - **(opus LOW)** `AdminPage` `title={sync_error}` exposes full untruncated string as native tooltip (multi-MB Anthropic error blob possible). Truncate to 500 chars matching visible cell. **File**: `AdminPage.tsx:160-164`.
  - **(opus LOW)** `useEmailChat.send()` writes `lastFailedInput = input` BEFORE the stranded-session check — minor race window before line-362 clear. Move `setLastFailedInput(input)` after the stranded check OR document the race as harmless (retryLast is gated by `error !== null` + closure-captured `myEmail`, so no user-visible bug). **File**: `useEmailChat.ts:348-373`.
  - **(opus LOW)** `quotaCooldownUntil` lazy init reads localStorage, then the mirror useEffect writes the same value back on first render — one redundant `setItem` per mount. Track first-render with a ref + skip. **File**: `useEmailChat.ts:127-129,313-324`.
  - **(opus LOW)** `LlmDashboardPage.CacheGauge` threshold cliff at 30% — real cache hit jitter flickers between warn/info bands. Add hysteresis or widen warn band to `< 50`. **File**: `LlmDashboardPage.tsx:170`.
  - **(opus Nit)** `EmailToolbar.ResyncConfirmDialog` Tab focus-trap only handles 3-button boundary case; adding a 4th button breaks the trap. Switch to `react-focus-lock` OR collect focusables via querySelectorAll. **File**: `EmailToolbar.tsx:253-264`.
  - **(opus Nit)** `Sidebar.tsx` hard-codes 8 Chinese labels (已标旗 / 所有邮件 / AI 会话历史 / 一键翻译 / 看板 Admin / 日历 / 设置 / 快捷键) — Sprint 7 i18n sweep target. Add `sidebar.*` keys. **File**: `Sidebar.tsx:154,161,181,207,222,228,237,244`.
  - **(opus Nit)** `EmailDetail.tsx:526,598` hard-codes `"在 Notion 打开"` — should use existing `toolbar.openNotion` key. **File**: `EmailDetail.tsx:526,598`.
